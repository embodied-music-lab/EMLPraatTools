# Handoff cover — door and unification round, 25 Aug 2026

Executing session: this package is the complete, adjudicated work order for
the round. Nothing in it is open — a conflict the documents don't cover
comes back through Ian, not decided in place.

## Contents, in reading order

1. `PUNCH_LIST_DOORS_UNIFICATION_2026-08-25.md` — the work order. Eight
   lanes; each item carries the ruled truth, sites, fix contract, and a
   red-demonstrated acceptance check. The acceptance protocol at the end
   is how the round closes.
2. `LANGUAGE_BATCH_2026-08-25.md` — every new or changed user-facing
   string, numbered. **Build no form or report wording until Ian approves
   this batch**; approved items are then verbatim — amendments come back
   through the batch, not ad hoc.
3. `WIZARD_HARDCODE_REVIEW_2026-08-25.md` — the complete wizard read
   behind the parity lane: 9 frozen choices, 4 draw-handoff sites, and the
   literals verified legitimate (do not "fix" section D).
4. `REPLY_TO_EXECUTING_20260824.md` — rulings on your 24 Aug memo,
   including probe 4 inverted, the reprint rule, and the line-chart row
   targets adopted provisionally.
5. `RISK_REGISTER_2026-08-25.md` — the round's named risks, each with its
   guard and its inspection hook. Part of the work order; changes only
   through Ian.
6. `ERROR_CENSUS_2026-08-25.md` with `error-census/` (row-level data) —
   the error-propagation census behind punch lane 9. Ruled: all 63
   mishandling sites fixed before the tag.
7. `planning/STUDY_DESIGN_MODULE_PLAN_2026-08-25.md` — the study-design
   phase plan; its ROADMAP.md entry (Phase 5d) sits uncommitted in Ian's
   working copy for review and commit. Post-1.0; no build work in this
   round.
8. `Sol Audit 8 24 26/` — Sol's frozen audit ZIP plus
   `EML_Praat_Audit_Continuation_2026-08-25.zip` (the verification
   session's sample verification of Sol's findings, the completed
   psychometrics leg — 154/154 against base R — and the census seed
   table). Treat the pair as one cumulative evidence package.

## Status against the real remote

The push happened; this cover's first edition was written against a stale
remote. Head is `3e34b1a`, the punch list is re-derived against it
(revision 2 — built items marked, they enter the verification queue), and
one local commit — the pitch work — still needs pushing. Language batch
item 10 is re-derived to match the direction wording that shipped.

## Rulings landed since your memo, in one paragraph

Any row reorder invalidates the fingerprint (probe 4 inverted); the report
reprints only when its text actually changed, with the one-line notice.
Two-way stays keyless. Table order stays the group-ordering default, every
comparison report names its ordering and direction, and the setting's
config-file persistence goes (session-only, the Erase-page pattern). The
wizard's contract is "same analyses" — full parity with the menu door.
Post-hocs are never gated on the omnibus, on any door; the hardcoded .05
gates come out, labels lose their gating clauses, and the caution and
effect-size caption lines from the language batch take over the teaching.
Normality summaries state their coverage. Explanations route per the
toggle rules in punch-list lane 6. The grouped-correlation scope control
ships in 1.0.

## Working rules that apply to this round

Author is never verifier — for the checks as much as the code. Every
acceptance check shows red against the pre-fix behavior before it counts.
Batch the photograph re-drives once at the end; re-measure tab indices,
never re-derive them by counting. Evidence and rulings go into the
repository so nothing depends on transport again. When the round is
reported complete, the verification session runs every acceptance leg
independently, then one whole-tree pass for interactions and unlisted
changes, before Ian sees a completion claim.

— verification session, against `2b9ef78`
