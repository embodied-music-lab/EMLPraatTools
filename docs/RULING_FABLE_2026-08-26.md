# Ruling — Fable, 26 August 2026

Verified and ruled on all four points put to her. Recorded verbatim in
substance; the item numbers below are the ones every diff must name, so the
end inspection can tie changes to numbers.

## Process instruction

**EVERY DIFF NAMES ITS ITEM.** A commit that changes behaviour states which
numbered item it serves. The end inspection ties changes to numbers, and a
change that names no item cannot be tied.

## The self-serve list, with attribution

**Item 1.6 — scatter store wiring.** Goes on the self-serve list. ITS
SEQUENCING BEHIND THE CENSUS FIXTURES STANDS: this is not started now.

**Item 3.5 (new) — the two doTukey literals.** The bridge passes a hard
literal `1` for `doTukey` at `graphs/eml-annotation-procedures.praat:4042`
and `:4649`. THE FIX IS THE LAUNCHING DIALOG'S ACTUAL POST-HOC CHOICE
REACHING THE BRIDGE — adding the field if none exists. **Never a different
literal.** Acceptance is the existing `v127` leg; the red demonstration is
the current literal.

**Lane 6 — explanations default.** Built as ROUTING, never as a bare global
flip: the menu seeds OFF, the wizard forces ON, and the graphs door inherits
by launch path.

**Item 2.2 — the two-group "Group order:" line.** Already the remaining
ordering clause of item 2.2. Not a new item.

## The duplicate report — build the minimal renderer, not the capture

The capture approach is rejected. It cannot honour "identical → print
nothing", because under capture the text only exists BY PRINTING; and
strip-list canonicalisation is fragile against lane 6.

**The shape, and punch item 1.2 is amended to name it:** one dual-mode emit
helper — print-and-buffer, or buffer-only — carrying the FACTUAL and
DISCLOSURE lines, on THE TWO STORE-WIRED DOORS ONLY. Explanation lines keep
going through the explain helper, which never buffers. The canonical text is
the buffer. The pre-print comparison runs buffer-only.

## The §d sentence is amended

Ruling `RULING_RESULT_STORE.md` §d's sentence about the census was wrong and
amends to:

> Readers consume only published names; the recorder emits from the
> publication; the store's published names are censused by v138 — the §b
> census (v112) cannot see them by design, since it excludes any global
> assigned inside the doors' own closure, which is where the single write
> site lives.

**DO NOT EXTEND v112.** Instead, `v138` is held to `v112`'s anti-vacuity
standard at inspection: resolver counts, plus a red demonstration seeding one
published-but-uncensused name.

## The August memo items

Answered 24 Aug in reply §4; restated here.

**Line-chart row targets — adopted provisionally at the proposed actuals:**
What the lines are 4; Column Mapping 23 advanced / 11 beginner; Right-Hand
Axis 7. These are the approved targets the checks pin. Rendered counts are
verified under Xvfb at inspection, and a mismatch reopens THE NUMBER, not the
page.

**Legend placement — the per-page maps win.** They are what Ian approved from
the mockups, and five built pages already render that order. Rule 4's
ordering sentence is the defect, and amends to:

> …Gridline mode, Legend placement, Show inner box, …, Font, Output DPI.

**No code moves** for this one.
