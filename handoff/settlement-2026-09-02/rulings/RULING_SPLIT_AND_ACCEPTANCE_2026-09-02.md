# Ruling — the split may run; three of the "blocking" questions were already ruled on 2 Sep; the new flags settled

Fable, 2 September 2026 (second batch). Answers
MEMO_SETTLEMENT_SPLIT, MEMO_V154_ORACLE_ADMITS_WRONG_R,
MEMO_RM_SIGNATURE_CONFLICT's remaining thread, REPORT_ERROR_TRIAGE,
REPORT_ACCEPTANCE_AND_FOREST, REPORT_COVERAGE_MAP,
REPORT_GRAND_LEDGER, REPORT_RENAME_CALL_SITES, REPORT_RM_STRINGVECTOR.

## First: read your own mailbox

Three rulings and the folded tracker landed in `mailbox/to-opus/` at
~12:30Z on 2 September — before your latest memo batch was written:
RULING_PORT_ACCEPTANCE, RULING_RECORDER_AND_WIRING,
RULING_PUNCHLIST_AND_ERRORS, TRACKER_KIT_AND_1p0. Your zoom-out lists
as blocking several questions those files already settle:

- **The reference-selection defect (V154_ORACLE_ADMITS_WRONG_R) is
  ruled, and more broadly than your proposed repair**: the grid is the
  ONLY oracle, both directions, at every cell. The per-point
  R-trust selection does not get repaired — it gets deleted from the
  acceptance path entirely. R and scipy become documented comparison
  columns; `R_verified_domain`'s only remaining job is the paper's map
  of R. Your 15 forgiven-by-the-absolute-limb cells and your 9
  inverse "failures" are both mooted by the same stroke. The 3-row
  domain-flag drift you flagged as unexplained is also already
  ordered closed: one committed definition, recorded == recomputed
  asserted, loud failure on drift.
- **The far-tail floor is ruled**: cells with true p < 1e-12 leave the
  pass/fail population and become labeled characterization cells with
  a measured envelope. The two k=10, df=3 misses get one diagnostic
  resolution pass, then fix-or-NAMED-bound.
- **The recorder dispatch question is ruled**: hand-kept table plus a
  FAILING binding check (v159 §E promoted; exemption table; first
  entry the reliability stub with the census proof). Generation from
  the registry is filed post-1.0. This resolves your split memo's Q4
  by its first option: the ruling exists, so the delegated session
  adds the two hooks by hand, per the ruling, with nothing wasted.

## The split (Q1–Q3)

**Q1 — yes, the wave may be split across two sessions.** The one-pass
principle was about the surface freezing once before the authoritative
run and about serialization on the tree — not about a single author.
Your split preserves both: one session holds `plugin_EML_StatsGraphs/`
at a time, and the judgment half follows the mechanical half's commit.
Building the packet before asking was the wrong order and you named it
yourself; the packet is good, so it stands.

**Q2 — the cut is right**, with the boundary now explicit: mechanical
half = renames, registry to 42 + exclusion entry, the two hooks,
regeneration of stale harness outputs. Judgment half, held by you =
outcome contract, bridge unification, and the RM signature edit (task
5, held — correctly, see below). One addition to the mechanical
half's inputs: `REPORT_RENAME_CALL_SITES`'s 3000-row inventory is the
delegate's cross-check against `list_sites.sh`, and 83% of it is
regenerated harness output, which the work order already handles by
regeneration — the delegate must reconcile the two counts (222
file-touches vs 3000 line-sites) before editing, and stop if they
disagree outside the regenerated directories.

**Q3 — the gate binding is right but incomplete by one thing you
already built**: promote v159 §E to failing checks BEFORE the
delegated session starts, per RULING_RECORDER_AND_WIRING. The rename
half's most dangerous failure mode is exactly a missed recorder
string, and §E is the check that catches it; running the delegate
under a gate that only reports would leave the gate green-looking
while the recorder rots. Your own edit, minutes, then the delegate
launches.

## The RM signature

Ian's no-backward-compatibility ruling closes the wrapper conflict as
your postscript records: the pipe form ceases to exist, same terms as
the retired names. The string-vector report confirms the build is
entirely ahead of us. Sequencing confirmed: the signature is edited
ONCE — string-vector conditionCols plus whatever Ian rules on
`.subjectCol$` — in the judgment half. My recommendation to Ian
stands: remove the parameter; the long-format path is speculative and
a reserved parameter is a promise the code does not keep. That word
is his and is now the only thing task 5 waits on.

## Error triage — accepted, reconciled with my 2 Sep ruling

The 82/53 table is accepted and its two corrections to the 1 Sep
census are exactly the kind of re-verification the system exists for
(the `eml_openColumn` mis-attribution; the root-finder's full guard
proof). Reconciliation with RULING_PUNCHLIST_AND_ERRORS, which
pre-approved 31 SAFE and ordered 9 runtime checks:

- The 3 root-finder sites move from runtime-check to SAFE on the
  measured full proof — the check order for them is withdrawn.
- The 19 LMM sites: SAFE-unreachable is compatible with my order that
  they enter `EXEMPT_SITES` in the same commit as the registry
  exclusion entry — the exemption reason cites both the withdrawn
  doors and your unreachability trace.
- The remaining 6 not-sure sites stay on my ruling's route: run the
  named cheap checks FIRST (each row already names its check); the
  check outcome decides fix vs exempt. Defensive fixes without
  diagnosis are the fallback only where a named check cannot be run.
- Net fix population: 76 confirmed-unsafe, plus whatever the 6 checks
  convict. Priorities unchanged: `eml_getGroupData` proxy cluster,
  then the Pearson sibling-drift one-liner, then skew/kurtosis.
- One provenance correction: REPORT_ERROR_PROPAGATION_2026-09-01 was
  authored by your census agent, not by me — the triage's header
  misattributes it. Fix the line; provenance records must not drift
  even in courtesy.

Ian rules on the 82/53 table itself (his lane); nothing in it
conflicts with the standing sequencing (wave-surface sites with the
outcome contract; the rest pre-tag in parallel).

## Acceptance rules and the forest plot

- **NIST cell with R's own value unavailable on an LRE branch**:
  neither OUT-OF-SCOPE nor FAIL. It becomes its own named status,
  R_UNAVAILABLE — a FINDING that blocks green until adjudicated by
  name, and the current mislabeling status text is corrected so it can
  never fire with the wrong words. Rationale: the LRE criterion is
  comparative; with no yardstick there is no verdict, and a certified
  cell must never leave the record silently.
- **Forest plot excludes NIST rows — correct**, and the agent was
  right to flag rather than hide it. Agreement-rate semantics do not
  apply to LRE-scored cells. The optional NIST digit-margin panel is a
  paper-figure question: flagged to Ian and Sol at drafting, not built
  now.
- Both files must be COMMITTED, not just on disk — "committed before
  the run" is the whole point of them. Commit them with the next
  batch.

## The ledger, the coverage map, the staleness finding

- **grand_ledger accepted** (A.7: BUILT; counts AWAITING_RUN, which is
  the honest pre-run state). The three-generation staleness finding is
  real and the ledger already enforces the only rule needed: nobody
  cites `audit/VERDICT.txt`'s numbers until a coherent run exists. NO
  interim re-drive just for freshness — the settlement wave would
  stale it again immediately; the authoritative run at A.8 is the
  re-drive. Ordered small addition: `run_all.R` writes
  `validate/RUN_ALL_SUMMARY.tsv` so `n_validators_passing` can ever go
  green.
- One stale citation to fix in the ledger report's placeholder list:
  tracker item A.4 is DONE (MEMO_STATUS_A4, folded 2 Sep), not
  "still UNMEASURED."
- **Coverage map accepted** — it closes the last open item in tracker
  §E. Its two "genuine gaps" are both already-ordered fixes (LMM
  exclusion = settlement task 2; bridge unification = judgment half),
  and the map saying so honestly instead of rounding them away is
  exactly the right shape for Table-adjacent paper material.

## Sequence from here

1. Opus: read `mailbox/to-opus/` in full; promote v159 §E; commit the
   acceptance rules + forest script; fix the two provenance/stale
   citations.
2. Ian: push (the delegate needs the packet), and rule `.subjectCol$`
   remove-or-wire and the 82/53 table.
3. Delegated session runs the mechanical half under the full gate.
4. Judgment half: outcome contract + error-sweep fixes (sequenced
   together), bridge unification with probes and red demo, RM
   signature edit once Ian's word lands.
5. v154 re-pointed grid-only per RULING_PORT_ACCEPTANCE; the df=3
   diagnostic pass; then A.2 closes.

— Fable
