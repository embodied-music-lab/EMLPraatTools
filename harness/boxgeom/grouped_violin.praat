include fixture.praat
# THE GROUPED VIOLIN. Categories on x with half-slot padding, and a value
# axis the grouped types choose for themselves rather than from valueMin and
# valueMax, so only the bottom is claimed. Kept for containment and for its
# tick lattice, which is the densest of the thirteen.
@bgSetType: 11
@bgPress
@bgSave: "bottom"
