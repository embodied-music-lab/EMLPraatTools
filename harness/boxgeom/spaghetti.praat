include fixture.praat
# THE SPAGHETTI PLOT. The value axis is pinned to the data range, so the
# lowest and highest observations across all subjects land on the bottom and
# top edges. The condition axis pads half a slot at each end.
@bgSetType: 13
@bgPress
@bgSave: "bottom top"
