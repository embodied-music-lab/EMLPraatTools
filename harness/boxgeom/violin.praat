include fixture.praat
# THE VIOLIN PLOT. The value axis is pinned to 10..120, which is exactly the
# range of v, so the lowest and highest observations land on the bottom and
# top edges of the frame. The category axis pads a half slot at each end.
@bgSetType: 7
groupColName$ = "g"
@bgPress
@bgSave: "bottom top"
