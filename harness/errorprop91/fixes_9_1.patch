diff --git a/plugin_EML_StatsGraphs/graphs/eml-annotation-procedures.praat b/plugin_EML_StatsGraphs/graphs/eml-annotation-procedures.praat
index b7c4ef2..9686403 100644
--- a/plugin_EML_StatsGraphs/graphs/eml-annotation-procedures.praat
+++ b/plugin_EML_StatsGraphs/graphs/eml-annotation-procedures.praat
@@ -4292,6 +4292,13 @@ procedure emlReportAnovaComparison: .tableName$, .dataCol$, .groupCol$, .tableId
                 if emlCohenD.error$ = ""
                     emlOneWayAnova.dMatrix## [.i, .j] = emlCohenD.d
                     emlOneWayAnova.dMatrix## [.j, .i] = -emlCohenD.d
+                else
+                    ; Punch list 9.1, the sibling of the fix in
+                    ; stats/eml-analysis.praat: a failed pair must not read
+                    ; as a true zero effect. The print loop below now shows
+                    ; "n/a" for it.
+                    emlOneWayAnova.dMatrix## [.i, .j] = undefined
+                    emlOneWayAnova.dMatrix## [.j, .i] = undefined
                 endif
             endfor
         endfor
@@ -4328,8 +4335,15 @@ procedure emlReportAnovaComparison: .tableName$, .dataCol$, .groupCol$, .tableId
                 .cellText$ = "---"
             else
                 .dVal = emlOneWayAnova.dMatrix## [.iGroup, .jGroup]
-                @eml_fixed: .dVal, 3
-                .cellText$ = eml_fixed.result$
+                if .dVal = undefined
+                    ; Punch list 9.1. A failed pair prints as unavailable,
+                    ; never as 0 -- same convention as @emlPairwiseT's own
+                    ; d matrix above in this report.
+                    .cellText$ = "n/a"
+                else
+                    @eml_fixed: .dVal, 3
+                    .cellText$ = eml_fixed.result$
+                endif
             endif
             .dRowLine$ = .dRowLine$ + left$ (.cellText$ + "            ", 12)
         endfor
@@ -4765,6 +4779,10 @@ procedure emlReportKWComparison: .tableName$, .dataCol$, .groupCol$, .tableId, .
                 if emlRankBiserialR.error$ = ""
                     emlKruskalWallis.rMatrix## [.i, .j] = emlRankBiserialR.r
                     emlKruskalWallis.rMatrix## [.j, .i] = -emlRankBiserialR.r
+                else
+                    ; Punch list 9.1, sibling of the Cohen's d fix above.
+                    emlKruskalWallis.rMatrix## [.i, .j] = undefined
+                    emlKruskalWallis.rMatrix## [.j, .i] = undefined
                 endif
             endfor
         endfor
@@ -4797,8 +4815,14 @@ procedure emlReportKWComparison: .tableName$, .dataCol$, .groupCol$, .tableId, .
                 .cellText$ = "---"
             else
                 .rVal = emlKruskalWallis.rMatrix## [.iGroup, .jGroup]
-                @eml_fixed: .rVal, 3
-                .cellText$ = eml_fixed.result$
+                if .rVal = undefined
+                    ; Punch list 9.1. Same convention as the Cohen's d
+                    ; matrix above: a failed pair reads "n/a", never "0.000".
+                    .cellText$ = "n/a"
+                else
+                    @eml_fixed: .rVal, 3
+                    .cellText$ = eml_fixed.result$
+                endif
             endif
             .rRowLine$ = .rRowLine$ + left$ (.cellText$ + "            ", 12)
         endfor
diff --git a/plugin_EML_StatsGraphs/stats/eml-analysis.praat b/plugin_EML_StatsGraphs/stats/eml-analysis.praat
index 622a8d2..b7855f2 100644
--- a/plugin_EML_StatsGraphs/stats/eml-analysis.praat
+++ b/plugin_EML_StatsGraphs/stats/eml-analysis.praat
@@ -136,6 +136,13 @@ procedure emlRunTwoGroupAnalysis: .tableId, .dataCol$, .groupCol$, .testType$, .
     .group1$ = emlCountGroups.groupLabel$[1]
     .group2$ = emlCountGroups.groupLabel$[2]
 
+    ; ERROR-READ EXEMPT: both calls below. @eml_getGroupData's only failure
+    ; path is @emlExtractColumn's "Column not found" (eml-extract.praat,
+    ; eml_getGroupData's own header), and .dataCol$ has already been proven
+    ; present and numeric by @emlRequireColumnPresent / @emlRequireNumericColumn
+    ; above (lines 120-134), with a goto out on either refusal. That failure
+    ; path is therefore already excluded by the time either call below is
+    ; reached -- see the fuller explanation at lines 108-119.
     @eml_getGroupData: .tableId, .dataCol$, .groupCol$, .group1$
     .g1# = eml_getGroupData.data#
     .n1 = eml_getGroupData.n
@@ -370,6 +377,16 @@ procedure emlRunAnovaAnalysis: .tableId, .dataCol$, .groupCol$, .doTukey
                 if emlCohenD.error$ = ""
                     emlOneWayAnova.dMatrix## [.i, .j] = emlCohenD.d
                     emlOneWayAnova.dMatrix## [.j, .i] = -emlCohenD.d
+                else
+                    ; Punch list 9.1. Left at the zero## default, a failed
+                    ; pair printed as an indistinguishable "0.000" -- a true
+                    ; zero effect and a computation that could not run read
+                    ; the same in a matrix a reader uses to judge effect
+                    ; sizes. undefined is what @eml_fixed already refuses to
+                    ; round silently, and the printer below now shows "n/a"
+                    ; for it, matching @emlPairwiseT's own matrix.
+                    emlOneWayAnova.dMatrix## [.i, .j] = undefined
+                    emlOneWayAnova.dMatrix## [.j, .i] = undefined
                 endif
             endfor
         endfor
@@ -653,6 +670,12 @@ procedure emlRunKWAnalysis: .tableId, .dataCol$, .groupCol$, .doDunn, .adjMethod
                 if emlRankBiserialR.error$ = ""
                     emlKruskalWallis.rMatrix## [.i, .j] = emlRankBiserialR.r
                     emlKruskalWallis.rMatrix## [.j, .i] = -emlRankBiserialR.r
+                else
+                    ; Punch list 9.1, the rank-biserial sibling of the
+                    ; Cohen's d fix above. Same shape, same reason: a failed
+                    ; pair must not read as a true zero effect.
+                    emlKruskalWallis.rMatrix## [.i, .j] = undefined
+                    emlKruskalWallis.rMatrix## [.j, .i] = undefined
                 endif
             endfor
         endfor
@@ -1972,6 +1995,22 @@ procedure emlRunPairedAnalysis: .tableId, .col1$, .col2$, .testType$
     @emlCSVInit
     @emlReportPairedComparison: .tableName$, .col1$, .col2$, .n, .mean1, .sd1, .median1, .mean2, .sd2, .median2, .testType$
 
+    ; Punch list 9.1. In "both" mode a single arm can fail beside one that
+    ; succeeds (.ranSomething = 1 above already excludes the case where
+    ; NEITHER ran), and until this the report simply omitted that arm's
+    ; section with no word of why -- @emlReportPairedComparison guards each
+    ; block on its own error$ and prints nothing else. The two-group report
+    ; discloses exactly this drop, right after its own report call, in this
+    ; wording; this is that same disclosure for the paired path.
+    if .failParametric$ <> ""
+        .parNote$ = "  Parametric results omitted — Paired t-test failed: " + .failParametric$
+        appendInfoLine: .parNote$
+    endif
+    if .failNonparametric$ <> ""
+        .nonNote$ = "  Nonparametric results omitted — Wilcoxon signed-rank failed: " + .failNonparametric$
+        appendInfoLine: .nonNote$
+    endif
+
     if .nExcluded > 0
         .exclNote$ = "  Note: " + string$ (.nExcluded) + " row(s) excluded for missing data (analyzed n = " + string$ (.n) + " complete pairs)."
         appendInfoLine: .exclNote$
diff --git a/plugin_EML_StatsGraphs/stats/eml-inferential.praat b/plugin_EML_StatsGraphs/stats/eml-inferential.praat
index e9c4c78..acffc92 100644
--- a/plugin_EML_StatsGraphs/stats/eml-inferential.praat
+++ b/plugin_EML_StatsGraphs/stats/eml-inferential.praat
@@ -3075,6 +3075,18 @@ procedure emlRequireNumericColumn: .tableId, .role$, .columnName$, .strict
                 endif
             endif
         endif
+    else
+        ; D148 / punch list 9.1. Until this branch existed, a column that
+        ; does not exist left .error$ untouched (still "" from the top of
+        ; this procedure) and the gate reported success -- the opposite of
+        ; its own name. Every caller in this file asks @emlRequireColumnPresent
+        ; first and so never sees this branch, which is exactly why the hole
+        ; went unnoticed: the miss was always shadowed. A future caller that
+        ; trusts "Numeric" in this procedure's name to also mean "Present" is
+        ; the caller this branch is for. Quotes @emlAuditColumn's own
+        ; message verbatim -- the missing-column diagnosis is made in ONE
+        ; place, and it is not this one.
+        .error$ = emlAuditColumn.error$
     endif
 endproc
 
