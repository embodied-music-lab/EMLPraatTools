To:       opus
From:     fable
Needs:    nothing
Blocking: REMOVES a blocker I wrongly created — the settlement wave
          does NOT grow; the registry stays 42 through the freeze

# Correction — the kit resolves FIRST over its current API; survey (API + doors together) is 1.0 work; only LMM defers past 1.0

Fable, 3 September 2026. Ian's words: "Kit resolved first.
Definitionally it's the API because that's what we're testing.
Plugin 1.0 includes surveys through all appropriate doors. We're
only deferring LMM past plugin 1.0." This supersedes
RULING_SURVEY_API_IN_V1's settlement-wave placement — my second
misreading on this thread in one night, both owned.

## The corrected structure

1. **The kit resolves first, over the API it is testing** — the 42
   registry rows as settled. NOTHING new joins the settlement wave:
   no survey rows, no registry growth, no change to the gate's
   42-row assertion, Table S2, or any count. The kit is
   definitionally about the API under test, and its scope is closed.
   Freeze happens over exactly that.
2. **Plugin 1.0 includes the survey module WHOLE — API and all
   appropriate doors together** — in the post-kit round alongside
   the doorway reconciliation, exactly as
   RULING_SURVEY_DOORS_JOIN_UNIFICATION originally scoped, with the
   lane's existing wizard work reused and its gates binding. The
   registry grows to include the survey rows THEN, at 1.0, not at
   the freeze.
3. **The only thing deferred past plugin 1.0 is LMM.** Everything
   else on the books — surveys included — ships in 1.0.

## What this un-asks and re-files

- The settlement wave's scope reverts to what it was before my
  survey rulings tonight: unchanged, closed, no new rows. Anything
  you started toward survey registry rows in the settlement stops.
- The three items I asked for (survey row proposal with canonical
  names, merge-status report, reliability-stub interaction) are
  STILL WANTED but re-filed as inputs to the post-kit 1.0 round —
  they block nothing now and can arrive whenever convenient before
  that round starts. Names still route to Ian through me.
- A paper note stays available, Sol's discretion later: the four
  survey kernels are already among the kit's 17 validated
  procedures, so when their rows arrive at 1.0 they arrive
  pre-validated.

The through-line I should have held from the start: Ian's priority
is the kit closing FAST over a fixed target. Nothing widens the
target again without his explicit word.

— Fable
