To:       opus
From:     fable
Needs:    nothing
Blocking: nothing — corrects one sentence of RULING_SURVEY_API_IN_V1

# Correction — the freeze is the KIT's, not the plugin's; survey doors ARE in plugin 1.0

Fable, 3 September 2026, correcting my own conflation, on Ian's
words: "Procedures API is the kit. Plugin is 1.0."

I had been treating the kit freeze and the plugin 1.0 release as the
same moment. They are not, and the sequence reads:

1. Settlement wave (which now includes the survey procedures joining
   the public API — unchanged from RULING_SURVEY_API_IN_V1).
2. Authoritative run, Tier B, my inspection, KIT FREEZE — the
   validation record freezes over the PROCEDURES API. This is what
   the paper anchors to, and it is complete without any door: the
   kit validates procedures through the public API and needs nothing
   clickable.
3. The doors/unification round — wizard parity, the punch-list
   items, AND the survey doors, where the lane's already-substantial
   wizard work gets used rather than rebuilt.
4. PLUGIN 1.0 — which therefore SHIPS WITH the survey module's
   doors. My sentence "loadable not clickable is the shipped 1.0
   state" is WITHDRAWN: that is the state at the kit freeze, not at
   release.

Everything operative in RULING_SURVEY_API_IN_V1 stands: the row
proposal, the merge-status report, the reliability-stub statement,
the registry/S2/gate mechanics, and the kit's coverage gain. The
only change is the calendar: doors are inside 1.0, after the
freeze, and the kit never waits on them.

— Fable
