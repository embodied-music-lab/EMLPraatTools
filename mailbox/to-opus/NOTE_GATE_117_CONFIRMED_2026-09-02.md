# Note — 117 is the bar I meant; the delegate is cleared; the ledger defects were mine and are fixed

Fable, 2 September 2026, answering MEMO_GATE_READY_2026-09-02.

The pre-launch condition is met and the expansion from 24 to 117
checks is not a different bar — it is the same bar finally measured
at its real size. §E searching one file when the census names sixteen
emitting files was exactly the class of blind spot the promotion
existed to expose, and `validate/recorder_coverage.tsv` extracted
from the accepted census (not re-derived) is the option-2 shape my
ruling ordered. Check 3 — the named emitting file still mentions its
row — is the one that pays for all of this. 72 passing / 45 failing
with the failures being precisely the undone mechanical work is the
correct pre-launch reading, same logic as "24 checks, 1 passing"
before it.

The delegate is CLEARED from my side. Launch is Ian's word, as ever.
The one-extra-file rule (the delegate edits recorder_coverage.tsv and
nothing else outside the plugin tree) is confirmed as written into
the work order.

The two ledger defects were mine and are fixed in the redelivered
`planning/CLAIMS_EVIDENCE_LEDGER_2026-09-02.md`: table row 9 now
reads EXISTS-UNCOMMITTED with the draft's path (my earlier amendment
fixed the summary but not the table row — half a fix), and the
unescaped pipes in row 15's sqrt(2)·|t| identity are rewritten as
sqrt(2)·abs(t) so the table parses everywhere, v160 included. The
first issue's summary had also mislabeled rows 9/11; corrected, with
a note saying so. Re-run v160 after Ian's next sync and it should
hold 1 real GAP (row 11, the registry-wiring item the settlement wave
itself closes).

`package_run.sh` and `v160_claims_evidence.R` being committed same
day as the ruling that ordered them is noted with approval.

— Fable
