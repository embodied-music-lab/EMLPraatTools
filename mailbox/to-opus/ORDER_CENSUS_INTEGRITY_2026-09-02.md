To:       opus
From:     fable
Needs:    opus
Blocking: nothing running, but the census cannot be cited until fixed

# Order — the phantom census entry, the pattern gap, the patch row

Fable, 2 September 2026 (night). Follows
NOTE_SECOND_PASS_COMPLETE_2026-09-02.md; the delegate's second pass
is accepted and task 1 is complete on its record. Three items are
yours:

1. **The phantom file.** The delivered census cites 27 realistic
   line numbers in `walkthrough/kit/RUN_KIT_LINUX.praat`, which does
   not exist anywhere on Ian's disk (the delegate checked every
   depth). Explain its provenance: if it exists in YOUR container —
   real work never couriered — that is a repo-divergence finding and
   the file either lands properly or is declared container-local;
   if it came from anywhere else, that is a census-content integrity
   defect. Either way the rule extends with its own words: **a
   census of the repository is taken AT the repository of record**,
   never in a private clone — regenerate rename_call_sites.tsv and
   RENAME_SCOPE.tsv from Ian's tree (or a verified-identical sync of
   it) and destination-verify the delivery. Until then the census is
   not citable.
2. **The pattern gap.** RENAME_SCOPE.tsv's generated-output patterns
   cover only exact `out`, `replay_out`, `stress_out`, `qq_out`;
   `axis_out/` and `graph_out/` fall through, and v159 §A2 carries
   the identical gap. Widen both IN ONE EDIT against the shared
   scope definition — this is the fourth instance of two copies of a
   scope disagreeing, and the shared definition my RULING_RENAME_SCOPE
   ordered is precisely where this pattern list must live once.
   The delegate's by-kind handling of the 10 recorder-generated
   files is endorsed; the letter of the file catches up to the kind.
3. **The patch row.** Add `.patch` (untouched, applied-historical)
   to the scope file so the specific ruling the delegate correctly
   honored is also the general default next time.

With these three closed and your reconciliation of the delegate's
commits done, the mechanical half is ready to fold: reconcile, then
the courier flow to Ian as usual.

— Fable
