# Ruling — ptukey correction accepted; one-run moves into the rewrite

Fable, 1 September 2026. Answers `MEMO_PTUKEY_CORRECTION_20260901.md`.
Supplements `RULING_OWN_THE_KERNELS_2026-09-01.md`, which stands.

## The correction is accepted

Version-dependent, not build-dependent — your four-version
measurement replaces the repeated 27 Aug figures, and the retraction
is the right kind: measured, specific, and it improves the
situation, since a version pins and a build does not. The
build-assertion ruling stands anyway: the authoritative run records
and asserts the Praat version, which is exactly the mechanism that
makes this class of question answerable from the evidence instead
of from memory.

## D-PTUKEY: no interim rewrite

Under the owned-kernels ruling the studentised-range path is
replaced and the clause retires before the authoritative run. A
clause that is not in force at the run does not need its text
corrected first — superseded files stay in history as the trail,
and your memo is the record of why the old justification was wrong.
If the replacement were ever descoped, the rewrite you describe
would become mandatory; it is not descoped.

## The recorded-value mismatch: settle it by asking the machine

`audit/praat_results.tsv` carrying the pre-6.6.30 value to sixteen
digits means Ian's machine produced that run on a pre-6.6.30 Praat,
or the file predates an upgrade. Ruled: before the authoritative
run, Ian's Praat version is read from the machine itself (About
Praat, or the next bridge session checks), and if it is below the
pinned 6.6.30 he upgrades to the pin first. Note what your finding
incidentally demonstrates: the sixteen-digit value identified the
producing version from the data alone — the assert-the-version rule
is doing exactly this job, formalized.

The 27 Aug certification is unaffected as a record of its own run:
its bounds were measured against what that machine produced, and it
was green under the rules then in force. The authoritative run is a
different event with the version asserted.

## One-run-per-case: rescheduled into the rewrite, on your evidence

Your 663-of-667 result upgrades `RULING_ONE_RUN_PER_CASE` from
performance to feasibility: at NIST scale the public route does not
complete, so no authoritative run completes without it. Ruled as
you propose: the extraction-caching rewrite happens INSIDE the same
pass as the owned kernels and the API settlement — the repeated
extraction lives in the ANOVA path being rewritten, and writing
that path twice is the exact shape Ian already rejected. The
sequence line in the owned-kernels ruling now reads: settlement +
owned kernels + one-extraction-per-case, one pass. The acceptance
from the one-run ruling stands (call-count probe; SmLs03 through
the public route within the same order as the bypass), and your
twelve-minutes-then-killed measurement is the standing red
baseline.

## Container Praat, and the process error

Running Praat in the container is endorsed for measurements — your
side-by-side version table is the proof of value — and changes
nothing about provenance: the authoritative run stays on Ian's
machine at a pushed commit, version asserted. On the premature full
kit run: correctly caught, correctly uncommitted, and correctly
mined for the one thing in it that was feasibility rather than
counts. Nothing further.

— Fable
