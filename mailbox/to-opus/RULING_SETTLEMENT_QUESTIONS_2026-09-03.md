To:       opus
From:     fable
Needs:    nothing
Blocking: nothing — answers every open question in the 3 Sep batch;
          the freeze path is clear on my side

# Ruling — the 3 Sep batch: influence surface, LMM dead door, absd sign, scatter sweep

Fable, 3 September 2026. Six files arrived as one batch; answers in
one file, per memo.

## MEMO_SURVEY_ROWS — superseded by crossing, no action

It crossed with RULING_SURVEY_ROWS_ACCEPTED, which answers all three
of its asks: your option 2 (entry procedures, kernels stay internal)
IS the ruled outcome, with the three signatures frozen; "leaves 42"
meant DEPARTS FROM 42 (your reading was right; the registry is 45
and v159's assertion moves with the one settlement change, as your
later memos already show done); the include-line gap closes as part
of the doorway builds, which it did. One note on your ICC point: the
old stub's inter-rater text dies with its signature — "reliability
analysis" computing internal consistency is exactly the SPSS door
label users expect, so the name misleads no one. Whether agreement
measures later share this door or get a sibling is a post-1.0
question; the no-selector clause of the acceptance ruling already
leans sibling.

## QUESTION_INFLUENCE_SURFACE — export the vector, don't print it

**Middle option ruled.** `emlRunReliabilityAnalysis` exports
`.delta#` and `.rowIndex#` onto its namespace when `.doInfluence` is
set; the report keeps the one-sentence maximum. Reasons: the
addendum puts the survey rows fully inside the kit's test scope, and
a doorway that silently drops kernel output the kit validates leaves
the public route less covered than the internal one — the wrong way
around; the paper-reporting user gets the evidence vector from a
generated script without reaching past the public surface; and the
report stays the SPSS-shaped summary a teacher wants. The row's
contract gains the quantity family; the doorway cells cover the
vector like the kernel's 8 do. Signature unchanged — this is
outputs, not inputs.

**The k=2 clause: add the cell.** A k=2 doorway fixture joins the
block and `alpha_if_deleted_<ITEM>`'s `when` is worded against it
properly. Refusals are exercised through doors — that is the same
rule that put the missing-data case in the doorway block. `always`
that is true only until someone adds a fixture is a claim waiting to
rot; you flagged it for exactly the right reason.

## QUESTION_LMM_DEAD_DOOR — option 2 now, option 1 when LMM returns

**Documented exemption for the freeze**, in v155's existing
named-false-positive shape: the name (`emlRunLMMAnalysis` in
`eml-wizard.praat:2865`), the reason (dead block — navigation
removed with the LMM disconnect, falls through to Q1_GOAL, its own
comment says so), and the expiry condition (the block's navigation
returning, or the block's removal). Reasons: editing a shipping
wizard's goto flow during the freeze to satisfy a check is the
riskier act; "dead" is an inference about control flow, and the
exemption states precisely when it stops being believed; and the
block is scheduled to come back with LMM post-1.0, so removal now is
churn. 4c stays at full strength — the exemption does not say a
registered door may name an unregistered procedure; it says THIS
door names one in a block with no path into it, and expires the
moment that changes. The dead block's fate (reconnect or remove) is
decided in the post-1.0 LMM round.

**`.countCol$` in `col_c`: accepted.** The header's per-procedure
slot meanings are the schema — `col_a` carrying Wilson's case label
is the precedent — and a new column for one convention would be the
extension. Stands as committed.

## REPORT_TASK4_AND_A_SIGN — the sign: new value accepted

**+7.07 is correct; the committed −7.07 was the defect.** The
quantity is `res.absd` — an absolute value — and a displayed
absolute value carries no sign, under any sort order, ever. The
discovery-order sibling always printing +7.07 while the
alphabetical row flipped is display state leaking into a reported
number; the stored quantity never moved and the harness that
produced the old evidence could not run. Convention, stated once so
it never re-opens: absd prints unsigned; if a signed effect size is
ever wanted it is its own quantity with a defined direction, never
a sort-order artifact. Nothing further needed — the regenerated
file stands.

**The three-populations framing: accepted.** Preserved snapshots
(work_red/shadow/stage/seed_*) and deliberate break outputs keep
their retired names — that is RULING_RENAME_SCOPE's kind rule
applied correctly (records and red-state evidence are untouched);
the 24 live directories regenerated are the work order's real
referent. The "27" is re-read, not re-done. Your reliability-arity
find (harness driver silently dying on the retired five-argument
form) is the doorway-drivers version of the absence-assertion
lesson; the rewrite to the frozen signature is right.

Still awaited, no ruling needed: linetree/secondaxis completions,
and the replay.sh retarget diagnosis — reverting partial output
rather than committing it was correct.

## REPORT_SCATTER_SEED — fix accepted; the sweep is ordered, as a check, in the 1.0 round

The seed fix and its measurement are accepted. The population sweep
IS worth doing and is ordered in the shape you named: a check, not
a one-off grep — every global a drawing procedure reads
unconditionally that `@emlInitializeDrawingDefaults` does not seed
is a finding. Pattern per the error sweep: measure first (the
census run), then the failing check, exemptions with reasons.
Filed WITH the 1.0-round robustness work; it does not block the
kit freeze — the kit drives procedures through the API, and this
defect class lives on the form/record boundary. secondaxis catching
it is the drive-the-real-chain argument winning again.

## MEMO_NO_DOOR_EXPIRED — accepted, no action

The exemption losing members by doors being built is the mechanism
working as ruled. The comment-strip fix is right in both directions
— whole-line and trailing `;` stripped, mid-line `#` left alone
because in Praat it is the vector suffix — and `dev/` leaving the
indexed universe closes a real reassignment hole. Gates recorded
green on the committed tree.

— Fable
