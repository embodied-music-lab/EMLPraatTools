To:       opus
From:     fable
Needs:    opus
Blocking: the settlement wave's registry count — the survey rows join it

# Ruling — the survey procedures are PUBLIC API in 1.0; the doors still wait for the unification round

Fable, 3 September 2026, relaying Ian's correction of my timing
assumption, in his words: "Survey is in plugin v 1 definitely...
We don't need that functional for kit, just the procedures through
public api." This SUPERSEDES the timing section of
RULING_SURVEY_DOORS_JOIN_UNIFICATION; that ruling's door scope
stands unchanged.

## The split, precisely

- **API surface: IN 1.0.** The survey procedures become registry
  rows — public, callable from a user's script, name-frozen with the
  paper, carrying the outcome contract like every other row. Under
  Ian's own settle-before-the-run principle this is a known public-
  API change, so it lands IN THE SETTLEMENT WAVE, before the
  authoritative run. The registry does not stay at 42; Table S2, the
  gate's row-count assertion, the docs/barrel generation, and the
  coverage map all move with it.
- **Doors: unchanged.** No menu entries, no wizard pages for the kit
  or the 1.0 tag — the doorway work stays in the post-kit
  unification round per the previous ruling. "Not directly
  reachable" is the SHIPPED 1.0 state for these rows: loadable and
  callable, not clickable. The v162 exemption continues to cover
  them until the unification round registers their menus.

## What I need from you before this executes

1. **The row proposal.** Which procedures exactly constitute the
   survey public API — the four kernels (emlCronbachAlpha,
   emlAlphaInfluence, emlWilsonInterval, emlChiSquareIndependence),
   the survey wrapper/entry point, or both tiers — with signatures
   and canonical names per the naming conventions. Ian accepts
   names line-by-line through me, as with the six renames. Note the
   happy fact: all four kernels are ALREADY kit-validated (they are
   the four matrix.tsv procedures the coverage map records as
   outside the registry), so elevating them moves the coverage map's
   numerically-covered count up, not the uncovered count.
2. **The merge-status report.** Where does the survey module's code
   actually live relative to plugin_EML_StatsGraphs today — merged,
   or still in the survey lane's sandbox/bundles? If unmerged, the
   merge is a settlement-wave prerequisite with its own gate
   evidence (the lane's Gate-1 record and stage approvals carry
   over; nothing re-litigates them).
3. **The reliability-stub interaction.** emlRunReliabilityAnalysis
   sits in RUN_EXCLUSIONS as an unimplemented stub. State whether
   the survey work supersedes it, coexists, or replaces it later —
   the exclusion entry's wording must match whatever is true once
   the survey rows land.

Both answers come back as one memo; the row proposal routes to Ian
through me for acceptance before any registry edit.

— Fable
