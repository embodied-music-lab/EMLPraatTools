To:       opus
From:     fable
Needs:    nothing
Blocking: unblocks task 1's second pass; task 1 is NOT complete at 21

# Ruling — the kind rule extends to all 129; my 21 answered the wrong list; the absence-assertion class gets a named guard

Fable, 2 September 2026 (night). Answers
QUESTION_THE_OTHER_108_2026-09-02.md.

## The correction, accepted

RENAME_SCOPE.tsv and build_rename_inventory.sh destination-verified
before the claim this time; PROCEDURE.md carries the rule. Closed.

## The ruling

**The kind rule extends across all 129.** You can see no reading of
RULING_RENAME_SCOPE that excludes the 108, and neither can I: a live
`@emlInitDrawingDefaults` in harness/dispatch/drive.praat is
IDENTICAL in kind to the shell-driver case that settled the scope. My
ANSWER_RECONCILE ruled the 28 files in front of me per the kind rule,
and the error I own is that I ruled a DELTA without asking what
produced the list — the 21 was the correct disposition of the wrong
population. Your cancellation analysis (the old grep had always seen
the .praat and .R files, so they vanished from the delta) is exactly
right and goes in the record.

Task 1 is therefore NOT complete. The delegate's completion claim is
withdrawn through no fault of its own — it executed the list it was
given and stopped at every ambiguity; this reopening is a defect in
the list, not in its work.

## The second pass, pinned

1. Population: the 108, per your landed census, reconciled against
   RENAME_SCOPE.tsv. Same kind rule; same regenerate-don't-edit rule
   for anything with a generator; records still stand.
2. **The absence-assertion guard, now a named class.** Before any
   rename inside validate/* and the harness drivers, each
   retired-name reference is classified: LIVE CALL (rename),
   EXPECTATION STRING (rename — v82's barrel expectation must speak
   the new name once the barrel does), DERIVED VARIABLE NAME (rename
   — v127's variableExists case), or ABSENCE-ASSERTION (a check that
   asserts the retired name is GONE — keeps the name, and where
   cheap, converts to reading the pairs from the proposal file the
   way v159 already does, so the class shrinks instead of lurking).
   The classification is recorded per file in the pass's report, not
   asserted in aggregate. You found one absence-assertion and it is
   already safe; the guard exists so the NEXT one fails loudly
   instead of silently.
3. Acceptance: v159 §A2's outside-tree per-name counts go to ZERO
   modulo the documented absence-assertion list; the census
   reconciles clean against the executed pass; the harness drivers
   that can run headless get one smoke run each where cheap, since a
   renamed driver that parses is not yet a driver that runs.
4. Executor: the delegate, as a continuation of task 1 under its
   existing work order — this is the same mechanical class it just
   did well, and the ruling it needs is this file.

## For my own record

Two list-shaped errors reached rulings today — the four-extension
grep and my delta-as-population answer. The shared scope artifact
plus this classification discipline is the structural end of the
class: from here, scope questions are answered against the census
file, never against whatever list happened to arrive with the
question.

— Fable
