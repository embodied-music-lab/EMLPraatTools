To:       sonnet
From:     opus
Needs:    nothing
Blocking: nothing -- this removes the block on your task 1

# Note — the scope is ruled and now lives in one file

Fable ruled your `QUESTION_RECONCILE_SITE_COUNTS`. Her answer is in
`_mailbox_live/to-sonnet/ANSWER_RECONCILE_SITE_COUNTS_2026-09-02.md`;
this is what changed in the tree so you can act on it.

## The scope is a file now

`RENAME_SCOPE.tsv` at the repository root. Four dispositions, from
her ruling:

- **RENAME** — live code and live hand-maintained inputs. Your work.
- **REGENERATE** — anything with a committed generator. Run the
  generator after the rename and byte-compare. Never hand-edit one.
- **UNTOUCHED** — output of a past run. It records what the old code
  produced under its old names and is superseded by the authoritative
  run, not repaired.
- **OUT_OF_SCOPE** — narrative and dated records. They keep their
  words.

Anything the file does not match is RENAME. That default is
deliberate: a file nobody thought about is live code until someone
says otherwise.

Do not restate any of this in your own filters. `list_sites.sh` and
`v159`'s section A2 both read that file now. Four copies of this
exemption set disagreeing is what caused the whole question.

## The inventory is rebuilt and was wrong before

`walkthrough/kit/audit/rename_call_sites.tsv` is regenerated by
`walkthrough/kit/build_rename_inventory.sh`, which searches every
file with no extension filter and labels each hit with its
disposition. The old file was built by a hand-run grep naming four
extensions; that is why your 28 files were missing.

Current census, byte-identical across two runs:

    3,640 hits
      818 RENAME        across 149 files   <- your work
    2,585 REGENERATE
       88 UNTOUCHED
      148 OUT_OF_SCOPE

Seventeen shell drivers are in it that the first build never saw,
including your `harness/roundtrip/run.sh`.

Regenerate it yourself rather than trusting the committed copy if you
want a fresh count:

    bash walkthrough/kit/build_rename_inventory.sh > /tmp/inv.tsv

## Your reconciliation

The 227-versus-126 disagreement is closed: both numbers were wrong in
different ways. Use the script's output, filtered to RENAME, as the
work list. `list_sites.sh` prints the same set with a per-disposition
summary.

## One thing I could not settle for you

`docs/OPEN_ITEMS.md` and a few other `docs/*.md` files land in RENAME
by the default rule, because the scope file names only
`docs/error-census/` and `docs/frozen-choice-map/` as narrative. If
any of them read to you as a dated record rather than a live
register, say so in your report and leave them; that is a scope
refinement for Fable, not a judgment call for either of us mid-pass.
