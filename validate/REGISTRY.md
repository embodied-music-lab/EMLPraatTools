# Validation registry — EML Stats & Graphs

Ian Howell — Embodied Music Lab — GPL-3.0-or-later

> **If you just want to check that the statistics are right, read
> [`README.md`](README.md) instead.** It is one page, it names the exact
> command, and it walks one number end to end by hand. This file is the full
> reference: what every script covers, the conventions chosen where
> statisticians disagree, the tolerance reasoning, the red-path cases, and an
> honest statement of what is *not* covered. It is long on purpose.

This folder is the validation record for the EML Stats & Graphs statistics
layer. It exists so that the plugin's arithmetic can be checked by someone
with no access to the audit session, the sandbox, or the GUI harness.

**Nothing in this project counts as validated until a script in this folder
tests it.** Values quoted in an audit log or a chat transcript are not
validation. If a statistic is not covered by a script here, treat it as
unvalidated regardless of what any other document claims.

---

## Running it

```
Rscript validate/run_all.R
```

Requires **R and a Praat at or above the plugin's floor** — R tested on 4.3.3,
Praat 6.6.30. This page said "R only" until 16 August 2026, and so did the two
other front-door documents; all three were wrong, and the correction was made
in all three on the same day so they cannot disagree. No R *packages* are
installed, loaded, or needed — that half was true and stays true, and every
quantity the plugin reports that base R does not provide directly is
implemented from its standard definition in `helpers.R`. Nor is the network
touched. But seven validators — `v59`, `v63`, `v64`, `v65`, `v70`, `v71`,
`v77` — resolve a Praat binary and drive it, and each fails the check *a Praat
at or above the plugin's floor is available* when there is none, which takes
the suite's exit status to 1. `validate/README.md` carries the measurement.
The **barren** edition does not substitute: it presents no dialogs, which is
the subject of most of `v59`.

Exit status is 0 if every check passes and 1 otherwise, so it can be wired
to CI unchanged. Individual scripts are also runnable on their own:

```
Rscript validate/v03_rm_anova_greenhouse_geisser.R
```

**The suite exits 0.** It exited 1 by design until 6 August 2026, when the
last undriven red-path case (R7) was driven. See "Reproducing this, as a
reviewer" below for what a passing run does and does not establish.

---

## What is being compared against what

Each check pairs a value the plugin **printed** with a value R **computes**.

**The printed value is read out of a committed capture, not typed into the
script.** `evidence/info/*.txt` holds the Info-window text, taken with
`info$()` written to a file — never transcribed from a screenshot — and
`helpers.R` provides the accessors that read it:

| | |
|---|---|
| `printed(cap, label, field, occurrence)` | a number in the plugin's column format |
| `printed_str(...)` | the raw text, for `p < .001`, `exact`, `---` |
| `printed_cell(cap, section, row, col)` | one cell of a printed matrix, addressed by **column name** |
| `printed_eq(cap, key, which, occurrence)` | the Stats Wizard's `label = value` format |
| `check_floored(...)` | asserts the capture really says `< .001` **and** that R agrees |

They fail loudly on a label that is absent, on an occurrence past the last
match, and on a value that is no longer numeric. They do **not** fail on an
ambiguous label — several matches resolve to the first, silently. This
paragraph claimed otherwise until 6 August 2026, and the ambiguity is real:
"Soprano" matches 5 lines in the v09 capture and 7 in v10, "voice type" and
"task" 2 each in v11. Those reads are correct only because block order is
stable. Call sites that depend on a label matching a known number of lines now
say so with `expect_hits`, which turns a wrong belief into a halt; the reads in
v09, v10 and v11 are pinned that way.

A capture that drifts out of step with a script must break the suite, not
quietly stop testing anything.

This matters more than it may look. Until 5 August every printed value
reached the comparison as a literal typed in by hand, which put an
unwitnessed step in the middle of the chain:

```
Praat prints X  ->  [transcription]  ->  literal in the script  ->  R  ->  compare
```

Running the suite verified the right-hand half only. Had a literal been
copied from R's own output instead of from Praat's, every check would pass
and the suite would validate nothing — the exact failure that
`plugin/dev/tests/REFERENCE_PROVENANCE.md` exists to prevent on the other
side. Six literals remain and each is labelled where it sits:

- **three in `v07`** — `441.0000`, `0.0023`, `0.5000` at lines 249–251. These
  are not properties of the table, as this paragraph said until 6 Aug 2026:
  they are the values the plugin *printed* on the 5 August drive of R2,
  transcribed by hand and compared against R's recompute of the same
  constructed table. **No capture of that drive is committed** —
  `evidence/info/rp_r2_rmanova_info.txt` is the 6 August re-drive, on a
  different table (see the R2 row below). So the transcription is unwitnessed:
  R reproduces all three to the stated tolerance, which is what the checks
  assert and what makes the arithmetic safe, but a reviewer cannot verify from
  this repository that the plugin ever printed them. Treat them as a recorded
  observation, not as evidence.
- **one in `v11`** — an input row count.
- **two in `v13`** — degrees of freedom read off the printed label `F(1,23)`,
  with a separate check asserting that label exists.

The inputs are the exact tables the plugin analysed, committed under
`evidence/csv/`.

That last point is not incidental. **The plugin's demo tables are randomly
generated on each creation.** Creating a fresh `Three groups (N=45)` table
and re-running will not reproduce any number in this suite. Every script
reads a committed input file for that reason, and a reviewer should not
substitute freshly generated data.

Tolerances follow the precision the plugin prints at: 5e-4 against a
3-decimal display, 5e-5 against 4 decimals, and so on. Two kinds of check
need looser tolerances and say so at the call site — a value derived from
two already-rounded printed values absorbs both roundings, and a quantity
that is ill-conditioned in its inputs (a *t* recomputed from an *r* near 1)
cannot be asserted at display precision in that direction at all.

---

## Reproducing this, as a reviewer

**You need R and nothing else.** Tested on 4.3.3. No packages are installed,
loaded, or required.

```
git clone <repo> && cd EMLPraatTools
Rscript validate/run_all.R
```

**Expect exit status 0, and expect the summary line to end `0 FAILED`.** All
checks passing, plus attestations reported separately on their own line and
not counted as checks.

**This document states no total, and that is the fix rather than an omission.**
Until 15 August 2026 this section opened with a figure, and so did the
repository README, `validate/README.md` at four separate sites, and the session
reports. On 14 August an audit found four of them carrying four different
numbers for this one suite — every one correct on the day it was typed, and all
four wrong against the live run, which had moved twice more by the end of that
week. A fifth correction would have restarted the same clock. A total is a
measurement of a thing that moves whenever a validator is added; zero failures
is a contract, which is why zero is the only figure this repository is now
allowed to write down. Everything else is generated from a specific run and
stamped with when it was measured:

```
Rscript validate/run_all.R | tee /tmp/suite.log
Rscript validate/tools/gen_counts.R /tmp/suite.log
```

`validate/tools/check_registry_counts.R` enforces it, and the shape of that
enforcement is the point: it fails on the *presence* of a total, which it can
decide from the documents alone in about a second, with no suite run and no
knowledge of what the right figure would be. The previous checker verified
written totals against a live run — correct, expensive, and powerless, since
knowing a document has drifted has never yet stopped a document from drifting.

Until 6 August 2026 this said "expect exit status 1", because R7 — the
small-range axis case — had never been driven and its placeholder failed on
purpose. R7 was described for a month as an axis case that had to be judged
from a rendered figure, and therefore belonged with the graphing work rather
than in an R suite. That was wrong: the draw procedures call no `beginPause:`,
so they run under `praat --run` with no X server, and an axis is a number. It
is driven now, and the suite has no designed failures left.

An **attestation** is a claim backed by a screenshot or a recorded
observation rather than by anything the script can evaluate. Most are in `v07`;
`v20` carries one, recording that its files came from the shipping
orchestrator rather than a harness. They print as `ATST`, are excluded from the
check count and from the exit status, and are listed separately so that a
report of *n* checks passed means *n* things were tested — not *n* minus
however many could never have failed.

D96 through D99 were failing here until 6 August. They now pass, and they
pass against captures re-driven after the fixes, not against the old ones.

### What a passing run establishes

That the values in the committed captures are correct for the committed
inputs, under the conventions named below, and that the plugin's report is
internally consistent — printed totals equal their printed parts, printed
matrices are symmetric or antisymmetric as their statistic requires, each
printed *t* equals its own printed estimate over its own printed SE.

### What it does not establish

**That the captures came from a real run.** Nothing in R can settle that;
only re-driving Praat can. If you want that assurance the package to ask for
is the plugin, Praat 6.6.30, and the GUI harness under `harness/` — the
suite is deliberately separable from it so that the arithmetic can be
reviewed without any of that apparatus.

It also says nothing about the graphing layer, the error paths, or anything
that is not a number in an Info window.

---

## The optional tiers

`run_all.R` needs **stock R and nothing else**, and that charter is
load-bearing: it is what lets a reviewer with a bare R installation check the
arithmetic without any of this project's apparatus. Three further tiers sit
outside it. None is required by `run_all.R`; each answers a question the base
suite cannot.

Contributed by an external audit (FABL-5) on 6 August 2026, after the response
to that audit asked for them: the claims they make had until then existed only
as assertions in a report, which is the same category of gap as the
unwitnessed transcription described above.

### `validate/tools/check_registry_counts.R` — stock R

This exists because two of the defects in this document were count claims that
I checked by eye and by `grep`, in a document whose subject is count accuracy —
including one where a line-based pattern missed a wrapped `TRUE)` and I
confidently contradicted a correct audit finding.

Its first version parsed every written total out of this document and the
repository README and diffed them against a live run. That version was correct
and it did not work. Diffing a written total against reality tells you the
document has drifted; it does not stop the drift, and it cost a full suite run
— several minutes — to say so, which is precisely why it was not run. By 14
August 2026 four documents carried four different figures and the checker had
been right about all of them for weeks.

It now enforces the opposite rule. **No front-door document may state a suite
total other than zero**, and the checker fails on the presence of one:

```
Rscript validate/tools/check_registry_counts.R
```

No suite run, no arguments, about a second. It reads `README.md`,
`START_HERE.md`, `validate/README.md` and this file, and reports the file, the
line and the offending text. The property that makes this different from what
it replaced is that **it never needs to know the true total** — it decides a
question about the documents, not about the suite, so it cannot itself fall out
of date while six branches are adding validators underneath it. It is
wrap-aware: the first draft walked past `the same move repeated 4058 / times`
in `validate/README.md` because the numeral and its noun sat on opposite sides
of a newline, so it now scans a two-line window as well as each line.

A line carrying `<!-- count-scope: ... -->` is exempt, and the comment has to
say what the number counts. That hatch is for the counts in this document that
are legitimately about something else — `v18`'s headless sweep, the primitives
suite under `plugin/dev/tests/`. It renders as nothing and reads as a
declaration, so a reviewer sees the claim of scope beside the number making it.
On 6 August 2026 a global search-and-replace aimed at this file's stale figures
overwrote the primitives suite's split with the `validate/` headline; the
marker is there so that number is visibly not ours.

Handed a run's log it does one thing more — it checks the run against itself:

```
Rscript validate/run_all.R | tee /tmp/suite.log
Rscript validate/tools/check_registry_counts.R /tmp/suite.log
```

The headline must agree with the `PASS`/`FAIL` lines it summarises and with the
by-script table beneath it, and the failure count must be zero. That is P4 of 6
August 2026 made permanent: the aggregate once summed to 460 against a headline
of 454, because one presentation counted attestations and the other did not.
Two presentations of one run must not disagree — and asking whether they agree,
like asking whether a document states a total, needs no external number.

### `validate/tools/gen_counts.R` — stock R

The other half of the same decision. It turns a run's log into the totals block
— headline, attestations, per-script table — stamped with the date, the commit
and the R version it was measured at, and writes it to standard output. Nothing
in this repository stores its result, because a stored copy is a stale total
with extra steps. Generate it when a report needs it; regenerate it when the
report is next revised.

### `validate/oracle/` — R for the dump, Python for the comparison

Answers "is `helpers.R` right?" against scipy, pingouin, scikit-posthocs and
statsmodels, for the 25 quantities base R does not provide.

The two-file split is the point. `oracle_dump.R` has **`helpers.R` itself**
compute the values and write `oracle_values.csv`; `oracle_check.py` compares
that file against the independent libraries. An oracle that re-implements the
formulas in another language is a transcription, and a transcription can
silently correct the thing it copies. This one tests the functions that ship.
It also fails on a dumped value with no oracle counterpart *and* on an oracle
value with no dump counterpart, so the two halves cannot drift apart quietly.

```
Rscript validate/oracle/oracle_dump.R
python3 -m pip install -r validate/oracle/requirements.txt
python3 validate/oracle/oracle_check.py
```

Currently: **25 statistics, 25 agree**, worst disagreement 8.8e-10 (a
Shapiro-Wilk *p*; every other value agrees to 1e-11 or better).

`oracle_values.csv` **is committed**, as a pinned reference. The dump is
deterministic on committed inputs, so regenerating it must produce no diff —
which makes it a regression detector rather than stale output:

```
Rscript validate/oracle/oracle_dump.R
git diff --exit-code validate/oracle/oracle_values.csv
```

A diff there means `helpers.R` or a committed input changed. That is a
finding, and it is the reason this file is committed rather than ignored.

### `validate/mutation/mutate_drive.sh` — bash, git and R

Answers "would this suite notice if a capture were wrong?" It corrupts one
committed evidence file at a time, re-runs the suite, and requires the suite
to respond — either by gaining a failure over the baseline or by halting. A
suite that stays green under a corrupted capture is validating nothing.

It is baseline-relative rather than count-hardcoded, so it survives the suite
growing. It refuses to run on a dirty `evidence/` tree. A preflight pass applies
every case to a copy before any suite runs; a `sed` pattern that no longer
matches is a **DEAD CASE** and exits 3, because a mutation that cannot fire is
not a pass. A case may opt out only by declaring a named `SKIPPABLE:<reason>`.
Restoration is by file copy under a terminating signal trap — the driver runs no
git command that writes — and is verified by byte-comparing every touched file
with its pre-run copy.

```
bash validate/mutation/mutate_drive.sh
```

Currently: **7 mutations, 7 detected, clean restoration.** Six raise a failure
count; deleting the `Cohen's dz` label halts the harness, which is the
behaviour the accessors promise.

### CI order

```
Rscript validate/run_all.R | tee /tmp/suite.log
Rscript validate/tools/check_registry_counts.R /tmp/suite.log
Rscript validate/oracle/oracle_dump.R
git diff --exit-code validate/oracle/oracle_values.csv
python3 -m pip install -r validate/oracle/requirements.txt
python3 validate/oracle/oracle_check.py
bash    validate/mutation/mutate_drive.sh
```

The first two need only R. The mutation driver needs a clean tree and a git
checkout. Only the third pair needs Python, and it is the only step that may
be skipped without weakening a claim made in this document.

---

## The scripts

| Script | Covers | Input |
|---|---|---|
| `helpers.R` | Shared harness and the statistics base R lacks: Cohen's *d* and *d*z, *r* from *t*, matched-pairs rank-biserial, RM-ANOVA with Greenhouse-Geisser, Kendall's *W* | — |
| `v01_pairwise_welch_bonferroni.R` | *Pairwise comparisons* wrapper: Welch *t*, Bonferroni, Cohen's *d* | `evidence/csv/demo_3groups_input.csv` |
| `v02_pairwise_holm_differential.R` | The same wrapper run twice, one control apart, to separate an applied adjustment from a labelled one. Also asserts, rather than assumes, that the two captures differ only in the adjustment | `evidence/csv/demo_3groups_b_input.csv` |
| `v03_rm_anova_greenhouse_geisser.R` | Stats Wizard RM-ANOVA: *F*, GG ε, condition means, Holm post-hoc | `evidence/csv/demo_rm3_input.csv` |
| `v04_friedman.R` | Stats Wizard Friedman: χ², rank sums, Wilcoxon post-hoc, Holm on ties | `evidence/csv/demo_rm3_input.csv` |
| `v05_paired_t.R` | *Compare paired/repeated*: paired *t*, descriptives, and the graphs-side CSV export row | `evidence/csv/demo_paired_input.csv`, `evidence/csv/pairedLong_results_5aug.csv` |
| `v06_D15_effect_size_defect.R` | D15, now resolved: each paired test reports its own effect size | `evidence/csv/demo_paired_input.csv` |
| `v07_redpath_degenerate_inputs.R` (reported per case as R1–R7) | Red path: inputs that should fail or sit on a boundary. **All seven driven** as of 6 Aug 2026 | generated into `validate/redpath/` by the script itself |
| `v08_twogroup_orchestrator.R` | *Compare two groups*: Welch *t*, Mann-Whitney, Cohen's *d*, Hedges' *g*, rank-biserial | `evidence/csv/v08_twogroup_input.csv` |
| `v09_anova_tukey_orchestrator.R` | *Compare k groups (ANOVA)*: ANOVA table, eta-squared, Tukey matrix, pairwise *d* matrix | `evidence/csv/v09_anova_tukey_input.csv` |
| `v10_kruskal_dunn_orchestrator.R` | *Compare k groups (Kruskal-Wallis)*: *H*, epsilon-squared, mean ranks, Dunn *z* and adjusted *p*, rank-biserial matrix | `evidence/csv/v10_kw_dunn_input.csv` |
| `v11_twoway_orchestrator.R` | *Compare two-way (ANOVA)*: main effects, interaction, partial eta-squared | `evidence/csv/v11_twoway_input.csv` |
| `v12_correlation_orchestrator.R` | *Correlate two columns*: Pearson and Spearman with their *t* and df | `evidence/csv/v12_correlation_input.csv` |
| `v13_regression_orchestrator.R` | *Linear regression*: model, overall *F*, coefficient table, direction | `evidence/csv/v13_regression_input.csv` |
| `v14_descriptive_orchestrator.R` | *Describe Table column*: central tendency, dispersion, quartiles, shape, CI | `evidence/csv/v14_descriptive_input.csv` |
| `v15_normality_orchestrator.R` | *Check normality (all columns)*: three columns, and the parametric/nonparametric recommendation | `evidence/csv/v15_normality_input.csv` |
| `v16_csv_export.R` | The CSV export: every number against R, plus the structural assertions that make the file unambiguous to pivot | `evidence/csv_export/*.csv` |
| `v17_broom_parity.R` | The broom-shaped export: tidy / glance / augment / post-hoc / effect size against R, structurally and numerically | `evidence/csv_export/broom/` |
| `v20_shipping_anova_broom.R` | **CSV migration checkpoint 1.** The SHIPPING one-way ANOVA path (`@emlRunAnovaAnalysis`) in broom's three-file shape: tidy / glance / augment, plus post-hoc and effect sizes as their own frames. Column names AND order asserted against broom's documented contract; every value against base R | `evidence/csv_export/broom/shipping_anova_*` |
| `v21_shipping_paths_broom.R` | **CSV migration, paths 2-11.** The other ten shipping orchestrators in broom's three-file shape, every file written by the orchestrator the menu calls. Asserts that htest paths write NO augment, since broom has none for them, and that the regression augment carries broom's `.hat`, `.cooksd` and leverage-corrected `.std.resid` | `evidence/csv_export/broom/ship_*` |
| `v19_nist_strd.R` | **Tier C.** The plugin against NIST StRD certified values, scored in log relative error. Contributes checks only when the `.dat` files have been ingested; prints a loud SKIP otherwise | `evidence/nist/` |
| `v18_sweep_parity.R` | **Tier B.** One-way ANOVA, Tukey-Kramer and Kruskal-Wallis over a 16-case designed grid: k = 2/3/5, n per cell 3-200, balanced and 6:1 unbalanced, tie-free to heavily tied, 1:1 and 10:1 variance ratios | `evidence/sweep/` |
| `v22_homogeneity.R` | **Ruling 1 numerics.** Brown-Forsythe (median-centred Levene), Welch's *k*-sample *F*, and Games-Howell, over a 17-case grid including the *k* = 2 identity (Welch *F* = Welch *t*²), a +1e6 offset case, a skewed case that would catch a mean-centred "Brown-Forsythe", and eight red paths asserted by exact refusal string | `harness/homogeneity/out/` |
| `v23_qq_points.R` | **The Q-Q figure's own points.** Theoretical axis against `qnorm(ppoints(n, a = 3/8))`, sample axis against `sort(x)`, reference line against `lm()`, and *W* against `shapiro.test()` — so the figure and the reported test are bound to the same points. Also pins the Blom-vs-`qqnorm` plotting-position difference above n = 10, and asserts no temp Table leaks | `harness/qq_out/` |
| `v24_influence.R` | **Ruling 4(d).** Leverage, Cook's distance and the leverage-corrected standardised residual from `@emlOLSInfluence` against `hatvalues()`, `cooks.distance()` and `rstandard()`. Pins the OLD `resid/sigma` form as wrong, so reverting the augment site turns this red. Red paths include a leverage-1 row at both ulp neighbours | `evidence/influence/` |
| `v25_anova_showboth.R` | **Ruling 1 at the report level.** Two captures from the same committed input differing only in data column, so the conditional is asserted in BOTH directions. The ABSENT case carries the constraint: on data that does not trip the check the report must look as it did before the feature existed, and the primary *F* must still be `aov()`'s pooled *F* rather than `oneway.test()`'s | `evidence/info/v25_showboth_*` |
| `v26_twoway_caveat.R` | **Ruling 3(a).** The interaction caveat asserted in both directions from two committed two-way inputs, plus its placement under the table it qualifies rather than under the effect sizes (D98), plus the assertion that the three *F* values are identical whether the caveat fires or not | `evidence/info/v26_caveat_*` |
| `v27_empty_frames.R` | **D111 uniformity guard.** Every Table-consuming draw procedure, given no usable data, must fall back to a unit axis and draw the labelled empty frame with its own disclosure line. The histogram used to `goto` past its own `Axes:` and write a blank white page. Includes a STATIC check that no `goto` returns to the draw library — that construct is how the defect got in. Reads `harness/stress_out/`, so `harness/stress_graphs.sh` must run first | `harness/stress_out/` |
| `v28_column_type_guard.R` | **Column TYPE guard, all twelve orchestrators.** Praat has two column readers that disagree: the row-wise one returns undefined for a text cell, but `Report two-way anova:` numericises the column as a whole and substitutes each value's ALPHABETICAL RANK — so the two-way test reported F = 132.92, p = 6.9e-15 on a column of singers' names, and one bad cell in 48 moved a real F from 34.11 to 0.7356. Asserts the refusal by exact message, that the printed output is EMPTY on refusal (the failure was an empty `error$` beside a full table), and that every legitimate analysis still runs | `harness/coltype/` |
| `v29_figure_disclosure.R` | **The figure-disclosure ruling**, all ten draw procedures: Info window always, the figure only when Annotate is ticked, the user's subtitle never. Asserts the exact skipped-row count in house wording, both directions of the Annotate gate (the OFF direction is the ruling), an `emlSubtitle$` sentinel unchanged across 40 renders, clean data producing no disclosure at all, and that the disclosure box and the form's omnibus box never share a corner. Includes a STATIC ban on `emlSubtitle$ =` across **every** file in `plugin/graphs/`, in three rules: no self-referential append anywhere, no assignment inside any `@emlDraw*` procedure, and a pinned inventory of the legitimate sites so a new one must be argued for. Also covers the three over-cap defects: the grouped-scatter annotation budget, a bar chart's missing group versus a measured zero, and the sub-group ceiling, now 24 = 8 hues x 3 fill patterns. All 24 styles are asserted pairwise distinguishable FROM THE RENDERED IMAGE, per channel, in colour and greyscale -- not from the palette table, which is what let two duplicate slots survive undetected. A negative control with the pattern dimension removed reports 24 confusable pairs | `harness/disclosure/out/` |
| `v30_wizard_state.R` | **An error return keeps the user's columns.** The wizard printed "Nothing has been lost" and re-rendered from a column guess; a user who pressed Run without touching anything had a different analysis reported as theirs. Asserts from before/after Info captures that the fixed one never names a guessed column and runs nothing after the return, plus three source guards: the helper is defined once, no hand-written copy survives, and all 16 call sites are present | `evidence/walks/d117/` |
| `v31_gridmode.R` | **One canonical gridline encoding.** Two incompatible encodings — `1 Both / 2 Horizontal / 3 Vertical / 4 Off` for the seven continuous types, `1 Horizontal / 2 Off` for the seven categorical — shared ONE persisted key, so a scatter drawn with gridlines off left a histogram's dropdown blank and refusing OK, on disk and surviving a restart. Three types clamped by INDEX, so "Horizontal only" silently became "Off". Now one canonical encoding translated at the dialog. The load-bearing checks are the registry ones: the plugin refuses to LOAD if a future graph type omits its entry | `evidence/walks/gridmode/` |
| `v32_legend_geometry.R` | **The plot rectangle is what the user asked for.** The dimensions typed into the graphs form describe the DATA AREA, not the data area plus its furniture: if a legend carves space out of the 6 x 4 someone asked for, "make my figure square" stops being satisfiable, because the plot goes oblong while the file stays square. So the plot rectangle must be IDENTICAL in all five legend placements, and a legend that needs room outside it must grow the SAVED IMAGE instead. **205 renders, from two fixtures.** 42 are the legend matrix — three figure sizes including a square (5 x 5) and a short-and-wide (10 x 3), entry counts 0/1/3/12/24, one label 480 characters wide, colour and greyscale, and a no-legend control at every size — all with no `emlLegendPlacement` declared, which is the calling convention all thirteen graph types still use. The other 15 drive the five placements directly. **A further 46 put a REAL four-group post-hoc comparison matrix under the plot** — the panel `@emlDrawMatrixPanel` renders, populated by `@emlBridgeGroupComparison` from a one-way ANOVA with Tukey HSD — because a matrix occupies a band BELOW the plot and placement 3 wants a band below the plot too. 36 of them are the five placements again at twelve and twenty-four entries per size, each with a legend-free control; 10 are the red paths. **Every number is measured on the rendered PIXELS** by `harness/legend/measure.py`, which finds the frame by thresholding at 50% grey and taking the rows and columns whose longest dark run spans half the image — not read back from what the script believed it drew, because both sides of that comparison are computed by the same arithmetic and move together (v1.23 of `@emlDrawLegend` measured itself at one font size and drew itself at another, and it looked right). PINS: the plot frame is the same rectangle in every placement and at every entry count, in inches exactly and in pixels to within the one-pixel rounding a fractional-inch canvas forces; the measured frame plus the theme's own four margins equals the requested rectangle, in position as well as size; placements 1, 4 and 5 save exactly the requested inches at 300 dpi while 2 widens and 3 heightens; declaring nothing renders the same file as declaring placement 1, pixel for pixel; the square request gives a square file while its frame is 1081 x 1239 px and is not square; nothing is clipped at the canvas edge in any placement. **DISJOINTNESS** is the load-bearing pin of the matrix half, and it is asserted twice on independent evidence: the rectangle `@emlDrawLegend` reported and the band `@emlDrawMatrixPanel` was drawn into do not overlap, AND the count of dark pixels inside the matrix band is EXACTLY the count in the same figure drawn with no legend call — 17621, 16365 and 26149 at the three sizes, both ways, with no tolerance. Red paths: zero entries, one entry, a label wider than the whole frame, and — where the defect was — a matrix set up by a caller that publishes no `totalCanvasHeight`, a matrix as deep as the figure above it, and a matrix measured and suppressed. `totalCanvasHeight` is a FORM local and `@emlInitDrawingDefaults` does not set it, so a standalone script or PraatGen companion asking for placement 3 drew its legend band straight through the panel: 11636 dark pixels of legend inside the matrix band on the default figure, 25564 on the twelve-group one, overprinting the omnibus line and the subtitle naming the correction. Closed in `eml-graph-procedures.praat` v3.29; the open numbers are kept as the guard, and the checks here go red against v3.28. **THE MISSING ASSERTION, ADDED 9 AUG 2026: does the key sit on the data.** Everything above is measured on a GEOMETRY RIG (`harness/legend/case.praat`) that draws two violins under a legend of up to twenty-four entries with the corner hardcoded to `"top-left"`. Every rectangle it measures is correct and none of it is a demonstration of a legend: the key named ten groups that were not in the figure, so "does the legend cover the data it names" was not merely untested but inexpressible; the forced corner meant no part of `@emlPlaceElements` was exercised, so no claim about which corner the product picks could be supported; and on a grouped violin the x-axis already carries the category labels, so the key is redundant furniture there anyway. `harness/legend/series_case.praat` is the fixture that can be wrong: **102 further renders** of a k-series LINE CHART (`@emlDrawTimeSeries`) and a k-group GROUPED SCATTER (`@emlDrawScatterPlot`), driven through the product's own graph-level procedures, where the lines cross so position does not name them, **the number of legend entries IS the number of series** (asserted on three independently produced numbers -- the table's k, the draw procedure's resolved `nGroups`, and the `legendN` `@emlDrawLegend` was handed), and the corner is whatever `@emlPlaceElements` scored -- the fixture names none, and that is checked in the source. Coverage is measured between TWO RENDERS: the figure, and the SAME figure ON THE SAME AXIS with `emlLegendPlacement = 5` so the legend is not drawn, then a count of pixels that are data-coloured in the control and changed in the treatment (`harness/legend/measure_cover.py`; data ink is identified by CHROMA, because every piece of the figure's furniture is achromatic, and the count is taken inside the plot frame because Praat subpixel-antialiases TEXT and the resulting orange/blue glyph fringes are more chromatic than an antialiased data line). PINS, recorded as numbers so a change moves a visible figure: on the 6 x 4 line chart, **1348 data pixels covered with no headroom pass and 0 with one** at five series; 20932 and 7385 at twelve. Where `@emlComputeAnnotationHeadroom` reports the band was afforded the overlap is required to be EXACTLY zero, and where it was capped the figure is required to SAY so and to name the way out; both populations are asserted non-empty. The headroom pass is required never to cover MORE than no pass, over every pair of arms, which holds whichever way that work lands. WHICH CORNER THE PRODUCT ACTUALLY CHOOSES is recorded rather than guessed: the line chart takes **top-left** at every size, the grouped scatter takes **bottom-right**, and the scatter moves to **bottom-left** when the headroom pass runs -- which is the left/right trade `@emlPlaceElements`' own comment predicts and bounds, and the bound (nothing crosses between top and bottom) is asserted over all eighteen pairs. The five placements are re-driven on this real path too, and placement 4 is required to change **nothing at all** in the main file. `harness/legend/` also stopped depending on `harness/stress_cases/_prelude.praat`, which names the plugin by ABSOLUTE path and so silently loaded `/home/claude/EMLPraatTools/plugin` from any tree it was copied into; it now has its own prelude with relative includes, plus an `EML_PLUGIN_ROOT` override that stages the cases against another tree. Static, in the shape of `v27`'s `goto` ban: every viewport a legend renderer selects is the rectangle it was HANDED — its own parameters or the theme's inner rectangle, never a page coordinate, never the figure-level drawn extent — plus the pinned inventory of the four procedures allowed to grow the saved figure, and `@emlAssertFullViewport` pinned as the one statement it is. **Written across the change**: the placement work landed in `eml-graph-procedures.praat` while this file was being written, so the fixture rendered the matrix twice, three hours apart, and both sets of numbers are in the file — what is asserted is the tree it runs against, what is recorded beside it is what the number was before. That is how D135 (the over-wide label overhanging the frame), the empty-legend box and the 30.96-inch layout estimate are pinned. **`harness/legend/run.sh` must run first**; the script HARD STOPS on a missing artefact rather than skipping, for the reason `v27` gives | `harness/legend/out/` |
| `v33_exclusion_parity.R` | **The two bridges exclude the same rows.** A figure and the analysis beside it are drawn from one table by two different readers, and if they disagree about which rows are usable the caption describes a different sample than the plot. One number against one number, per draw procedure, per fixture, both a clean and a dirty arm. The dirty arm is required to actually exclude something — two readers that both drop everything, or both drop nothing, agree perfectly, and the harness hit exactly that vacuous pass on its first run. Seven procedures x two arms are declared through `eml_census`, so a procedure dropped from the harness fails loudly instead of shrinking the check. `harness/parity/run.sh` first | `harness/parity/out/` |
| `v34_label_escape.R` | **A figure's auto-composed title keeps its special characters.** Praat's drawn text treats `%`, `#` and `^` as style escapes, so `@emlSanitizeLabel` escapes them — and it ran a second time over its own output, which destroyed the character instead of protecting it: `Jitter (%)` rendered as `Jitter ( )`. The procedure is now idempotent (normalise, then escape) and this pins the round trip | `harness/disclosure/out/` |
| `v35_assembly.R` | **The plugin ASSEMBLED, not its parts.** On 11 Aug 2026 fifteen menu entry points were dead at parse time — a duplicated `include` of a file containing `label` statements — while the whole R suite, the stress and disclosure harnesses and both round trips were green. Nothing had ever loaded `scripts/eml-lib.praat`, which is what all sixteen shipped wrappers load: the suites exercised a composition no user runs. Asserts every entry point parses, and that the graphs workflow advances through its real dialogs when handed a Table — in ORDER, not merely present. All 26 wrappers are declared through `eml_census`: the old ">= 20" floor passes while six are missing. `harness/wrappers/run.sh` and `harness/gui_e2e/run.sh` first | `harness/wrappers/out/`, `harness/gui_e2e/out/` |
| `v36_stress_output.R` | **The 29 stress cases that were exercised but not validated.** `v27` asserts on the ten `empty_*` cases because that is what it is about; the other 29 — the ones named after the pathologies — had only the driver's smoke-test verdict, "it rendered and Praat did not error". It could not have been written earlier: 22 of the 39 cases called `randomGauss` with no seed, so their ink and chroma churned every run and no value was pinnable. Seeding them (§14) is what unblocked this. Covers all 39 with a declared inventory, pins ink and chroma with a stated tolerance, and asserts what each case is NAMED for — the skipped-row count `violin_undefined` discloses, derived from the fixture rather than copied from the log; the bin count in `hist_1bin` / `hist_200bins` read off the case's own draw call; `violin_hugevalues` and `violin_tinyvalues` as each other's controls across twenty-one orders of magnitude; `legend_cap`'s four box slacks recomputed from the frame. Records two facts nothing had: eight violin pathologies disclose NOTHING (asserted as silence, not assumed), and `violin_longlabels` is the only case whose canvas exceeds 1200 px. `harness/stress_graphs.sh` first | `harness/stress_out/` |
| `v37_determinism.R` | **The harness no longer grades its own homework.** `harness/determinism/run.sh` renders each of the ten Table-consuming draw procedures twice in two separate Praat processes and compares the PNGs — and it was the one harness no R script read, so the 10/10 byte-identical figure was the harness reporting on itself. Determinism is what licenses reading a diff of two renders as a regression, so every byte-for-byte claim downstream inherited that. The driver now emits `DETERMINISM.tsv`; this asserts all ten types present by name, both passes non-empty and equal in size, the logs free of Praat errors — and **re-compares the two PNGs itself, byte for byte off disk**, rather than reading the driver's verdict column. Verified: one flipped byte in one PNG, file length unchanged, is invisible to the verdict and to every size check, and fails here | `harness/determinism/out/` |
| `coverage.R` (reported as `v38`) | **For everything a driver renders, is there some check on it?** Runs LAST in the suite and is the only file here whose unit is the ARTEFACT rather than the validator. Every other check is scoped to a subset its author named, so a population nothing named passes everything: on 12 Aug 2026 the stress artefact held 39 cases of which 29 were asserted on by nothing, for weeks, with the suite green. `eml_census` cannot see that -- `v27` reads all 39 rows and claims ten ON PURPOSE. The map of validator-to-cases is deliberately NOT written down, because a hand-maintained list is one more thing that can disagree with reality; each validator calls `eml_claim()` with the same vector its own checks loop over, and this file reads each artefact's population off disk itself, so both sides come from somewhere other than here. Distinguishes three failures: a case nothing claims, an artefact NO validator reads (the largest form, and what `harness/determinism/out` was until `v37`), and a validator claiming a case never rendered. Names the claimants, so "covered" reads as "covered by what". Verified by reproducing the history: with `v36` removed it fails and names the 29. **This cell read "all five harness artefacts" until 16 August 2026**; the declared list in `coverage.R` names twelve, having grown with `v39`–`v45`, and the figure is written here only because a reader can read it back off that list in one look | the twelve harness artefacts declared in `coverage.R` |
| `v39_record_coverage.R` | **The recorder driven the way a menu drives it.** Population: every capture hook in the plugin — thirteen analysis orchestrators, fourteen draw procedures, the eight auto-conversions the graphs form performs, the annotation bridge and the two scatter-with-statistics paths, thirty-eight in all — declared as a list and censused, so a hook added without a leg fails here rather than shrinking the population. What nothing before it could reach: every earlier test of the recorder started a recording and added steps IN ONE SCRIPT SCOPE, and a menu command ends and takes every variable with it. Re-attaching across that boundary is the entire design of the feature and nothing had ever crossed it. Crossing it found three operations CRASHING while being reported as "recorded nothing", a draw hook that could never fire from a menu invocation, a phrase table the shipped plugin never loaded (so every recording a user could make said `[MISSING PHRASE]` on every step), a recorder asking a Sound for its number of rows — which killed all four acoustic draws, and only with recording switched on, which no unit test does — and a session whose provenance lived in script variables and so reached flush empty, emitting "NOT RECORDED. Nothing in this session named the object it ran on" three lines above a manifest naming that object. Coverage is pinned as a FLOOR, and DIDNOTRUN is separated from `silent`, because a crash and a missing hook leave the step count identical and only one of them is a fact about the plugin | `harness/record_e2e/out/RECORD.tsv`, `recorded.praat` |
| `v40_norecord.R` | **The plugin without the recorder — the same population, the opposite question.** Population: the identical thirty-eight operations `v39` pins, asked whether each RUNS AT ALL when `stats/eml-record.praat` is not loaded. The recorder is optional by design: anything that includes the stats and graphs files directly rather than through a shipped barrel — a hand-written user script, a PraatGen companion, every harness in this tree — gets the analyses and the figures without it, so each capture hook is guarded on `variableExists ("emlRecordLoaded")` rather than on recording state, because Praat only errors on an undefined procedure when it EXECUTES the call. On 12 August 2026 that contract broke twice in one afternoon: twelve analysis hooks went in UNGUARDED, so `eml-analysis.praat` could no longer be loaded without the recorder, and `@emlRunAnovaAnalysis` called `@emlRecordAnova` unconditionally — a procedure that opens with `@emlRecordInit`, so the ANOVA path required the recorder too. Nothing in `validate/` saw either, and the reason is structural rather than an oversight: EVERY SHIPPED BARREL INCLUDES THE RECORDER, so `harness/wrappers` loads the barrels and `harness/record_e2e` loads a barrel and then switches recording on, and both were green throughout. The missing driver is the one that loads the individual files and nothing else | `harness/norecord/out/NORECORD.tsv` |
| `v41_blank_group.R` | **A blank group cell is missing data, not a category.** Population: four fixtures — clean, one blank cell, whitespace only, and a genuine two-group table with one blank — put through `@emlCountGroups`, which matches on the NORMALISED label and had no case for an empty one, so a blank or whitespace-only cell became a level of its own, indistinguishable from a real one. The count is not a display detail. It is *k*, and *k* propagates: `dfBetween = k - 1` and `dfWithin = N - k`; *k*(*k*−1)/2 comparisons in every post-hoc, so one phantom group at a real *k* = 3 gives six adjusted *p*-values instead of three, each inflated; `@emlRunTwoGroupAnalysis` refuses above *k* = 2 and routes the user to ANOVA, so a single blank cell made the *t*-test unavailable on a genuine two-group table; and the draw layer sizes its palette and legend from *k*, so the figure grew an unlabelled entry. Under alphabetical sort the blank also sorted FIRST, taking index 1 and shifting every real group's index. Nothing caught it because no fixture in the tree had a blank GROUP cell — every value-column blank in `harness/` is in a value column — with one exception: `harness/disclosure/probe_exclusion_parity.praat` has stated the defect in writing since it was authored, and no validator consumed it. Documented, unasserted, unfixed | `harness/blankgroup/out/BLANKGROUP.tsv` |
| `v42_legend_room.R` | **The graphs form's two-pass headroom loop, driven.** Population: five draws through `@emlGraphsDrawWithLegendRoom` — the procedure that draws a figure, asks `@emlLegendHeadroomAfterDraw` whether the legend needs y-axis room, and if so THROWS THE FIRST PASS AWAY and draws again on the widened axis — with the pass counter, the axis mode and the per-type axis read-back recorded per case. It sits at file scope with a comment saying it was extracted so that a probe could drive it, and until 13 August 2026 no probe ever did: it, its pass counter and `@emlGraphsDispatchDraw` had never executed outside a live dialog on a real display, part of the whole span of `@emlGraphsWorkflow` from the draw commit onward that nothing had run. `harness/legend` covers the headroom ARITHMETIC by reimplementing the two-pass in its own fixture with room = 0 and room = 1 arms; that is a statement about the maths, not about the procedure where the maths is actually used. What driving it cost is itself the finding: `@emlGraphsDispatchDraw` is a flat dispatch in which EVERY argument is a form global and which selects an outer viewport of `figure_width x totalCanvasHeight`, a variable computed in a pre-dispatch block from the comparison-matrix geometry that nothing outside the dialog had ever run either, so the probe has to supply that state by hand — and has to know each global's exact name, the spaghetti path reading `sp*` rather than `spaghetti*`, which the dispatcher disclosed by refusing them | `harness/legendroom/out/LEGENDROOM.tsv` |
| `v43_form_helpers.R` | **Three procedures inside the graphs form, called only from inside it, with no test of any kind.** Population: seventeen cases over `@emlGenerateUniquePath`, `@emlGraphsCSVDefaultName` and `@emlGraphsCSVRowAnalysis`. None of the three needs a display; the seven thousand lines around them do, so until 13 August 2026 nothing had ever loaded that file outside a live dialog. `@emlGenerateUniquePath` IS THE NON-DESTRUCTIVE-SAVE PROMISE — the figure, the separately saved legend, the CSV export and the recorded workflow script all route through it, its entire job is that an existing file is never silently overwritten, and a regression there does not produce a red test or a wrong number: it destroys a figure the user drew an hour ago, and the user is the one who finds out. `@emlGraphsCSVDefaultName` is why an analysis called `Cohen's d / one-way` cannot put a path separator into a filename. `@emlGraphsCSVRowAnalysis` is the RFC 4180 field-2 reader underneath it, walking the row honouring quoting rather than splitting on commas, because a table legitimately named with a comma quotes field 1 and shifts every later field for any reader that counts naively. And because `fromClipboard_results` is the name produced BOTH by a clipboard with no analysis and by a clipboard whose analysis the parser failed to read, the parser is probed directly as well — section 4 exists because section 3 cannot tell correct from broken | `harness/formhelpers/out/FORMHELPERS.tsv` |
| `v44_tab_walk.R` | **Where Tab actually goes in a Praat pause dialog, measured.** Population: forty-four keyboard walks over seven dialog shapes — no fields, one field, two fields, six fields, checkboxes, a `folder:`, and a default-button probe — forward and reverse, each recording whether the dialog CLOSED, stayed open or opened a chooser, and which button the press reached. `harness/gui_e2e` had stopped at the column-mapping dialog for two days holding two contradictory laws: its header said Praat's pause dialogs are walked with Tab, which visits every focusable widget; its case table sixty lines below said Tab walks the button row exactly. Neither had been measured, and the four counts in that table had never been exercised, because the harness returned before reaching a dialog that needed one — so the file was simultaneously the only statement of the law and unable to test it. The header was right and the table was wrong in the way that does not announce itself: Tab visits every FIELD before it reaches a button, so `Column Mapping tabs=4` lands on Go Back and the harness would have looped that dialog forever; Return in a text entry or on a checkbox presses the DEFAULT button rather than the focused widget, so an off-by-one does not hang, it silently presses something else and the run looks like it worked; Return on an optionmenu opens its dropdown and dismisses nothing; and `folder:` is not an entry at all but a GtkTextView with a Browse button, and GTK text views swallow Tab AS A LITERAL TAB CHARACTER, so on the Save Figure shape no forward count from 0 to 13 reaches a button and every Tab sent is appended to the output folder path. What replaced it is derivable rather than measured per dialog: focus starts at ring position 0, so one shift+Tab wraps backward to the last widget, shift+Tab xN presses the Nth button FROM THE END on every shape whatever its fields, and the count comes from the `endPause:` list in the source | `harness/tabwalk/out/TABWALK_JOINED.tsv` |
| `v45_gui_teardown.R` | **The graphs workflow driven to teardown, and what it left on disk.** Population: five named stages — the main form, column mapping, Graph Complete, Save, Saved — asserted individually AND in sequence, plus the files the save wrote and the direction of every keypress. `v35` asserts that the workflow ADVANCES; nothing asserted that it FINISHES, because until `v44`'s reverse walk nothing had driven it past column mapping, leaving the whole span from the Draw commit onward — the type-specific commit block, the draw, the post-draw dialog, Save, Export Results, Redraw and teardown — unexecuted outside a live display. Driving it found three things invisible from the mapping dialog: the driver was not doing what a wrapper does, jumping straight to `@emlGraphsWorkflow` rather than running an analysis first, so with `annotate = 0` in beginner mode the CSV buffer was empty and "Graph Complete" came up with three buttons and no Exp CSV — a variant of that dialog no user reaches; the driver's include set was three files short of the shipped barrel, which is only detectable by running code that needs them; and the run wrote its figure into the HOME DIRECTORY, because `@emlSaveConfig` persists `lastPNGFolder`, the Save dialog seeds its folder field from it, and the config left in the harness's pref dir said `/root` — a harness that inherits yesterday's config saves wherever yesterday's run was pointed. The branch order is deliberate: Save, then Export CSV, then Redraw, then Done, so the SECOND pass through the workflow is what presses Done and teardown is reached from a re-entered workflow rather than a fresh one | `harness/gui_e2e/out/DIALOGS.tsv`, `ARTEFACTS.tsv` |
| `v46_export_surface.R` | **Which writer an export reaches — a population of CALL SITES, read off the source.** The plugin writes CSV in two formats, the legacy single long file and broom's three frames, and ONE `if` decides which, on whether the analysis declared. Until 13 August 2026 that `if` lived inside `@emlWrapperExportCSV`, so the plugin's other export — the graphs form's post-draw Exp CSV button — could not reach it and called `@emlExportStatsCSV` directly: the same analysis produced three broom frames from the stats menu and one legacy file from the graphs form. Nothing could have caught that, and the two reasons are why this file is static rather than artefact-driven. `v20` and `v21` enumerate the stats-MENU orchestrators — `v21`'s own words are "every file written by the orchestrator the menu calls" — so a second exporter reached from a form is outside that enumeration by construction, not by oversight. `coverage.R` compares rendered cases against claimed cases per artefact; it catches an unclaimed case and even an artefact with no reader, and it cannot catch a path that produces NO ARTEFACT AT ALL, which is what a button nobody has pressed produces. So the population that needed checking was never a set of results. What it pins: exactly one live call of the legacy writer, inside the fork; exactly one place reads the declaration flag to make a decision; no export dialog reaches a writer except through `@emlExportResultFiles`; `@emlGenerateUniquePath` sits in core utilities rather than in the graphs layer, because the non-destructive-save promise has to be reachable from every layer that saves; `@emlWrapperExportCSV` is neither called nor DEFINED; and the panel's folder seed is set at file scope rather than inside a procedure — the fact that survives the deletion. Had the migration been scoped this way, the graphs form would have appeared on day one: there have only ever been two live callers of the legacy writer, and one of them was that button | the plugin source |
| `v47_plugin_folder_name.R` | **The install folder name, agreed between the recorder, the rigs and the instructions.** Population: the rendered output of the one recorder harness that does NOT override the plugin root, compared against an oracle that is executable rather than another literal. Praat's rule is that the folder under `preferencesDirectory$` is `plugin_` plus a name of the author's choosing, so the name is a CONVENTION and cannot be derived from anything in this tree, whose source folder is called `plugin/`. The installed folder is `plugin_EML_StatsGraphs`, and a recorder that spells it differently from the folder Praat installed emits an include block naming a folder that does not exist: the recorder's entire output unrunnable, on every platform, from one misspelling. That has happened here. Three separate things let it stand, and each is closed here. The phase-1 test ASSERTED THE WRONG STRING, so the defect read as correct, and its only assertion touching the include block was that the root is non-empty, which passes for any string at all. `harness/record/roundtrip.sh` — the one harness that RUNS the emitted script and byte-diffs the result, and therefore the one thing that would have failed instantly — overrides `emlRecordPluginRoot$`, which is correct for its purpose and makes the production literal untestable by construction. And nothing read the folder name out of a RENDERED artefact at all. The oracle is `harness/walks/rig.sh` and the two walk libraries, which INSTALL the plugin by symlinking it to `plugin_<name>`: a rig that names it wrongly produces a Praat that cannot find the plugin and those harnesses fail, so the recorder and the rigs cannot drift apart without something breaking | `harness/record_e2e/out/recorded.praat` and the source tree |
| `v48_save_paths.R` | **The Save button pressed on every path that has one — a population of JOURNEYS.** `@emlSavePanel` has ten call sites, and until 14 August 2026 exactly one had ever been pressed by a harness: the graphs one. The other nine were covered by `v46`, which is static — it proves the call site exists and names the panel, and every claim it makes is true and none of them is enough. What a static check cannot see arrived on the first press: `@emlSavePanel: 0, tableName$ + "_two-group", emlLastCSVFolder$`, and nothing seeded `emlLastCSVFolder$`, because its seed had lived inside the procedure the panel superseded and was deleted along with the call. Praat evaluates a procedure's arguments before entering it, so all nine non-graphing paths aborted with "Unknown variable" on the FIRST press of Save in a session and took the user's analysis with them, with the call site present and correct throughout, and `harness/wrappers` green as well, because it asks only whether a wrapper parses and an unbound variable parses. Each leg is asserted to have reached the panel, come back, and written what it wrote as one set under ONE FOLDER AND ONE STEM, which is the whole reason the panel exists. The coverage check is the important one: the leg list is compared against the call sites read out of the source, so an eleventh wrapper that gains a Save button fails this file until somebody drives it — the property whose absence let the Exp CSV button go unpressed for a year. The graphs caller is deliberately NOT driven here and is named as `harness/gui_e2e`'s, so the division of labour is a checked fact rather than a convention | `harness/savepaths/out/<leg>.dialogs.tsv`, `<leg>.artefacts.tsv` |
| `v49_every_path_exports.R` | **Every analysis a user can run can be saved.** AUTHOR RULING, 14 August 2026. Population: TERMINAL BRANCHES — every place the wizard finishes an analysis and hands the user to the post-analysis page — plus every stats wrapper, enumerated out of the source rather than listed here. Three branches did not reach the export step, and the way they failed is the point rather than the fact that they failed: the post-analysis button row is gated on `wizCanDraw` and `wizCanExport`, and a branch that never sets the second simply comes up with no Save button. No error, no empty file, no "Nothing to Export" — the button is absent, and an absent button looks like a decision. It had even been written down as one, in a comment saying those branches "deliberately do not" set the flag "because they fill no result buffer": a sentence TRUE about the code and FALSE about the intent, which once written made the gap invisible to reading. Nothing else could have reached it. `harness/savepaths` presses Save on every CALLER of the panel, and a branch with no Save button is not a caller, so it is outside that population BY CONSTRUCTION — the same shape as the Exp CSV button being outside `v20`/`v21`'s. `v46` reads call sites, and a branch that never reaches one has none to read. `coverage.R` compares rendered cases against claimed cases, and a path that offers no export produces no artefact at all. It also found a fourth gap nobody had named: `scripts/eml-check-normality.praat`, the standalone menu wrapper, ran an orchestrator that had declared correctly for weeks and then offered the user no way to keep the result | the plugin source |
| `v50_api_export.R` | **`@emlExportResultFiles` called as CODE, and the documentation held to the script that ran.** Population: API CALLS — one leg per way a user's own Praat script can call the exporter — plus the example in `plugin/docs/API_EXPORT.md` compared LINE BY LINE against the script the harness actually ran. The procedure has three callers: two are dialogs inside the plugin, and the third is a voice researcher's own script, which the procedure's own header names while reporting a defect against it. That defect was found by reading, fixed on 14 August 2026, and the case it was fixed for stayed undriven. Four things could not reach it, and none of them is a gap in itself. `v46` is static. `v48` is a journey check whose journeys all begin at a dialog, and `@emlSavePanel` has ALREADY established that there is something to export and has ALREADY called `createFolder:` on the folder the user typed — both are the PANEL's preconditions, which an API caller inherits neither of, so the two cases that distinguish the API path are exactly the two that population is constructed to exclude. `v49` enumerates wizard branches: same dialogs, same preconditions. And nothing at all read the documentation, and a documented example nobody runs is a guess about an API — this repository has shipped two of those and both were found by a user. The three no reading could settle: `fresh`, where the declaration flag is not 0 but ABSENT, and the call must come back `declared=0 nWritten=0 reason=empty` without aborting; `collide`, where the BASE is uniqued once against the tidy frame, so a second export must produce a COMPLETE second set under the walked base rather than frame 1 of the new set beside frames 2 and 3 of the old; and the batch pattern, where each analysis must refill rather than accumulate. `.skipped$` is asserted as an absent file that is EXPLAINED rather than missing | `harness/api_export/out/`, the plugin source, `plugin/docs/API_EXPORT.md` |
| `v51_advanced_mode.R` | **Two fixes that rested for a day on nothing but a reading of the source.** Population: one advanced-mode journey through the graphs form, driven — a wrapper's annotate preset surviving a beginner Draw and a Redraw into Advanced, and the annotation bridge's re-declaration. Both are invisible in the ordinary sense: when they fail nothing errors, nothing is missing from the file list, no warning prints. They fail as SILENCE. The preset is consumed ONCE before the outer repeat and was never written to the advanced stash, so a user who asked a wrapper to annotate, drew in beginner mode and switched to Advanced found the box unticked with nothing to say it had ever been set; and `@emlReportBridgeStats` opens with `@emlCSVInit`, which zeroes the declaration flag, so it DESTROYS whatever declaration the calling wrapper's orchestrator made and must re-declare or the export writes the legacy single file. `harness/gui_e2e` draws in BEGINNER mode where `annotate` is forced to 0, so the bridge never runs and the restore branch is never entered — both fixes are outside its journey by construction. `v46` and `v49` are static, and neither a tickbox state nor which procedure made a declaration is a property of the source. And a file-set check cannot separate them either, which is the subtle part: an annotated draw and an unannotated one BOTH leave tidy and glance on disk — same filenames, same shapes, different author. What separates them is the Info report. "Two-Group Comparison" appears ONCE if the preset was lost and TWICE if it survived; measured by driving the harness with the violin restore arm disabled and then restored, 1 section / 1833 bytes broken against 2 sections / 3627 bytes fixed. The ORDER of the drive is itself load-bearing and the first version of the harness got it wrong: toggling straight to Advanced without drawing first proves nothing, because the preset is read before the outer repeat and sets `annotate = 1` directly, so the box is ticked on the first pass whether the restore arm exists or not — and the break test PASSED. A drive that cannot fail is not evidence | `harness/gui_adv/out/` |
| `v52_acoustic_calls.R` | **The acoustic calls are ours; the DSP is Praat's.** AUTHOR RULING, 14 August 2026: Praat's processes are validated within Praat — we do not re-derive its pitch tracker or its cepstrum — and what this project owes is narrower and entirely ours, that the CORRECT process is applied with the CANONICAL PraatGen settings to the right object for the right purpose. Population: the batch module's nine acoustic call sites, pinned argument by argument against APPENDIX_D, plus a live drive of each on a synthesised signal. A parameter set is not the kind of thing that announces its own corruption: change 0.02 to 0.2 in the jitter call and every file still yields a number, the CSV still has the right shape, the suite is still green, and the column is quietly a different measurement than the one its header names. There is no downstream consequence to notice, so the only defence is a pin. Four properties: the canonical lists; the two pitch algorithms routed to their own purposes — filtered autocorrelation for mean F0, raw cross-correlation for the PointProcess that jitter and shimmer are measured from, which is neither interchangeable nor stylistic, and swapping them produces no error and no missing column; floors and ceilings taken as `max()` against their canonical bases, so a user's highest-expected-F0 can widen the search and never narrow it below canon, where a bare assignment would silently octave-halve a soprano study; and `noprogress` on every in-loop analysis command, which over hundreds of files is not cosmetic but blocking. The live half exists because the static half cannot see ARGUMENT ORDER: Praat's positional forms accept a number wherever a number is expected, so a signature that moved the ceiling would set max-candidates to 600 and the ceiling to 0.14 — no error, no warning, a plausible wrong number in every row. Version honesty is part of the file rather than a footnote: written first on a sandbox carrying 6.4.06, where four of the nine cannot run at all, it recorded them as UNRUN and their argument-order evidence as MISSING rather than passing over them, and the gate is kept, so an older sandbox will again report MISSING rather than lie | `harness/acoustic/out/MEASURES.tsv` |
| `v53_batch_flow.R` | **The batch module RUN, not read.** Population: seven corpora driven end to end through the shipped module — the one part of the plugin that reads a folder of somebody's recordings, calls Praat's acoustic extraction over them unattended, and writes a file the user will treat as data. `v52` owns the calls; this owns everything around them, which is where a batch actually fails. A loop that pairs the wrong row with the wrong file has NO symptom — right row count, right columns, a plausible number in every cell, and nothing in the artefact asserting the correspondence — so the corpus is five files at five different F0s whose alphabetical order is deliberately not their creation order, and the check is per row: this stem, therefore this F0, to within 2 Hz. One bad file used to cost the whole night: the read was unguarded and the analysis was attempted at any duration, and the results Table is written to disk AFTER the loop, so an abort at file 372 of 500 produced no CSV at all. The TextGrid branch is the subtlest of the three, because a branch that reads the grid, finds the labelled intervals, fills in the bounds correctly and then measures the WHOLE FILE passes every structural check anyone would think to write — so `c1_split` is 130 Hz for one second and 260 Hz for the next with only the second half labelled, and the gap between an unconstrained 194 and a constrained 260 is the evidence that the constraint applied. Also: a stop sentinel honoured mid-run while a foreign `STOP.txt` comes back byte-identical; the output folder counted as user-designated, with nothing left in the corpus; and six engineered implausible measurements that must WARN and still reach COMPLETE, because APPENDIX_D §7 is explicit that an implausible measurement never ends the script. Section 1 comes first and everything else is a claim about the shipped module only for as long as it holds: `beginPause:` hard-crashes under `praat --run`, so the harness cuts the two dialog stanzas out by line number from anchors it requires to be unique and HASHES the remainder against the shipped file, with the excised text committed for a reader to check rather than take on trust | `harness/batch/out/` |
| `v54_batch_praatgen.R` | **Every OTHER call the batch module makes, against the corpus that defines what they mean.** Population: twenty-three Praat commands — the Strings file list, the results Table, the TextGrid interval queries, the Sound channel handling, the folder scaffolding, the CSV write, the two dialogs, the object lifecycle — each cited to the PraatGen corpus for arity and each RUN, because the corpus settles arity and says so: every `COMMANDS_*.txt` carries a banner that it establishes existence and documented parameter COUNT and "does NOT verify parameter ORDER, parameter TYPES, default values, or semantic correctness". The three `Get mean` calls here share a name across three object classes and differ in arity, which is exactly the case that banner warns about, and a correctly documented signature says nothing about whether the argument you pass is in range. The failure this is shaped around is that one bad file out of five hundred takes the whole run, because the CSV write sits after the loop and a Praat error is not catchable: measured on 6.6.30, four of the module's calls end the PROCESS rather than the file, three of them on an argument taken straight from an unclamped `natural:` field — `Get string:` past the end returns `""` and then `Read from file:` fails on `<folder>/`; `Get number of intervals:` aborts on a tier past the count and again on a POINT tier; `Is interval tier:` aborts past the count, which is why the count must be asked for first. A green end-to-end run cannot see any of it: drive the module over a clean folder of well-formed wavs with matching single-tier grids and every one of these is invisible, because the bugs live entirely in the inputs a careful test folder does not contain — and for a batch tool malformed input is not an edge case but the ordinary one, since the folder is the user's corpus and not ours. A leak cannot be read off the source either: pairing each creation with a `removeObject:` proves the text consistent, not the object list, so the harness takes `numberOfSelected ()` after every iteration. The version floor is reported as a discrepancy rather than asserted as a check, and one command's minimum is recorded as UNKNOWN rather than assumed clear | `harness/batchcmd/out/COMMANDS.tsv` |
| `v55_editor_addressing.R` | **The editor deletes the column you picked.** Population: eleven operations on a table built deliberately wrong — two columns with the SAME NAME and DIFFERENT CONTENTS — driven through the shipped editor and read back cell by cell. `scripts/eml-edit-table.praat` is the only entry point that WRITES to a user's table; everything else reads one and produces a report, a figure or a new object. So its failure mode is not a wrong number in an artefact somebody can re-run: it is the loss of data that has no other copy. Praat's Table commands address columns by LABEL, and labels are not unique — measured on 6.6.30, `Rename column (by number)`, `Append column` and `Insert column` all accept a name a sibling already carries, and a CSV read off disk can arrive holding duplicates. The editor's menus are POSITIONAL, and until 15 August 2026 every operation threw the index away and passed the label, so the editor honoured the SELECTION in the dialog and operated on the FIRST LABEL MATCH: the audit's verifier picked the third menu entry, the B-data column, pressed Delete, and the A-data column died. Nothing announces it — no error, no warning, no undo, and the table that comes out is well formed, with the column count you expect, the header you expect and a plausible value in every cell. Four things covered this file and were blind by construction: a source read sees a correct command with a correctly typed argument, so the census and the parse gate pass on it and would forever, because the defect is in which of the dialog's two outputs is passed; a menu walk sees a working menu, since the delete completes and the count drops by one; a schema check passes on the wrong table as readily as the right one, the two differing only in which values sit under a header that is identical either way; and every other fixture in this tree has unique column names, on which the old code and the new are the same operation. The refusal WORDING is read statically out of the excised dialog text and the behaviour around it is measured live, because a refusal with perfect prose that does not stop the delete is the original defect wearing a hat, and a silent refusal that does stop it is the next bug report | `harness/edittable/out/` |
| `v56_save_guards.R` | **Four ways a Save could end the session at the last step of a completed analysis.** Population: the panel's guards, static then live — a `pre/post` typed into the Base name field, an unwritable target folder and an unwritable parent, the receipt's drawn line count, and the wrapper's numeric probe — with two GUI legs that press Save on values a user supplies rather than on the defaults. They are one family: a value the user is entitled to type reaches a Praat command that raises on it, Praat has no try/catch, and the raise stops the script INSIDE `@emlSavePanel`, so the panel never draws its receipt, never returns, and the caller's repeat-until loop never runs again. The analysis is still computed and completely unreachable, and Praat's recovery text points the user at a window that has closed. `v48` and `harness/savepaths` are the reason this file could be written at all, and they press Save ON THE DEFAULTS — a base name containing no hostile character, a folder writable by construction — so every leg passed, every leg was right to pass, and the defect lives one keystroke past where they stop. A harness that only ever accepts what a dialog proposes is testing the proposal. The overprint was worse than untested, it was UNTESTABLE: `comment:` RESERVES the height of one line and DRAWS whatever it is handed, so a path longer than the dialog is wrapped by the toolkit into two or three drawn lines inside one line's height and prints its tail over the path below, and its only symptom is ink on a screen. That repair is half structural — the receipt's lines are now BUILT by `@eml_saveReceiptLines` and only then drawn, so what is asserted here is a number in a file rather than a thing someone has to look at. And the probe: nothing in the tree had ever selected a Matrix and pressed a stats button, while `setup.praat` registers EML items on Matrix and TableOfReal, so eleven menu entries led straight into a raise on the all-undefined `row` column that coercion manufactures | `harness/savepaths/guards_out/` and the plugin source |
| `v57_export_integrity.R` | **What reaches disk, and what a refusal may claim.** Population: not values and not artefact counts, but the SHAPE of an exported frame against the shape of the press that produced it, and the CONTENT of the sentence an orchestrator hands a wrapper at the moment it declines — both driven on the shipping orchestrators with no dialog in the way, and both directions every time, because a check that only ever sees the failing case cannot tell "refuses correctly" from "refuses always". The 14 August 2026 audit recomputed more than 150 statistics across every shipping path and found no mismatch; its findings are all of the other kind, a correct number surrounded by an incorrect claim about itself. Check normality tests every numeric column in one press and the dialog says so, and Save wrote a tidy frame with ONE data row — the last column — because the orchestrator ran `@emlCSVInit` at its own entry while the wrapper called it once per column, so every pass wiped the pass before it. The exported row was correct to 1e-11 against scipy, which is precisely why nobody noticed: there was nothing wrong to see, only something absent. The rule it holds is INIT ONCE PER PRESS, ACCUMULATE PER LOOP. Every numeric validator is blind to that by construction — `v15`, `v03` and `v05` compare a printed value against a computed one and not one of them counts ROWS — and a validator built to ask "is this number right" cannot ask "is this number the only one left". `v20` is worse than a gap and is why the ANOVA augment frames carried `e/s` under broom's name for `rstandard()` for six days: its line PINS the defect, so correcting the plugin would have turned the suite red — the same quantity is asserted correctly in `v21` and `v24` on the regression arm, and the plugin had both the right check and the wrong one on two arms of one quantity. That correction is not this file's to make and it says so at the site, asserting the identity against the live drive instead. Also: a refusal may only claim what it measured — the D97 refusal told a user "every subject shows exactly the same pattern across conditions" about a population half of which had been removed, the exclusion note printing on the SUCCESS path below results the refusal never reaches — and a failed analysis is a refusal rather than a result, which one path did and another did not | `harness/exportint/out/` and the plugin source |
| `v58_recorder_replay.R` | **What a recorded workflow does when you RUN it.** AUTHOR RULING, 14 August 2026: replay is NON-INTERACTIVE, the SPSS model, where dialogs author syntax and running syntax is headless. Population: a replayed recording compared against TWO references rather than one, plus the emitted file's editable header block edited and re-run. The recorder had two validators before this and both measure the EMISSION — `v39` that a session's steps reach the buffer and the file, `v40` that the layer stays clean when the recorder is absent — and neither runs the file it produced. Running it turned an advanced-mode annotated, jittered violin back into a plain violin: bracket gone, points gone, numbers underneath perfect. Four mechanisms, not one of them an error — the advanced settings are not ARGUMENTS but globals the form sets and the draw procedure reads, so the recorded call carries every argument correctly and still draws a different figure; the bracket is drawn by a third party after the draw returns, and an emitted file cannot include the form, because including it runs it, so a recorded annotated figure was structurally incapable of coming back annotated; the save step replayed as a DIALOG and re-proposed "old stem + new stamp", so each generation grew another timestamp onto the base name; and the include block promised a portability it did not have, its rewrite running in a scope that dies with the first menu command. `harness/record/roundtrip_graph.sh` replays a recorded figure and compares the PNGs byte for byte, which is exactly the right check, and draws a BEGINNER violin, so both defects are outside it by construction and it was green throughout. What separates the states is an integer: one `_YYYYMMDD_HHMMSS` stamp is the panel's convention and two is the accretion; 0 pixels differing from the original and 2740 from a deliberately un-advanced draw of the same data on the same axis is a faithful replay, and the reverse is the defect — an identification rather than an inequality, saying which way it failed rather than merely that it did. `cmp` is refused here on measurement rather than principle: the jitter offsets are seeded identically before each draw, and what survives the seed is a resolved axis emitted to six decimals, which anti-aliases forty marks' edges by at most 4 of 255, and the threshold of 32 sits in the gap between that and a missing bracket's 254. RULING 9 lifts column names into the editable block, one variable per ROLE — the same column in the same purpose in two steps is one variable, the same string used as a two-way factor in one step and a plot's grouping column in another is two, and a figure's axis LABEL is not a column at all — and section 8 does not grep the block, it EDITS it on five lines and runs the file against a table whose columns are named differently, because a renderer that gathers every name into a perfect block and leaves the steps below reading their own literals passes every static assertion anybody would write and is worth exactly nothing. RULING 4 renames the recording object, whose existence IS the recording, so a user tidying the Objects window ends their session; a validator cannot read English, so what is pinned is that the name the recorder CREATES is the name the re-attach LOOKS FOR and the name both Stop commands QUOTE, in all four files — a half-finished rename starts a fresh recording on every menu command and raises nothing | `harness/record/replay_out/REPLAY.tsv` and the recorder source |
| `v59_entry_points.R` | **Every registered button opens its dialog.** Population: REGISTRATIONS — the (object type, script) pairs `setup.praat` writes, read out of that file rather than listed here, so a registration added tomorrow is in the population tomorrow — each DRIVEN against a real object of its registered type under a real Praat. On 14 August 2026 eight of the eleven TableOfReal/Matrix registrations broke the promise, and broke it BEFORE the dialog in native Praat error text, which is the worst place for a plugin to fail because none of the plugin's own error machinery is reached. `To Table: "row"` writes the literal `?` into the row-label column for every row whose label is empty; a Matrix has no row labels at all, so every row of a converted Matrix gets one; `@eml_strictNumericColumn` scans a column for `""` and `--undefined--` before an un-`nocheck`'d read, `?` is neither, and the read raises. A TableOfReal the user LABELLED converts to real labels and works perfectly — same button, same script, same code path, opposite outcome depending on whether the user happened to name their rows, which is why it survived a green suite, a green harness and a live audit leg that drove the labelled case, and why both TableOfReal states are driven here as separate cases. No existing population contains it: `harness/wrappers` runs every script with NOTHING SELECTED and asks only whether it parses, so a wrapper that dies on a Matrix and one that dies on nothing at all are indistinguishable to it, its population being FILES; `v46` reads call sites, and an entry point that never reaches the call has none to read; `harness/savepaths` presses Save, and a path that dies three procedures before the panel is not a caller; `coverage.R` needs an artefact, and a button that dies before its dialog renders none. A static read of `setup.praat` would have said all eleven were fine, because the defect is not in `setup.praat` — it is four procedures downstream, in whichever of FOUR different coercions the script happens to use. The oracle is Praat's own exit status, and it works without a display: `beginPause:` traps headless, so exit 133 with no `Error:` means the script ran, coerced, built its form and asked for the window; `Error:` anywhere means it died first; exit 0 means it never asked | the plugin source, driven live |
| `v60_wrapper_paths.R` | **The buttons after the result, and the verdict on a file.** Population: a post-analysis button ORDER no harness had walked — Draw, and then New — and a file-mode data check compared against Praat's own reader as ground truth. Press Draw and then New on Compare Paired Observations and the entry form came back offering Subject, Condition and Value: the ROLE names of the wide-to-long reshape the spaghetti plot is drawn from, on a reshape that has already been removed. Run then dies with "Column not found: Condition", Back returns to the same form, and only quitting Praat recovers. One shared array does it — the entry form's option lists are built from `emlTableColumnNames.name$[]`, `@emlGraphsWorkflow` re-reads that array for whatever object it is handed, the object here is the reshape, and the wrapper filled the array once before the loop and then trusted it. Separately, Check & repair data in file mode reported "No import problems found" on a CSV whose rows are not all the same width, and Praat's own reader then refused the identical file with "Row 3 incomplete" and produced no Table at all. The check was not wrong — it checked doubled-quote escapes and the header delimiter and found neither — but the sentence it printed was about the FILE, and row-length consistency had never been looked at. Nothing that reads NUMBERS could see either: every statistic on the paired path matched scipy to the ulp before the Draw and after it, and the defect is in what the form OFFERS. Nothing that reads FILES could: both analyses exported correctly, the figure was written, and the file check wrote no file at all. `harness/savepaths` stops at Save; no harness in the tree had ever pressed New, still less pressed New after a Draw, which is the only order in which the array is wrong. `harness/gui_e2e` arrives at the graphs form from the graphs form, so the wrapper it would have corrupted is not in the picture. And no static reading of `eml-check-data.praat` shows the gap, because the gap is between that source and PRAAT'S READER — whose three arms are re-established by the harness on every run rather than pinned here as a claim about Praat, two of them being undocumented, including the one where a surplus field on the FINAL row is discarded silently and the read succeeds with data missing. One audit finding is pinned as REFUTED, so that a later reader tidying up "hardcoded strings" has to argue with a failing check rather than with a comment | `harness/newpath/out/` |
| `v61_graphs_seams.R` | **Five seams in the graphs form: four silent, one fatal.** Population: a live drive of the form on the *k* ≥ 3 nonparametric arm with a SIGNIFICANT omnibus, plus the static seams. An annotated nonparametric draw on three or more groups died with `Unknown variable: emlKruskalWallis.rMatrix##`, and only when the omnibus was significant, because that is the branch on which `@emlBridgeGroupComparison` runs Dunn and `@emlReportKWComparison` then reads a matrix its header says "the orchestrator guarantees". `stats/eml-analysis.praat` does guarantee it, one line after the same call; the graphs bridge is a SECOND orchestrator and guaranteed nothing. The whole graphs session went down with it — no figure, no pause form, a truncated report — and the crash fired precisely when a user had a result worth annotating. `v10` recomputes the same orchestrator against R and cannot see it, because the crash is in the graphs layer's own call sequence into those procedures and not one number changes. `harness/gui_e2e` draws in beginner mode. `v51` is the closest thing that existed and uses the very technique used here, and its journey is PARAMETRIC and two-group, so no arrangement of it could reach the branch — and its toggle-to-Advanced measures the RESCUE rather than the default journey. What separates the states is the three rank-biserial VALUES rather than the matrix HEADING, which is printed one line before the read that used to abort and appears in the audit's own screenshot above an error dialog: scipy puts them at 0.5822, 0.7867 and 0.4400, and the reporter prints 0.582, 0.787 and 0.440. The omnibus *p* is checked FIRST, because a fixture that drifted above alpha would make every check below pass while testing the branch that never had the defect. The other four: one integer for the lost annotate preset, where the driver's own orchestrator writes one banner and the bridge writes a second only if `annotate` was still 1 at the beginner commit; the MULTIPLICITY of one (table, analysis, term, field) key across three Draws, because a row count alone can be argued with — a grouped scatter legitimately writes three analyses — and one press must write each exactly once; a legend file that should not exist, from a "Separate figure" placement persisted by an advanced session and read by a beginner Save, where everything else about the artefact set is identical including the figure; and three static seams, a persisted custom axis label with zero reads and blanked at every page entry, group-column fields live while their tickbox is clear, and a preset channel with no producer anywhere in the plugin | `harness/graphseams/out/` and two source files |
| `v62_graphs_axes_channels.R` | **The axis a figure is drawn on, and the channel it is drawn from.** Population: rendered figures measured as PIXELS, plus a stereo dialog driven on a display. Six audit findings and four author rulings, all one species — the numbers were right and the figure was wrong — and for an instrument whose audience is singing teachers that is the same thing as being wrong. The Mix-to-mono / Left / Right choice was UNREACHABLE: `@emlHandleStereo`, `@emlCheckChannels` and `@emlApplyChannelChoice` had been in the library since v3.18, correct and documented, with zero callers, so any check of the form "the stereo procedures are present" would have been green throughout while asserting something true and meaningless. The evidence is therefore a DIALOG ON A DISPLAY — a window either appears in front of a stereo Sound on its way to a figure or it does not — and the number that makes it matter is that a fixture of 220 Hz left and 330 Hz right handed to `To Pitch` as-is returns a contour at 110.0000 Hz, the fundamental of the mixture, a frequency present in NEITHER channel and in nothing anybody sang, against 220.0000 through the gate with Left chosen. Also here: a sustained note drawn as chaos over an axis whose six ticks all read 200; points outside a typed range drawn OUTSIDE the frame; a one-sided range silently inverted; the annotation panel sitting on a datum; the Mann-Whitney gloss naming the wrong statistic; the y-axis NAME and its tick labels drawn into each other, so "Power (dB)" and "100.10" read as "Powe100.10"; `Column_k` holding SOURCE column k−1 at two coercion doors; one extracted channel Sound left behind per press of the stereo gate, all sharing one name; and a one-bin Spectrum drawn as an empty frame, chased and measured here and deliberately not repaired, because the remedy was the author's to choose. Six things were blind to these. The R suite recomputes what the plugin PRINTS, and the audit re-derived the graphs layer against scipy with no disagreement anywhere. The stress renderer asks whether a figure is non-blank and chromatic, and a collapsed-axis contour is vividly non-blank, as is a scatter with five dots painted in the margin and a panel sitting on a point. The parse gate proves an unreached procedure parses. A static call-site grep is the obvious check and is the one that would have been wrong for the longest. The GUI harnesses drive TABLES, and every fixture in this tree is mono. And a human looking at the figure, for the axis in particular: the collapsed contour LOOKS like data, with structure, excursions and a plausible range of tick numbers, and only reading the six identical labels reveals that the whole vertical extent of the frame is one part in ten thousand of a hertz. For the collision, nothing that reads source and nothing that reads a measurement emitted by a Praat script can see two pieces of ink touching, so the evidence is a COLUMN PROFILE OF THE RENDERED PNG. For the off-by-one, a census of column headers cannot help, because `Column_2, Column_3, Column_4` is a perfectly good set of distinct non-empty names and the defect does not damage the names, it damages what they POINT AT — so the probe's Matrix is filled with `col * 100 + row` and the assertion is about the mapping. Sixty-four deliberate breaks in a shadow tree, all red, and two of them are the point of the exercise: one COMMENTS OUT a call rather than deleting it, and the first version of that check passed because the paragraph EXPLAINING the call site contains its name; one deletes the core call from the gate's body only, and the first version of that check passed because another procedure calls the core from its own body. Both were green and both were worthless until a break test said so. Five checks here have never been red and are named rather than implied otherwise — two are arithmetic done in R on the audit's own published numbers, three are guards on the fixture that say the probe has stopped probing | `harness/graphaxes/out/AXES.tsv`, `STEREO.tsv` and the graphs source |
| `v63_coercion_parity.R` | **One Matrix, three doors, one table.** Population: the three places the plugin coerces a Matrix or a TableOfReal into a Table, driven together in one run — `@emlDescribeCoerceSelection`, `@emlWrapperInit` and `@emlCleanConvertedTable`, written by three separate hands, which on 15 August 2026 disagreed about what the manufactured row-label column should contain: `r1..rn`, empty, and `1..n`. The same Matrix produced a different table depending on which menu item the user pressed. None of the three was broken in a way that raises — each is internally consistent, each opens its dialog, and a validator pointed at any ONE of them goes green. The disagreement is only visible from above all three, which is what this file is. `r1..rn` is the author's ruling of 14 August: a column of bare integers is a column of numbers, so the five-row numeric sniff in every wrapper classifies `1..n` as a measurement, it lands in every column picker looking like data, and a user can select it and get a regression on row order. An EMPTY column is not that hazard but is its own, a named column with nothing in it that the user has to guess the meaning of and that reads as data loss. Neither has a failure symptom and both open the dialog, which is exactly why the convention needs a check rather than a convention. It also holds the duplicate-header half of the same mechanism, which is the severity-1 editor defect arriving by another route: `To Table: "row"` writes the literal `?` as the header of every column its source did not name, so a wrapper's column menu reads `row, ?, ?, ?`, and every name-addressed read in this plugin is answered with the FIRST column of that name — selecting the second silently analyses the first one's data under the second one's heading, and no output in the plugin names a column by index, so there is nothing for the user to notice. Two of the three are driven by including the shipped module and calling the procedure, so the bytes under test are the shipped bytes; the third is lifted verbatim between its `procedure` and `endproc` lines because its file's body opens a dialog. The probe runs in a sandbox of symlinks rather than in `plugin/scripts`, because Praat resolves a relative include against the top-level script's folder and a validator that writes scratch files into the tree it is measuring has started changing that tree | the plugin source, driven live |
| `v64_display_and_coercion.R` | **What the Info window is allowed to print.** AUTHOR RULING, 15 August 2026: no raw double may reach the Info window — statistics print at fixed four decimals and *p* in APA style, and full precision belongs to the CSV export, which is the artefact a reader is meant to compute from. The report is for reading. Population: the WIDTH of a printed field on a live drive, plus a static census that `@eml_fixed` is the ONLY caller of `fixed$` left in `stats/eml-output.praat`, because one unrouted call site is how the second one arrives. Nothing in this file asserts a VALUE: the plugin's numbers were right before the change and are byte-for-byte the same after it. The reported symptom was `Skewness  -0.0000000000000001` from a line reading `fixed$ (.value, 4)`, and the defect is in the FORMATTER rather than the statistic — the skewness of a symmetric column is zero, floating-point lands a few ulps away from zero, and every statistics package does the same; what was wrong was printing those ulps. Measured on the binary under test rather than quoted from a manual, Praat's `fixed$` formats to the LARGER of the precision it is given and however many decimals are needed to show one significant digit, and returns a bare `0` for exact zero — a minimum-significance formatter wearing a fixed-precision name. So the leak was never one line: it was one line PER STATISTIC THAT CAN SIT NEAR ZERO, which in a statistics tool is most of them — the *t* of two identical means, a Cohen's *d* of no difference, a confidence bound on a null result, a residual mean, an eta-squared of nothing. The bare zero is the same defect with its sign reversed, and the one number a reader most wants to recognise at a glance is the only one that does not line up in its column. Every numeric validator is blind by construction: reading `-0.0000000000000001` as a number gives −1e−16, comfortably inside every tolerance in the tree, so the printed STRING is wrong and the printed VALUE is right, and a validator that parses before it compares cannot see the difference — which is not a gap in those files but what they are for. The CSV checks assert the opposite property over a different artefact. No red path fires, because nothing fails. A golden-file diff would have shown the string and can only ever say "this changed", never "this was always wrong" — and the leak was IN the committed evidence, undetected, which is the strongest argument available for asserting the shape rather than freezing the bytes. It also measures two coercion findings that are not parity findings and therefore not `v63`'s | the plugin source, driven live |
| `v65_display_standard.R` | **One numeric display standard, in the two files that had no door to it.** AUTHOR RULING 6, 15 August 2026. Population: `scripts/eml-wizard.praat` and `stats/eml-analysis.praat` — the pairwise, repeated-measures and Friedman reporters and the whole of the wizard — driven live, with a static census asserting there is no second implementation of the rounding anywhere. `v64` owns `@eml_fixed` and its case grid and covers `stats/eml-output.praat`; its own sweep on two identical groups drives `@emlRunTwoGroupAnalysis`, which is a different orchestrator from `@emlRunPairwiseAnalysis` and prints no matrix at all, so the reported symptom — a post-hoc row reading `A vs B  0 (6.00)  1.000  1.000  0`, and a wizard preview printing a skewness at seventeen decimals and a Shapiro-Wilk *p* at eleven — was found by a live drive of a wrapper nothing had driven. Neither symptom is a numeric defect: a *t* of two identical means IS zero, a skewness of a symmetric column IS zero to within a few ulps, and every one of those numbers is byte-for-byte the same after the change. What was wrong was their WIDTH. The escape is one line per statistic that can sit near zero, which in a post-hoc table is most of them at once — *t*, Cohen's *d*, *U*, rank-biserial *r*, Scheffé's *F*, a mean difference, a group mean, an epsilon, a partial eta-squared, Kendall's *W* — so fixing the two reported lines would have left the mechanism and the next reader with two similar groups would have reported it from a different cell. The trap it is built around is THE FIX-SHAPED FIX: the cheapest way to satisfy every width assertion is to make the formatter return a zero of the right width for everything, which satisfies "exactly three decimals", "no more than four decimals" and "no bare zero" and is catastrophically wrong. So section 4 drives the SAME reports over WELL SEPARATED groups and a real repeated-measures effect and compares the printed numbers against R, and section 5 asserts that the CSV still carries more precision than the report — a break that clamps goes red in 4, a break that rounds the DATA to satisfy 4 goes red in 5, and neither is worth much alone. That second one cost a revision: the first draft asserted that some exported field carried five or more decimals, and the break PASSED it, because a *p* of 3.4e−05 is one `fixed$` refused to round, so the check is now a round trip against R. Thirty-one deliberate breaks, built as copies of the tree, every one red, and between them they turn all but one of this file's assertions red at least once. The exception is named rather than glossed: its subject is the BINARY, not the plugin, and it goes red on the day a future Praat fixes `fixed$` itself, which is the event it exists to announce. A break also found a defect in this file rather than in the plugin — a truncated report left `check` a zero-length value and R aborted mid-run, on the one break the file exists to catch, and a validator that dies instead of failing has reported nothing | the plugin source, driven live |
| `v66_draw_layer.R` | **Four rulings that all land in the same three files.** Population: the draw layer, the annotation layer and the normality wrapper, driven — record-then-replay on the SAME data and on RETARGETED data, seven categorical procedures on one narrow dB axis, both post-hoc arms, and the printed width of the annotation reporters. A violin recorded with the dialog's "both 0 = auto" axis replayed with the axis FROZEN at the numbers the first run resolved, so re-run on retargeted data — the whole point of a recorded workflow — every violin fell outside the window and the replay drew a fully furnished, completely EMPTY frame: box, ticks, group names, title, both axis names, 43 KB of PNG, no data, nothing warned. `v27` is the closest predecessor and calls a figure blank when it has no CHROMATIC ink, which is exactly the verdict this deserves, and it never sees it, for two structural reasons: the stress cases pass explicit bounds or ordinary data, so none of them is a REPLAY of a recorded call at all, and v27's blank verdict is scored against a same-family baseline case, which makes it a check on the DRAW path rather than on the record path. `v58` and `harness/record` prove that an emitted script RUNS and that a same-data replay reproduces its figure, and both were green over the defect for the reason that IS the defect: on the data it was recorded from, a frozen axis and an auto axis resolve to the same numbers and the two figures are byte-identical. Both arms are driven here on purpose, and the same-data arm is what stops this file being satisfied by a replay that ignores its arguments entirely. Also: ruling 7 at FOUR PIXELS of white at 300 dpi between "Power (dB)" and a tick reading "100.10", reproduced through six of the seven sites before anything was changed — `v62` measured that side, printed it as a NOTE and asserted nothing, because the file belonged to another hand that turn, and this is the other half; ruling 1b, the figure saying which post-hoc produced it now that the parametric arm offers no adjustment menu, because the figure outlives the dialog; and sixty-nine raw `fixed$` calls reaching the Info window from the annotation reporters, which hold the ANOVA, Tukey, Kruskal-Wallis, Dunn, two-way, paired, regression and correlation tables and therefore most of the plugin's printed output — the formatter was closed and the largest set of its callers was not. The figure/Info boundary runs through the MIDDLE of one file and getting it wrong in either direction is a defect: the DRAWN text is laid out against its own measured width, so re-formatting it is a LAYOUT change and a sweep that "fixed" every `fixed$` in the file would move figures nobody asked to move — §1c asserts both sides, the printed ones all routed and the drawn ones all surviving, counted. Three traps are answered explicitly. Every assertion is anchored to a measurement taken on BOTH trees — the 4 px and the 17 px, the frozen 160..340 and the resolved 900..1800, the empty frame's 0 ink and the drawn frame's 6794 — and a check whose fail side was never observed is not in this file. Every static check reads code with COMMENTS STRIPPED, because these files carry long prose naming the very procedures being checked for, so an unstripped grep would find the repair in the paragraph describing it and call the wiring present after the call site was deleted. And no size threshold is used, because a 43 KB empty violin and a 53 KB empty spectrum both sail through "bigger than nothing" | `harness/drawlayer/out/` and the three source files |
| `v67_axis_and_spectrum.R` | **Two author rulings of 16 August 2026, both about a figure that says nothing and raises nothing.** Population: a Spectrum drawn over windows holding one bin, two bins, no bins and one below the floor, on a SHORT token; and an emitted recording whose editable block is edited and re-run. Praat's Spectrum `Draw:` joins bin points with line SEGMENTS, and one point is no segment, so a window containing exactly one bin rendered a titled, labelled, gridded, tick-marked frame with ZERO ink inside it — and the bin that was not drawn held the peak of the tone. Two bins draw normally, which is the control that says the finding is about the COUNT and not about the range. How reachable it is was the half the first measurement understated: bin width is 1/duration after the zero-pad, so a 1.4861 s tone needs a 0.2 Hz zoom and reads as a laboratory curiosity, while a 0.15 s token has 5.383 Hz bins and ANY zoom under 10.7 Hz reaches it — and short tokens are what phonetics is made of. Measured at HEAD on 6.6.30: a 1 kHz tone in a 0.15 s token drawn over 998..1002 Hz put ONE bin in range, bin 187 at 1001.294 Hz holding 80.34 dB, and produced a 48,870-byte PNG with no ink inside its frame. `v66` found it, drove it and printed the bin count, the peak dB and the zero ink as a NOTE, asserting no remedy, because the choice between a stem, a marker and a refusal is the author's and not a validator's — and it drove it on the LONG fixture. The ruling is DRAW WHAT YOU CAN, so here the remedy is asserted and the fixture is the short token; zero bins in range stays empty, because there is genuinely nothing to draw. The second ruling lifts the axis request into the recording's editable block, under a sentence already promising that nothing below it names an object or a column, where the axis was the third thing a retargeting user has to change and the only one still buried in the body. Section 4 does not grep the block: it EDITS it on two lines and runs the file, and asserts BOTH directions of the comparison — the edited replay must match a native draw at the EDITED axis and must NOT match the figure that was recorded — because either alone is satisfied by a replay that ignores its arguments. The size trap is on the record with its numbers the wrong way round: the empty one-bin spectrum weighs 48,870 bytes and the drawn one 48,531, so a threshold that tried to separate them would have to say the bigger file is the broken one. The stem's TOP ROW is compared against a ruler taken from PRAAT ITSELF — the row where Praat's own `Draw:` puts the same bin's vertex in the two-bin figure — because a stem drawn to the full height of the frame has a SMALLER top row than a correct one and a stem drawn at the naive dB is 96 rows lower, and its foot and its column are checked separately, since a mark can be right in one of the three and wrong in the other two. What this file does not test is stated plainly rather than left to be found: the form's half of the axis contract is another file's, and the leg proving the recorder PREFERS the published globals publishes them itself | `harness/axisspec/out/` and two source files |
| `v68_form_axis_and_display.R` | **The graphs form's half of that same axis contract.** Population: the form driven on both passes that used to destroy the evidence before the recorder could see it — the bracket pass, which on an annotated bar, violin or box plot with at least one bracket resolves (0, 0) into the data's extent and then widens the ceiling for the brackets, and the legend pass, which draws, measures the legend, writes the widened extent back into the same two variables and DRAWS AGAIN, the second draw being the one that gets recorded. The form now publishes `emlGraphsAxisYReqMin` and `emlGraphsAxisYReqMax` once, where the range validation has just finished with the user's own numbers, and neither pass writes them again. Measured at HEAD on an annotated violin over four cohorts with the y-range left on auto: the dialog held (0, 0), the bracket path resolved it to 195.0000 .. 268.9205, and the emitted block declared those two numbers as the user's request. Replay that on the same data and it reproduces the figure exactly — which is ruling 10(a) working, and why every replay test in this tree was green over the defect — and point it at a second speaker, the entire purpose the block exists for, and the figure is drawn on the FIRST speaker's axis, with nothing erroring. `v67` and `harness/axisspec` are the other half of the contract and are thorough and green over this, structurally rather than by oversight: that harness cannot run the form, says so in its own header, and publishes the two globals BY HAND to test its own side, so on every path it can drive the argument IS the request and nothing can disagree with anything. `harness/record/roundtrip_graph.sh` is the strongest replay evidence in the tree and calls the draw procedure directly, so the fallback fires and it is byte-perfect on both trees; a round trip proves an emitted script reproduces the figure it came from and says nothing about whether the numbers in it are the user's. No PNG comparison at any tolerance can see it either, because the resolved range is what the figure was legitimately drawn on — both trees draw the same picture, and that is the defect's defining property rather than a coincidence to be tightened away. Nor can a check on the WIDTH or the FORMAT of the block's numbers, which is the shape this defect invites: `0.0` and `195.0000` are both well-formed, and so is the zero a fix-shaped fix would produce by clamping every published number, which would satisfy every format assertion while throwing away the range of every user who typed one — so sections 3 and 4 assert the VALUE in both directions, 0.0 where the user chose auto and 150 and 400 where the user typed them, on the same two paths in the same run. It also carries the last active `fixed$` site under ruling B and says in numbers rather than glossing that the change moves nothing today: the site is an ellipsis NOTE whose two numbers cannot reach the magnitude where `fixed$` diverges, swept and recorded as a measurement, so what is asserted is the formatter at the site, the byte-identity of the notes, and the measured size of the domain. Nineteen shadow trees, one deliberate defect each, every one red in the 16 August re-run (75 of 99 checks) <!-- count-scope: v68_form_axis_and_display.R's own break re-run, not this suite's total; reproduce with `bash harness/formaxis/break.sh` --> — and the ten assertions no break reaches are named: three rig guards, six fixture guards and one positive control. The second positive control went with its subject: @emlCheckPlausibility, three raw `fixed$` calls and no caller in the tree since v2.3, was RETIRED on 16 August 2026, and the pin that had been on its caller count is now on its ABSENCE — from this file and from the whole plugin — so a re-introduction is red rather than silent (`plausibility_readded`) | `harness/formaxis/out/` and two source files |
| `v69_bracket_disclosure.R` | **The layout that puts the *p*-values ON the picture must say what produced them.** AUTHOR RULING, 16 August 2026. Population: eight driven figures — Tukey, Dunn under three corrections, a narrow frame, the two two-group arms and one non-significant omnibus where no caption may appear — read off the rendered files rather than off the source. **AUTHOR RULING C, 16 August 2026, moved the two-group legs across that line and this row with them.** They were the legs proving a caption correctly DECLINES, on the argument that two groups is one comparison with no post-hoc and no family to correct; §8 of the same file recorded, as an attestation and not as a test, that the same figure therefore named no test ANYWHERE on it. Ruling C keeps the half of that argument that held — nothing was corrected, and the caption says so rather than borrowing Tukey's family-wise claim — and drops the half that did not. `mw_two` is driven with `holm` sitting in the form scope precisely so that a repair reaching for `.correction$` on that arm goes red. The general invariant behind it is `v76`'s. Ruling 1b gave the MATRIX layout a sub-line naming the post-hoc test and, on the parametric arm, where its family-wise control comes from; the BRACKET layout was left with neither, which is the wrong way round, since brackets put *p*-values and stars DIRECTLY ON the picture, over the data, pair by pair, and are therefore the layout a reader is most likely to take a claim off. The sharpest measurement of the silence is a byte comparison taken before the repair: the SAME figure driven with Dunn under holm and under bonferroni came back BYTE-IDENTICAL, one MD5 both ways. The user's choice of correction changed the *p*-values behind the stars and left no trace whatever on the artefact that leaves the session. The Info report already disclosed it and nothing was hidden from anyone reading the report — but the figure is the artefact that goes into a slide, a paper or a supervision, and the reader looking at it has no report beside them. The two arms say DIFFERENT things and that is the content rather than the style: Tukey's *p* comes from the studentized range distribution and is already family-wise, so that caption states it and claims no further adjustment, while Dunn's *z*-tests carry no family-wise control of their own, so that caption NAMES the method the user chose and claims nothing about what it achieves — one sentence covering both would be false on one of them whichever way it was written, and the asymmetry is asserted in both directions. `v66` owns ruling 1b, is the closest predecessor and was correctly green over this from the day that ruling closed: its legs force the matrix layout and leave the bracket count at zero, and the subtitle it measures is drawn by the matrix panel, so a bracket figure never enters its universe. `v29` holds the three-channel disclosure rule and does not reach it either, because the caption is written by the annotation BRIDGE and rendered after the draw procedure has returned, outside every population `v29` counts. And a golden-file diff calls two byte-identical correction arms healthy | `harness/bracketcap/out/` |
| `v70_p_precision.R` | **The exact *p* tail is three significant figures, and the significance criterion is not a statistic.** AUTHOR RULING, 16 August 2026. Population: the plugin's SECOND renderer. The wizard's repeated-measures line printed `F(2, 38) = 583.1232, p < .001  (3.0359635874099574e-29)` — seventeen significant digits of raw double on a line whose entire job was to say "about 3e-29" — and so did the Friedman line beside it, the normality reports and the Kruskal-Wallis block; twenty-nine committed artefacts carried one when this was written. THE TAIL IS NOT THE DEFECT AND MUST NOT BE DELETED, and half this file exists to make that impossible to forget: the APA rendering floors at .001, so 5.8e−07, 2.1e−13 and 3.0e−04 all print `p < .001`, nine orders of magnitude flattened into one string, which is findings D28 and D35 and is why the exact tail was added at all. Deleting the parenthesis makes every width assertion here pass and puts D35 straight back, so the three D35 values are asserted DISTINCT and ORDERED, at the renderer and again at the procedure a report actually calls, each within a fraction of a percent of the *p* it stands for. The mechanism was `string$`, Praat's ROUND-TRIP renderer, which emits however many digits it takes to reconstruct the double exactly — driven on this binary rather than quoted — so the width was never a property of that *F*, that path or that wrapper. Praat has no `printf` and the obvious substitute is the defect again: `fixed$` and `@eml_fixed` give DECIMAL PLACES, and the answer to "3e−29 at four decimals" is 0.0000, nine orders of magnitude flattened onto one, which is D35 with the repair's name on it. `v03` and `v04` read this exact number at tolerances of 5e−30 and 5e−26, chosen in August precisely so a plugin flooring it to zero could not pass, and they are blind to the whole defect, because a validator that PARSES before it compares cannot see a width. `v65` FOUND it, measured it, named the repair by file and line and ATTESTED rather than asserted, because the repair was in a file that change did not own — and its `stripExact` helper removes the parenthesis before applying the four-decimal rule, so the tail is the one string in the report the house width standard explicitly does not reach; this file is what that carve-out was waiting for. `v64`'s one-door census counted it as compliant by never looking at it, since `string$` is not `fixed$`, and section 1 adds the matching assertion for the second renderer. Against a fix that returns a constant of the right shape, every pinned string is also parsed back to a number and compared against the *p* it came from, and 216 values spanning 1e−4 down to the smallest subnormal double are compared against glibc's `%.2e` in R — a genuinely independent correctly-rounded implementation rather than a second copy of the thing under test. Ruling B is the other half and is a REFUSAL: `@emlReportAlpha` stays on a bare `fixed$`, because routing the criterion through `@eml_fixed` prints an alpha of .0001 as `0.000` — the threshold the report says it marked significance against, rendered as zero, which is the one value no threshold can have. A statistic is a measurement of the data whose trailing digits are arithmetic noise; a criterion is a number the reader chose, and quoting it back at less precision than it was set with is not tidying but misreporting. Twenty-six deliberate breaks, all red; the three no break reaches are measurements of PRAAT'S own renderers and are the premise rather than assertions about the plugin | the plugin source, driven live |
| `v71_tidy_vocab_and_warning.R` | **Two rulings that both turn on the same question: which artefact is a string FOR?** AUTHOR RULINGS, 16 August 2026. Population: the tidy frame written by a three-column normality press, read back off the written CSV and compared against R's own G1 and G2 per column; and the two destinations of the repeated-measures caution, asserted as which VARIABLE each reads. A user who tested one column for normality and pressed Save got a glance frame carrying *W*, *p*, skewness and kurtosis; the same user testing three columns in one press got a tidy frame carrying *W*, *p* and method — no shape at all, in any file. The more they asked for, the less they were given, and nothing errored, nothing was missing from the header, and the file was a perfectly well-formed broom frame. `v17` and every parity check are the wrong instrument by construction: skewness and kurtosis are not broom's, because `shapiro.test` reports a statistic, a *p* and a method and nothing else, so a frame that omits a column broom also omits is a frame in perfect parity — a conformance check cannot see the absence of something the standard never had. `v57` reads the same three-column export and asserts that each tidy row's statistic is that column's *W*, and passed in both directions, because a check that asserts the columns it names says nothing about the columns nobody named. `v64` came as close as a validator can come without asserting: it drove both presses, read both files, printed the asymmetry and pinned the PREMISE, so a repair by deletion goes red — and deliberately did not assert the tidy half, because the repair lived in two files that were not that change's to edit. The trap here is that A VOCABULARY CHECK IS NOT A VALUE CHECK: writing the columns and putting anything in them satisfies it, so three columns are driven that no single wrong implementation survives — one symmetric column whose skewness is exactly zero, and two mirror-image columns with equal kurtosis and opposite skewness. Copying column 1 into every row fails all three; clamping to zero passes the symmetric column and fails the pair; emitting kurtosis where skewness belongs passes the pair on kurtosis and fails it on skew. The second ruling splits one string that had two destinations — the report owes four decimals and a sentence, the export owes full precision and a column, and where one string served both, one of the two was always being short-changed and which one could not be seen from the source. Because the two halves render identically at every *k* this plugin can be driven at, a validator that measured the printed width would be green on both trees and testing nothing, and no such check is written here: what is asserted is which variable each destination reads, statement-joined out of the source rather than matched against the comment explaining it, and the *k* at which the two formatters part company is measured on the binary, so "identical today" is a measurement with a date on it. The exported bytes are frozen against a named commit, which is the one kind of literal this repository normally refuses and is correct here because the assertion IS byte-identity | the plugin source, driven live |
| `v72_batch_registration.R` | **The door is registered, and somebody walked it.** AUTHOR RULING, 16 August 2026: register batch voice analysis, on the condition the coverage set. Population: the menu entry AS RENDERED — a fixed keyboard walk that must arrive at this module's dialog, driven twice, the second time against a `setup.praat` with the two registration lines mechanically cut, so the two walks must reach DIFFERENT dialogs — plus the form pressed and the CSV read back. `v52`, `v53` and `v54` cover the module thoroughly and none of them can say anything about either half of a registration, that the MENU ENTRY exists and reaches this module and nothing else, and that the FORM behind it comes up and works. `v53` is the strong one and it reaches the loop by CUTTING THE TWO DIALOG STANZAS OUT of the shipped file, hash-verified, because `beginPause:` hard-crashes under `praat --run` — so the best evidence in this tree about this module was gathered with its form deleted, and no amount of it says the form opens. This repository has twice paid for a claim about source standing in for a claim about behaviour: the graphs form's Exp CSV button wrote the wrong format for weeks because nothing pressed it, and nine of ten Save callers died on the FIRST press while `v46` — which reads the call sites and proves the string appears in each — passed every day. A registered menu entry that has never been clicked is that shape with a menu bar in front of it. Counting the entries cannot help, because a count moves the same way whether the new entry is where it should be, at the foot of the submenu, or in the middle of the compare group. A screenshot cannot: GTK menu items have no X window and no queryable property, so the machine-readable fact about a menu item's position is which command a FIXED KEYBOARD WALK arrives at — and that is worth nothing on its own, because a walk that reached the right dialog by luck reports the same string, which is why the negative leg exists and why without it this whole file would be unfalsifiable and green. A size or presence threshold on the output cannot either, since it passes on three rows of `--undefined--`, so the values are required to be the synthesised 78, 180 and 640 Hz of the three fixtures, which is also the only thing here that proves the row-to-file pairing survived the GUI. Section 5 adds the two APPENDIX_D §7 rules the module had never implemented, and the fixtures are built to DIVIDE: 78 Hz must trip the 75 Hz raw-cross-correlation floor and must NOT trip the 50 Hz filtered-autocorrelation floor, 640 Hz must trip the 330 Hz cepstral ceiling and not its 60 Hz floor, and 180 Hz must trip nothing — the silences are asserted as hard as the warnings, because a guard that fires on every segment is a guard that says nothing. Every band is pinned against the whitespace-collapsed CODE line with WHOLE-LINE equality rather than a substring, which `v53` learned the hard way when a break widening the CPPS band from 25 to 250 sailed through a fixed-string match | `harness/batchgui/out/BATCHGUI.tsv`, `INFO.txt`, `RESULTS.csv` |
| `v73_directional_p.R` | **The same data through the same procedure twice, with the two groups exchanged — a RELATION BETWEEN TWO RUNS, which is a unit of evidence this suite did not have.** Population: five parametric families driven forward and reversed — Welch, Student, paired, Pearson, Spearman — plus the wrong-direction perfect effect and Mann-Whitney as a control, read out of a COMMITTED capture and settled against R, which is the evidentiary shape this document states as the suite's rule and the half that survives on a machine with no Praat and that a reviewer can diff between releases. `v77` is the same subject driven live; the two were authored in parallel and numbered on landing. Until 16 August 2026 every parametric kernel computed its one-tailed *p* as `studentQ (abs(.t), .df)` — the smaller tail of the ABSOLUTE statistic — so a test whose stated alternative is directional answered `.0227` for (A, B) and `.0227` again for (B, A), and whichever way the data happened to fall was reported as the direction the analyst had predicted. **Every individual run was correct-looking and only the PAIR was wrong**, which is why a suite built entirely out of per-run assertions was blind to this by construction rather than by oversight. `v08` and `v12` read these very procedures through the shipping report, and every registered menu path in the tree passes `tails = 2` — confirmed by reading the call sites, not by memory — so no report either of them can see ever carried a one-tailed *p* at all. `v18`'s Tier B grid sweeps SHAPES of data through the same procedures and the defect is not a property of any shape: both runs of a reversed pair are already inside its population and it has no assertion that RELATES them, because relating them was not an idea anybody had until the defect was found. The dev suites came through the semantics inversion unchanged, because all six `tails = 1` sites there happen to use data whose observed direction is positive — and a suite that survives a semantics change unchanged has not confirmed the semantics. No golden-file diff could have helped either: `evidence/info/v73_directional_info.txt` is the first committed capture of a one-tailed *p* anywhere in this tree, and a golden file can only say "this changed" when there is something recorded to have changed from. Four deliberate breaks, each watched red and restored, and what each one reddened is the argument for the file's shape. Restoring the defect goes red in every one of the five families, in the perfect-effect section and at the named entry points, and the clearest line it prints quotes both numbers it compared — `welch: reversal MOVES pGreater`, with 0.0227323 standing against 0.0227323 where a gap above 0.9 was required, which is the defect stated as a measurement. INVERTING the direction — the two tails exchanged at source, so the test answers the mirror question — goes red ONLY against the R oracle, because the mirror of a valid answer satisfies sum-to-1, satisfies the reversal exchange and satisfies the doubling identity: **a sign-reversal matrix on its own is not sufficient evidence**, which is the most important result in the file. Computing `pLess` as `1 - pGreater` is invisible to the sum-to-1 invariant BY DEFINITION, agrees with R to about 7e-18, and is caught only by a gap of two ulps that `tol = 0` sees and `tol = 1e-12` does not — the whole case for zero tolerance, made as a measurement rather than as strictness. Mann-Whitney is the falsifier: its exact tails are closed intervals sharing the point mass at the observed *U*, so they sum to 4/3 rather than 1 and its two-sided *p* is a clamp rather than a doubling — an invariant nothing in the tree ever violates is indistinguishable from an assertion of nothing | `evidence/csv/v73_directional_input.csv`, `evidence/info/v73_directional_info.txt` |
| `v74_axis_consume_once.R` | **A published global cannot be unpublished, so it has to be SPENT.** AUTHOR RULING A, CHANGE ORDER 7, 16 August 2026. Population: TWO DRAWS IN ONE PRAAT PROCESS with only the first going through the graphs form — the sequence no rig in this tree had ever performed — driven as a form press followed by a Q-Q plot, as a form press followed by a direct violin call at 150 .. 400, and as an annotated figure whose bridge records a step between the publication and the draw. Ruling 10(b) had the form publish the range the USER asked for, `emlGraphsAxisYReqMin` and `emlGraphsAxisYReqMax`, because two passes inside the form turn AUTO into explicit numbers before the draw the recorder records; `@emlRecordAxisRequest` preferred that pair whenever it EXISTED, and in Praat existence is permanent. So the first press of Draw in a session armed every recorded draw after it — "some form ran earlier this session" was indistinguishable from "this draw came from the form", in any menu command, from any file. Draw a violin on a typed 0 .. 100, then ask for a Q-Q plot in the same session with a recording running, and the Q-Q step's editable block declares `axisYMax = 100.0` on a figure whose only axis argument is its own auto sentinel; edit that block to re-run on a second speaker — the entire purpose the block exists for — and the plot is drawn on the first figure's dialog range, silently. The pair cannot simply be reset, which is the simplification this file exists to stop somebody making: 0 and 0 IS the auto sentinel, so "spent" and "the user asked for auto" would be the same two doubles. The state therefore lives in a STEP STAMP, the one variable that can carry a value meaning nothing, because step numbers start at 1 and 0 can mean CONSUMED and nothing else. **Every axis rig in this tree is green on a tree with the defect in it, each for a reason of its own construction**: `harness/formaxis` draws ONCE per leg and every draw it makes is a form draw, so the publication is always legitimately live and preferring it is always right; `harness/axisspec`'s `rec_form` leg publishes the pair BY HAND and then draws once, so on every path it can drive the argument IS the request and nothing can disagree with anything; and `harness/record/roundtrip_graph.sh`, the strongest replay evidence in the tree, calls `@emlDrawViolinPlot` directly, so no form runs, the globals never exist, the fallback fires and the round trip is byte-perfect while the defect sits untouched one layer up. **No image comparison could ever have seen it either, because no pixel moves**: the figure is drawn on the axis the draw procedure resolves from the data whatever the recorder later writes down, so ink, byte counts, image rows and file sizes are identical across the repair and none of them is read here. Nor can a grep for the stamp's name, which passes against a stamp that is published and never read, published and never reset, or compared with the wrong number — section 1 is deliberately the SMALLEST part of the file and every claim it makes is re-made as a driven value below. Two traps are answered by fixture design rather than by argument: every leaking leg TYPES 0 .. 100, because on auto the value it leaks and the value the Q-Q plot correctly records would BOTH be `0.0` and the rig would be unfalsifiable; and the VALUES are read rather than the declaration COUNT, since a broken tree emits FEWER declarations once the renderer merges the duplicates, so "the block is complete" reads better on the defect. Six shadow trees, one deliberate defect each, all red — and they include the three repairs this file has to tell apart: dropping the preference altogether closes every leak and undoes ruling 10(b) in silence, and dies where a correct stamp must still deliver the sentinel over an argument of 150 .. 400; accepting the pair whenever a stamp EXISTS passes everything except the leg publishing a stamp that names a step the draw will never be; and taking the stamp at publication time rather than at dispatch dies on the annotated figure, whose declaration comes out 0 .. 184.8013 — the widened ceiling — because the bridge's analysis step moved the number under it | `harness/consumeonce/out/CONSUMEONCE.tsv` and the emitted scripts beneath it |
| `v75_legend_single_step.R` | **One press of Draw is one recorded step, and the block's note names the axis the figure was drawn on.** AUTHOR RULING B, CHANGE ORDER 8, 16 August 2026. Population: a grouped violin whose legend needs y-axis room, recorded through the shipped `@emlGraphsDrawWithLegendRoom`, emitted, and RUN. That procedure draws the figure, measures where the legend landed, and draws it again on a widened axis with the first pass discarded — which is not a trick, since the alternative is the form re-deriving every legend-bearing type's auto-range with its own copy of that type's arithmetic. NEW-G8-3 rewound the CSV collector between those passes on 15 August and left the RECORDER running, so one press emitted `# --- Step 1 (draw) ---` and `# --- Step 2 (draw) ---` over the same call and the block read `steps 1 (draw), 2 (draw)`. The duplication is the visible half; the half that costs a reader something is that `@emlRecordColumnManifest` quotes the FIRST step to use an axis pair, so with two steps sharing one pair the resolved-range note named the pass that was thrown away — `195.0000 .. 235.0000` beside a figure drawn at 195 .. 275 — and under ruling 10(b) an auto axis is emitted as the `0.0` sentinel, which makes that note the file's only record of where the figure sat. The repair is the twin of the CSV pair: `@emlRecordMark` / `@emlRecordRewind` in `stats/eml-record.praat`, which name no legend, no pass and no graph, so the next two-pass caller can use them without this file learning what it is for. **Every behavioural check here reads the emitted script or a PNG, never the source**, and that is the difference between this file and a grep: Praat refuses to remove a Table's only row and `nocheck` in front of that refusal is a SKIP, so the obvious implementation — mark, rewind, both called in the right order — leaves the discarded pass in the buffer whenever the figure is the first thing in a recording, which is the ordinary case. It did, on the first run. **The note is then proved in bytes**: the harness copies the emitted file, replaces the two axis declarations with the two numbers THE NOTE ITSELF quotes (parsed out of the file, so the leg cannot tune the replay to anything the file did not say), counts the changed lines and their position relative to the first step separator, runs it, and compares the PNG with the figure the recording drew — byte-identical, `cmp`, not a threshold. A second sub-leg puts an ANOVA in front of the same press: two steps survive, the analysis is one of them, the draw is step 2, and that is also the only path that reaches the row-removal branch rather than the buffer replacement. What the change does NOT fix is stated as an attestation with its measurement beside it — an unedited replay of an auto legend figure is 15017 pixels away from the picture the user got, because the emitted script draws once and the two-pass loop belongs to the form | `harness/record/replay_out/REPLAY.tsv`, `legend_emitted.praat`, `legend_after_emitted.praat`, `LEG_ORIG.png` / `LEG_TUNED.png` |
| `v76_bracket_names_test.R` | **Every bracket-bearing figure names its test, and the subject is the invariant rather than the arm that was broken.** AUTHOR RULING C, CHANGE ORDER 9, 16 August 2026. Population: the four arms of `@emlBridgeGroupComparison` that can put a bracket on a figure — Welch's *t* and Mann-Whitney *U* at *k* = 2, one-way ANOVA with Tukey and Kruskal-Wallis with Dunn at *k* ≥ 3 — enumerated out of the source, plus every driven `bracketcap` leg that has a bracket, read back with tesseract. All four arms composed an omnibus string and handed it to the caller for the Info window; only the *k* ≥ 3 arms set `annotTextN`, so the graphs form's post-dispatch stage had NO LINE TO ROUTE into the corner box and a Welch drive left the session carrying a bracket, `***, d = -6.08` and no test name anywhere on it. **This is the third time this exact shape has been repaired one arm at a time** — ruling 1b gave the matrix layout a post-hoc sub-line and left the bracket layout silent, ruling 11 gave the bracket layout a caption on the *k* ≥ 3 arms and left *k* = 2 silent and recorded it as an attestation — which is why a validator asserting *this* repair would be the fourth entry in that series: green, true, and blind to the fifth arm. So §2 parses the bridge into a block tree and requires every site that writes a bracket label to be DOMINATED by an `annotTextN = 1`: a statement at an enclosing level, not merely one somewhere in the same arm. **The distinction is measured, not argued.** The `text_n_in_matrix` break moves the Welch arm's line inside its own `if .useMatrix`, where an arm-scoped grep still finds it — checked, and it does — and the bracket path still names nothing; the dominance rule is red on it. **Neither half of the file is sufficient and both are shown so.** `new_arm_silent` adds a fifth bracket-producing arm with no test name and drives nothing new: seven red, all in the enumeration, every rendered figure unchanged and every OCR check green. `no_route` leaves the bridge perfect and deletes the form's route into the corner block: every enumeration check passes and no figure says anything. §4 is written to cover a leg this file has never heard of — it takes each leg's test name out of the leg's OWN emitted omnibus rather than from a map kept here, so a leg driving a future arm is covered on its first run | `harness/bracketcap/out/BRACKETCAP.tsv`, `*.fig.ocr`, `*.kv`, `BREAKS_V76.tsv` |
| `v77_one_tailed_direction.R` | **`v73`'s subject in the other evidentiary shape: Praat driven LIVE out of R, and nothing it reads outlives the run.** THE P0 OF 16 AUGUST 2026. Population: the same five parametric kernels run forward and reversed inside ONE process, plus `studentQ` itself measured on the binary under test, the four `@...Alt` entry points that name the alternative in words, and the refusal path. The two files were authored in parallel and numbered on landing; `v73` is the committed-capture half, and this one is what reaches the places a capture cannot. The Praat fact the whole repair rests on is DRIVEN rather than quoted — `studentQ(2.5298, 7.6) = 0.01836`, `studentQ(-2.5298, 7.6) = 0.98164`, `studentQ(0, df) = 0.5`, so the tail is SIGNED and the defect was the `abs()` in front of it — because every line below depends on that signedness and a Praat that changed it would make the file agree with itself and with nothing else. The population neither this file nor `v73` inherited from anything earlier is not a procedure, a menu path or a data shape: `v08`, `v12` and `v18` already read all five families, and each of them asserts ONE run against R. It is a RELATION BETWEEN TWO RUNS. `studentQ (abs(.t), .df)` made every single run correct-looking and only the PAIR wrong, so a suite of per-run assertions was blind to it by construction. **The trap this file is built around is the symmetric fix.** "Reversal changes the answer" is satisfied by a repair that is still not a hypothesis test — selecting the tail from the sign of the difference of MEANS, or returning `min(pGreater, pLess)` — so the one-tailed values are not merely asserted different under reversal: each is pinned against R's own `t.test(alternative = "greater")` and `cor.test(alternative = "greater")`, an independent implementation rather than a second copy of the thing under test. A `min()` repair passes "they differ" and dies on the wrong-direction row, where R says .977 and `min()` says .023. **The regression that matters runs the other way.** Every registered menu path passes `tails = 2`, so nothing shipped ever printed the defective number and the entire exposure was the public scripting API — which cuts both ways, since a repair that quietly moved the TWO-sided *p* would break every shipped report while fixing an API nobody could see. All five two-sided values are therefore recomputed in R from first principles at the double-epsilon scale rather than at a display tolerance. That scope claim is a statement about OTHER files, so it is read out of those files rather than taken on trust from the header that makes it, and a future `tails = 1` appearing on a menu path goes red here rather than being discovered by a reader of the output. `pLess` is a second evaluation of the tail on the other side and not `1 - pGreater`, and the argument is made on a real number this file produces: a right tail of ~5.6e-46 comes back from the subtraction as exactly 0, every significant digit gone, and the reader is handed a *p* of zero for a test that has a perfectly good one. An unrecognised alternative must REFUSE rather than fall back to two-sided, which would be this file's own defect in a new coat, and the error path must leave the new outputs unset rather than stale. What is NOT asserted is stated plainly instead of left to be found: Spearman is compared against `cor.test` on the RANKS, because the plugin computes the *t*-approximation its own header claims, where R's exact AS 89 distribution answers 0.0011 against that approximation's 0.00043 | the plugin source, driven live |
| `v78_repo_hygiene.R` | **The repository's statements about itself, which is the one subject nothing else here has.** Every other script in this table checks a number the plugin PRINTED. This one checks four claims the repository makes in its own voice, and it exists because all four were red on 16 August 2026 and three had been red long enough to have stopped being read. **MANIFEST.txt had been stale for twelve days** — `build-manifest.py --check` was correct the whole time and nothing ran it, which is the entire failure: a generated file that is checked in goes stale whenever any listed file changes size, so it is green only while something forces the regeneration. Being in this suite is that force. The generator was also wrong about what it should list: it gave each of 204 PNG sprites a row with a curated description nobody would write and a "lines" figure computed by splitting binary data on newlines, so `sprites/` is now ONE row carrying the file count and a sha256 over every byte of the set — which still goes red if a single sprite is added, deleted or re-rendered, and the recipe to reproduce the digest with `find` and `sha256sum` is in the generator. **`harness/check_includes.py` reported 22 entry scripts with unresolved calls and 21 of them were its own false positives**, which is the shape that teaches a reader to skip the report — and the one true entry was in that list the whole time. Two causes, both measured against Praat 6.6.30 rather than assumed: Praat has THREE comment characters and the checker knew one, so a design note in `eml-draw-procedures.praat` naming `@thisProcedureDoesNotExist` (the 10 Aug measurement that a guarded call to a nonexistent procedure is free) was read as a call in 19 closures; and the four `eml-lib*.praat` barrels — comments and `include` lines, no procedure, no statement — were checked as if they were scripts, when `eml-lib-graphs.praat` says in its own header that it is not usable alone. The checker now also understands the existence guard the draw layer is built on, `if variableExists ("emlRecordLoaded")`, on the TRUE arm only. The survivor was real and is fixed in the source, not suppressed: `@emlAssertFullViewport` lives in the graphs layer and was called unguarded from `@emlSavePanel` in the STATS layer, unreached only because all thirteen stats wrappers pass `.offerFigure = 0` — an argument, checked nowhere, in thirteen places, and one wrapper passing 1 would have raised "Procedure not found" at the Save press, which is the exact defect this checker was written for and the exact defect it could not see. Both call sites now sit behind `if variableExists ("emlDrawnMinX")`, which is assigned at file scope by the layer that defines the procedure; measured 0 under `eml-lib-stats.praat` and 1 under `eml-lib.praat`, so reachability is unchanged and a static reader can now confirm it. **`plugin/README.md` linked `docs/procedure-reference.md` and `docs/recipes.md` at three sites and neither file has ever existed.** Both were removed rather than written: `stats/` and `graphs/` define 491 procedures, a hand-written signature list that long is wrong in places within a week, and a thin one is worse than none. The README now says so and points at what is kept true instead — the header block above each `procedure`, `MANIFEST.txt`, `plugin/docs/API_EXPORT.md`, `setup.praat`, and Record script, which emits a runnable commented script of the user's own analysis and is a better recipe collection than eight guessed cases. The 491 is recomputed here, because an argument resting on a number should not rest on a stale one. **There was no `.github/workflows` at all**, which is why the other three could sit red, and writing it disproved a front-door claim: README.md and `validate/README.md` both said this suite needs "R and nothing else — no Praat". Seven validators drive a real Praat and fail without one; with none on PATH `v59` drops from every check passing to a handful with one red and the suite exits 1, and the BARREN edition is not enough either — it presents no dialogs, so most of `v59` fails against it and none of it fails against the full Linux build. The two figures are in `validate/README.md`, scoped to `v59` and reproducible from one command. Both pages now say what is true. The workflow installs `r-base-core` (4.3.3 on ubuntu-24.04, the version `validate/README.md` names) and Ubuntu's `praat` 6.4.06 purely for its shared-library closure, then puts the real 6.6.30 build in `/usr/local/bin` ahead of it; it takes no secret and no token, so it runs on a fork's pull request. THE FOUR GATE EACH OTHER: CI runs the suite, the suite runs this file, and this file checks that CI exists and runs the suite. Every check here is written against a vacuity too — the link scanner must resolve at least twenty references, and the include checker must report both that it examined entry scripts and that it allowed a guarded call, because a broken regex finding nothing is indistinguishable from a clean tree | the source tree; no harness artefact, no Praat |
| `v79_release_artefact.R` | **The folder Praat installs — which is not in this repository, and must not be.** Every other script in this table reads a COMMITTED artefact. `plugin_EML_StatsGraphs` is built by `plugin/dev/tools/build-release.py` out of `plugin/`, and the reason it cannot be checked in is the defect it exists to catch: git records the executable bit and nothing else, so a committed copy of the artefact is a copy with the mode evidence stripped off. Population, and it is split on a line that is not arbitrary. **BUILT WHILE THE SUITE RUNS** (0.5 s, 322 files, one zip): the folder name re-derived from the recorder's ten literals rather than read out of the build's own record, both digests reproduced by a second build, every `include` in the artefact resolved against the including script's own folder, and — the strongest reading here — **the zip unpacked under `umask 077`**, with every mode measured on what `unzip` created rather than on what the builder chmodded. That last one is the only reading in the tree taken on a tree nobody chmodded, and the only thing that can see a zip carrying no entry for its own top-level folder: unzip then creates the plugin folder under the user's umask, which on a managed or shared account is `0700` and makes all 322 files unreadable to every other account at once, with each of them recorded perfectly. That half CANNOT GO STALE, because there is no record — it is made from the tree as it stands. **READ FROM A COMMITTED RECORD**, and only the three facts that need an X server: the keyboard walk into the EML Stats & Graphs cascade, the same walk with the registration removed, and the cascade's label read off the photograph with tesseract, because Up-then-Right is POSITIONAL and enters whatever cascade is last in the New menu whatever it is called. **THE LABEL IS READ OFF THE HIGHLIGHTED ROW, NOT OFF THE SCREEN.** tesseract's page segmentation DROPS the selected row — white text on the GTK selection blue is a block its layout analyser treats as an image — and the selected row is the only one whose identity the walk is claiming, so `validate/tools/menu_label_ocr.py` reads the page whole for a human and then crops every band painted in the selection colour and reads each on its own. The harness and this file share that one implementation, so the drive and the check cannot read the photograph two different ways, and the FALSIFIER'S photograph is read the same way and must NOT carry the label: with the registration cut the highlighted row is one of Praat's own cascades, and a reading that said yes to both screens would not be reading the row. Four things keep that half from rotting the way `gridmode` and `graphaxes` did, and none is 'remember to re-drive': the record is BOUND BY DIGEST to the two files its claim rests on — `setup.praat`, which decides whether a menu appears, and the script the walked command points at, which decides the dialog it reaches — and deliberately not to the whole artefact, because an alarm that fires on every comment fix is one that gets silenced; the record must carry the KEY SET OF A FINISHED RUN, since the evidence committed the day before was an aborted one, 18 lines of 25, stopping before the falsifier leg with every line in it true; every scalar in it is READ by some check here, censused through the accessor so a key that stops being read stops being claimed in the same edit; and the verifier is PROVED ALIVE on five deliberately damaged copies — one file at 0600, the folder renamed, a foreign `plugin_` name left in a shipped file, a shipped document linking `../` out of the folder, and a shipped document linking an artefact path that is not there — because `--verify` on a freshly built tree asserts the chmods the same function made moments earlier and is close to a tautology. The last two are the newest thing `--verify` asserts: the install instruction is "copy this folder", so the folder is the whole of what a reader ends up holding, and a document inside it offering `../docs/API_EXPORT.md` offers a file that was never in the download — silent, with no missing menu, no changed mode and nothing for the install walk to notice. The scan uses `v78`'s own reference grammar so the artefact scanner and the repository scanner cannot disagree about what a link is, and it prints its population (67 references in 10 documents) so a grammar that had stopped matching reports zero bad links loudly rather than quietly. What it cannot see is written in its header: one platform of the three the install instructions name, one command of the nineteen registered, and a file added to `plugin/` since the GUI drive | the source tree, built live; `harness/release/out/RELEASE_INSTALL.tsv` and the photographs beside it |
| `v80_shipped_history.R` | **The one script here that reads the shipped SOURCE and rejects a sentence in it.** Every other row in this table is about a number the plugin printed or a claim the repository makes about itself. AUTHOR RULING, pre-release: a shipped file describes what the code does, never what it used to do wrong — defect and change history lives in git and in `plugin/dev/HISTORY_LEDGER.md`. The reason is not tidiness. A header entry reading "the guard was on `.nGroups`, which was always true" makes a claim about a version nobody has, standing beside claims about the code in front of the reader, in a file where nothing can tell the two apart; and it is the one part of the file this suite cannot exercise, so it is the part that can be wrong indefinitely and stay green. **IT IS A LINT BECAUSE THE SWEEP CANNOT BE READ.** The sweep it closes removed 1,623 lines of header narration from 27 shipped files and rephrased 78 more into the present tense; the failure mode of proving that by reading is a missed line, which looks exactly like a line that was considered and kept. So the sweep was DONE when this went green, and stays done because this now stands guard — the next fix that writes its own history into a shipped header turns the suite red in the commit that wrote it, naming file and line. **CAPTURE FIRST, DELETE SECOND, PROVE THIRD.** `plugin/dev/tools/extract-history.py` copied every block into the ledger verbatim BEFORE any of it was deleted, and it was run TWICE — once against `b4ca5e6`, the tree as it stood before two earlier sweeps removed 241 of these lines with no ledger in existence, and once against the tree this sweep edited — so a reader can tell material removed before the ledger existed from material the ledgered sweep removed. The two share one pattern list, and v80 re-derives the Python list out of the tool's source and asserts it is identical to its own, because "everything this lint would reject was first written down" has to be a property of the two files rather than of anyone's memory. **WHAT SURVIVED, REPHRASED.** Every explanation whose reason is a property of PRAAT or of STATISTICS stays and was moved into the present tense: `fixed$` is not a fixed-precision formatter, Praat cannot nest a procedure call in an expression, `and` does not short-circuit, `nocheck` is a skip and not an error suppressor, Praat cannot unset a variable so 0/0 as the auto sentinel means the pair cannot carry consumed state, Praat will not remove a Table's only row. A reader who deletes one of those reintroduces the defect, so a line that collides with a pattern and is load-bearing is REPHRASED until it stops colliding rather than allowlisted. **EXCEPTIONS ARE A FILE.** `v80_history_allowlist.tsv` carries path, line-pattern and a one-line justification; the two entries in it are the README's file map and the generated manifest row, both of which collide only by naming `dev/FIX_NOTES.md`. An entry that matches nothing is a FAILURE, not a no-op — a stale exception tells its reader the tree still holds a line it does not, and waits to excuse a future line on a justification written about another one — and no entry may cover more than five lines, because an over-broad pattern would switch the lint off for a whole file with every other check here still green | the shipped tree: `plugin/` minus `plugin/dev/`; no harness artefact, no Praat |
| `v81_recipes.R` | **A page of documentation, executed.** `plugin/docs/RECIPES.md` documents the DIRECT-KERNEL API — a Table becomes two vectors, the vectors go into `@emlTTest` and its relatives, and numbers come back in variables — and that surface has the least protection in the plugin, because nothing inside the plugin calls it: every menu command goes through an orchestrator. Population: the five recipe scripts the page prints, each run in its own headless Praat, plus every name the page uses. **THE BYTES THAT SHIP ARE THE BYTES THAT RUN.** `harness/recipes/extract.py` lifts each script out of the `.md` by a three-line fence grammar and writes it verbatim; `run.sh` substitutes exactly two path prefixes — the install folder and the data folder, the only two things in a pasted script that belong to the reader's machine — and then substitutes them BACK and demands the original file returns byte for byte, so a substitution that touched a third thing fails the leg. Nothing in this tree holds a second copy of a recipe, because a second copy is one that drifts and the reader is the one left running the wrong version; this file re-implements the fence grammar in R and compares `out/scripts/R<n>.praat` with the page directly, so a harness that quietly ran its own copy is caught from the other side. **A CAPTURE CANNOT PIN ITSELF**, which is why there are three anchors and none of them is the capture. THE PAGE: each "What it printed" block must appear as a CONTIGUOUS run of lines inside that recipe's output — move a number and the page is wrong, and this file names the recipe and the line it could not find. BASE R: `t`, Welch `df`, two-sided and one-tailed `p`, Cohen's `d`, Hedges' `g`, the paired `t`, Pearson `r`, and the one-way ANOVA's SS / `F` / eta-squared are recomputed from the committed fixtures and compared against the numbers PARSED OUT of the capture, because the page and the plugin agreeing with each other is not either of them being right. THE PROCEDURE HEADERS: every `@call` on the page must be a procedure that exists, every `procedure.field` a recipe READS must be a field that procedure actually ASSIGNS, and every argument list the annotation blocks print must be the real signature, character for character. That last anchor is the one the page is most exposed to and the least visible by reading — the extractors return `.group1#` and `.data1#`, not the names anyone would guess, and a plausible wrong name reads as correct prose while returning zero or a stale value from the previous call. It found four while the page was being written: `@emlMannWhitneyU` has no `.exact` (the choice is in `.method$`), `@emlRange` returns `.range` and not `.result`, `@emlCI` returns `.marginOfError` and not `.margin`, and `"semitones"` is not a unit `@emlExtractPitchValues` accepts — Praat wants `"semitones re 100 Hz"` and stops the script on anything else. **R5's FIXTURE IS SYNTHESISED, NOT RECORDED**, by `harness/recipes/make_sound.praat`: four harmonics of a 196 Hz carrier, frequency-modulated at 5 Hz with a modulation index of 2.0, and a silent window in the middle. So its pinned numbers are PREDICTIONS rather than measurements — the median is the carrier, the SD of a sinusoid is A/sqrt(2), its quartiles sit at -/+ A*sin(pi/4), and the silence is what makes `.n` and `.nTotal` differ at all. The tolerances on the spread are looser than on the centre for a stated reason and not for slack: autocorrelation reads F0 over a window several periods long, so a frame straddling a turning point of the modulation averages across it and the measured quartiles pull toward the median. Every scan asserts against its own vacuity: the page must yield printed blocks, the source must yield procedures and fields to resolve against, and the recipes must read enough fields to be worth resolving | `harness/recipes/out/`, `plugin/docs/RECIPES.md`, the plugin source, `harness/recipes/fixtures/` |
| `v82_generated_barrel.R` | **The one include line a user writes, and the file `setup.praat` has to generate for that line to exist.** The problem it closes is a property of PRAAT rather than of this plugin, and this file re-measures it on the live binary before asserting anything else: `include` is a parse-time text paste, and a relative path inside an included file resolves against the TOP-LEVEL script's folder, not against the folder of the file the line is written in — two files in a scratch tree, one reachable only under one of the two rules, settle it every run. So `scripts/eml-lib.praat`, whose lines read `../stats/…`, resolves them against the USER's folder and finds nothing there; the shipped barrel is right for the sixteen menu commands, which live in `scripts/`, and cannot work for anybody else. A static file cannot compute where it was installed to. `setup.praat` can — Praat executes it from the plugin's own folder at every launch — and it writes `scripts/eml-lib-user.praat` with full, machine-correct paths, from `@emlPluginRoot`, THE SAME PROCEDURE THE RECORDER CALLS to build the include block of a recorded script, so a generated barrel and a recorded script cannot name different folders. **FOUR FAILURES, ALL DRIVEN, in sandboxes this file builds from `plugin/` into `tempdir()` and launches Praat in — never a symlink into the working tree, which would make the validator write a generated file into the thing it is measuring.** (1) A STALE BARREL: one is seeded naming a folder the plugin is not in, the next launch must repair it, and the repair is compared byte for byte against a regeneration from nothing rather than against text retyped here. (2) A DRIFTING INCLUDE LIST: the barrel is compared against the block `@emlRecordRender` writes, taken off a script emitted by a real recording on the same installation — two independent writers of the same module list, compared to each other and both to the canonical order. (3) AN UNCHANGED LAUNCH THAT WRITES: the modification time is read to NANOSECONDS across two launches, because Praat 7 challenges a script that touches disk and a plugin that rewrote an identical file every launch would prompt a user who changed nothing — this is a REQUIREMENT and the one a later reader is most likely to mistake for an optimisation and delete, so the read is also proved able to tell two writes apart. (4) A SECOND COPY OF THE ROOT ARITHMETIC: read off the source, because no drive can see a duplicate that happens to agree today. **THE SELF-HEALING IS DRIVEN RATHER THAN ARGUED.** Two sandboxes move an installed plugin — carrying its now-wrong barrel with it — and launch Praat at the new location; the second move is the one PRAAT itself makes between 6.x and 7.x, `~/.praat-dir` to `~/.config/praat`. Both then RUN a user script whose only library line is the generated barrel, because plausible-looking paths are not evidence that paths resolve. Break tests, each watched red and restored: the write guard weakened so a stale barrel survives a launch (5 red), two modules transposed in the generator's table (3 red), `@emlPluginRoot` returning a wrong folder name (10 red), the comparison removed so every launch writes (2 red), and the root resolved a second time in `setup.praat` instead of through the shared procedure (9 red) | `plugin/setup.praat`, `plugin/stats/eml-record.praat`, and Praat itself, launched sixteen times into sandboxes built here; no committed artefact |
| `v83_pin_definition.R` | **The only script here whose subject is this suite's own claim to be pinning anything.** Every other row in this table asks whether the plugin is right. This one asks whether the file that says so would notice if it stopped being right. **A COMMITTED ARTEFACT IS A WITNESS STATEMENT, NOT A LIVE WITNESS.** `harness/legend/out/RESULTS.tsv` records what the plugin did on the afternoon somebody drove it, and it goes on recording that, unchanged and in perfect confidence, after the code it describes has been rewritten, reverted or deleted. A check that reads only the artefact therefore pins THE ARTEFACT: it proves the file still says what it said, and never that the code still does what the file says. The two are indistinguishable in a green run and they are not the same claim. **That was measured three times from three directions before it was written down.** `validate/tools/redrive_census.sh` re-drove 34 harnesses from source into a scratch tree: nine reproduced their committed artefacts, fifteen DIFFERED, and several of the stale ones were the evidence this suite was quoting to state things about the plugin that had stopped being true. `v29` asserts 144 renders that no commit in this repository's history has ever been able to produce — carried in from elsewhere, green throughout. And the findings-ledger backfill reverted `plugin/` WHOLESALE, the strongest mutation available, ran 36 validators against it, and **eight rows' pinning validators stayed GREEN** — every one of the eight pinned by a validator whose only input is a committed harness artefact. The first and the third are one decoupling seen from opposite ends: the artefact drifting away from the code, and the check failing to see the code move. So the remedy is a DEFINITION and not eight repairs: **a `pinnedBy` may only name a validator that REGENERATES ITS EVIDENCE FROM SOURCE OR DRIVES PRAAT LIVE WITHIN THE RUN.** **Nothing here weakens an artefact-only check.** `v03` settling a printed *p* against R at half a display ULP, `v73`'s reversal matrix, `v18`'s sixteen designed shapes — these are ORACLE AGREEMENTS, they settle the arithmetic against an independent computation, which no amount of re-driving can do, and they are why a reviewer with no Praat can still check the statistics. They are simply not PINS, because a pin's whole claim is *something would notice*, and the backfill measured what these notice. **IT CARRIES NO LIST**, because a hand-kept table of which validator is which class is one more thing that can quietly disagree with the tree, which is the disease rather than the cure: it sources `validate/tools/evidence_census.R` and classifies every validator from its own parse tree on every run — comments gone by construction, roots propagated through the `EML_*_DIR` overrides to a fixed point so a variable pointing at a harness directory reads as ARTEFACT, and a root counted only where it reaches a reading call. It then asserts that the committed census record REPRODUCES from that live classification, so the census's own artefact is held to the standard this file exists to state rather than being its one exception. **VACUITY IS ASSERTED AGAINST IN BOTH DIRECTIONS**, because there are exactly two ways this could be green and mean nothing. A classifier matching nothing finds no artefact-only pins: so `v77` and `v70` must come back LIVE, `v80` SOURCE, `v01` and `v37` ARTEFACT-ONLY, each named explicitly, and the outcome must be a partition rather than a constant — a broken classifier cannot satisfy assertions that point in opposite directions. An empty ledger satisfies the rule vacuously: so an unreadable ledger, a zero-row ledger, and a ledger in which no row claims a pin at all are each a FAILURE. A `pinnedBy` naming a validator that does not exist is UNRESOLVED and red, not skipped. Four breaks, each watched red and restored against a fixture ledger in scratch: an artefact-only pin (`NEW-G10-2 pinnedBy v37 — ARTEFACT-ONLY, does not qualify as a pin`), the classifier blinded so it matches nothing (red on `v80`, `v01`, `v37` and the partition guard, not silently green), a zero-row ledger, and a `pinnedBy` naming `v99`. **It is READ-ONLY on the ledger** — it names the row, the validator and the class, and leaves the editing to a human, because a check that edited the ledger to make itself green would invert its own point | `audit/FINDINGS_MACHINE.json`, and every validator in `validate/`, classified live by `validate/tools/evidence_census.R`; no harness artefact, no Praat |
| `v94_page_composition.R` | **A page is composed of panels, and the page is computed rather than declared.** The draw dialog gained two per-draw controls -- `Erase page first`, ticked, and a panel origin in typed inches -- and unticking the erase adds the figure to whatever is on the page instead of replacing it. There is no page width, no page height and no grid anywhere in the plugin: the page rectangle is the extent union `@emlExpandDrawnExtent` already maintains, which is already what `@emlAssertFullViewport` hands to Save. **THE LOAD-BEARING CHECK IS THE ONE ABOUT WHAT DID NOT CHANGE**, and it is a comparison between two files rather than against a hash typed into a validator: `harness/compose` renders the same figure twice in one run, once through the controls at their defaults and once with `emlEraseFirst` / `emlPanelOriginX` / `emlPanelOriginY` never assigned at all, and the two PNGs are required to be identical BYTE FOR BYTE. One differing byte is a failure, deliberately -- two renders of one figure from one seeded fixture on one Praat have no jitter for a tolerance to absorb. (Both also matched the render taken at `d95cf12`, before any of this existed; that comparison is in the commit message rather than here, because a hash in a file only says nothing has changed since somebody typed it.) Six cases drive the graphs form's own dispatch loop with no dialog: one panel, one panel with the controls untouched, two side by side, two stacked, two at ONE origin with the erase off -- the overlay the author ruled permitted however it looks -- and a four-panel page whose separate legend is parked. Every page dimension is asserted as the arithmetic consequence of typed origins and per-panel sizes (12.5 x 4 side by side, 6 x 8.5 stacked) and then again in pixels off the saved file, because a union nobody saves is a number. **The parked separate legend** used to take its band twelve inches below the bottom of the panel whose legend it is, floored at 24; on the four-panel case that put the key at 24.0, inside the third left-hand panel, which runs 19 to 28. The band now derives from the union, and the check that it is NOT at the 24-inch floor is the one that goes red if the union term is removed -- driven and confirmed, not reasoned. Static, in `v27`'s and `v32`'s style: the erase and the extent reset are one decision in one procedure and both sit in the same `if` arm, so neither can happen without the other; every viewport the form selects starts at the panel origin; the dialog carries the fields and neither is written to the persisted config, because a remembered `erase off` is the session mode this design refused; the headroom negotiation's second pass is gated on the erase; the save panel's title is a variable and there is no finish-page action anywhere in the code; the recorder states the page on draw steps and only on draw steps and the manifest lifts the three by NAME rather than by argument position. **A CENSUS OF EVERY `Erase all` IN `plugin/graphs/`**, attributed to its enclosing procedure, is pinned as an inventory: `@emlBeginPanel` and `@emlDrawLMMForest`. The second is **D136** -- severity 3, measured, latent and API-reachable, fix DEFERRED to the mixed-model phase by author ruling -- and it is named here so the inventory states the truth rather than the intention. The day it is repaired this check says so. `bash harness/compose/run.sh` first; a FILTERED run of that driver publishes a partial artefact, and the case census here goes red on it rather than quietly checking less | `harness/compose/out/` |
| `v95_second_axis.R` | **Two series in one draw, and the pens that tell them apart.** A boolean on the line chart's column-mapping page opens ONE follow-up page carrying everything a right-hand y-axis needs -- column, range on the 0/0 auto sentinel, name, and that series' own line style -- because Praat forms cannot reveal fields dynamically and five inert fields on the main page is the defect class where a control looks live and is not. The second series ADOPTS the first one's plot box, maps its own range onto it, and draws its ticks and its name in the right margin with Praat's own `One mark right:` and `Text right:`. **THE AUTO RANGE IS RECOMPUTED HERE FROM THE FIXTURE'S OWN ARITHMETIC**, not restated from the artefact: the right series is placed at the same FRACTIONS of the box the left series occupies, so the headroom the figure negotiates once -- for nice numbers, for a legend, for a bracket -- was negotiated for both, and the check is the formula run independently in R and then the property stated a second way. A typed pair is obeyed exactly and is not re-derived. **The compatibility claim is byte-identity between two files the harness made in one run**: the same figure with the two globals never assigned, and with both at their documented defaults. Colour is the palette's and style is the series' -- slot one and slot two by their exact hex read off the rendered pixels, the grouped primary pushing the right series to slot nGroups + 1, the black-and-white pair taking the ends of the grey ramp, and a static statement that the second series' block declares no colour literal of its own. The four line styles are measured as INK: a dotted primary lays down hundreds of pixels fewer than a solid one along the same path, and most of a series' ink is its markers, which are never styled. Four refusals are quoted whole and required to be three different sentences -- the categorical x-axis has no continuous horizontal axis under it, the histogram is a distribution with one count axis, and the rest are told the current scope so the answer is a version rather than a shrug -- and every refusing figure must still DRAW. The recorded script's editable block declares all seven settings under the numbering convention, and **the emitted file replays the two-scale figure BYTE FOR BYTE** with the library repointed and the data rebuilt and nothing else edited. The dialogs are driven under Xvfb with no screen coordinates: the tickbox is ticked with the keyboard, and the transcript shows the follow-up page, then the refusal, then THE SAME PAGE AGAIN, each photographed before it was dismissed. Static: one judge called exactly once by each of the fifteen figure-drawing procedures, `Line style:` (which 6.6.30 does not have) called nowhere, each of the four style commands issued from one place, every procedure that sets a pen resetting it at least as often, the form publishing and clearing the request in one place each, and the draw layer never writing the request globals -- a resolved range parked in `emlSecondAxisMin/Max` would be the range the next figure drew on. **Section 12 settles the one thing the ruling left open**, on rendered pixels: a probe drives all three right-margin commands under a red pen with a `Draw line` for control, and the margin comes out black. The right axis furniture cannot wear its series' colour without hand-drawn margins. `bash harness/secondaxis/run.sh` and `bash harness/secondaxis/gui_pause.sh` first | `harness/secondaxis/out/` |
| `v96_line_style.R` | **Four pens, and the proof that they are four.** Measured at HEAD before this change order: not one call to a line-style command anywhere in the graphs layer and no user option for one, so every line this plugin had ever drawn was solid while Praat offered four styles natively. There is now a `Line style` menu -- Solid, Dotted, Dashed, Dashed-dotted, default Solid -- on every type that strokes a series: the pitch contour, the waveform, the spectrum, the LTAS, the line chart with its confidence band or without it, and the spaghetti plot, where the pen is the STRAND's and the mean overlay stays solid. The choice is per type, remembered across presses, recorded per drawing and surfaced in the emitted script's editable block; the second series on a right-hand axis carries its own menu defaulting to Dashed, two controls rather than one with a hardcoded partner. **THE PROOF IS A RUN STRUCTURE AND NOT A PIXEL COUNT**, and that is this file's whole argument: a count says a figure changed, not that it changed in the way the option names, and no count separates Dashed from Dashed-dotted -- on this fixture their totals sit 229 pixels apart out of ten thousand, inside the noise of one extra marker. `harness/linestyle/stroke.py` walks the columns strictly inside the plot frame and reports how many carry ink, how many maximal runs they form, and how many holes lie between the first and the last, so four presses of Draw differing in one option must yield four DISTINCT signatures: Solid one unbroken run with no holes, the inked fraction falling Solid > Dashed > Dashed-dotted > Dotted, the mark count falling Dotted > Dashed-dotted > Dashed, and the smallest margin in each order printed in the check's own text. **The reset is pinned on a SECOND figure drawn in the same process**, because Praat keeps the line style in the Picture window rather than in the figure: a dotted line chart and then a bar chart, and the bar chart must be byte-identical to the bar chart drawn alone. A bar chart sets no pen at all, so nothing in its own code would be wrong if the pen had leaked -- and what a leak reaches is measured rather than guessed: on the tree `break.sh` builds with every reset commented out, the bar chart's interior ink falls from 1668 pixels to 625 while its FRAME STAYS SOLID. **The first draft of this validator claimed the opposite** -- that a leaked pen would dash the inner box and the ticks drawn after the series -- and the break test disproved it. `furniture_pen.praat` is the measurement that replaced the claim: under one dotted pen, an ordinary `Draw line` comes back in 331 pieces of one pixel while `Draw inner box` and `Marks left every:` come back solid the full width and height of the box. Praat's plot furniture ignores the line style exactly as its margin commands ignore the colour (v95 section 12), so the frame is not a leak detector -- it is the stable rectangle every run-structure measurement in section 3 is taken inside, which is why it is checked on every case. The spaghetti plot forces a second threshold and the trap is recorded as a measurement: its strands are lightened past the 50% grey everything else in this tree thresholds at, so the dark count of a dotted strand and a solid one agree to within a handful of pixels while thousands have changed. Static: six types each reading their own variable, the publish exhaustive with Solid before its branches, eight menus offering the four words in one order, the draw layer reading one published name through a guard and never writing it back, and the recorder naming it in the block table. **Five deliberate defects were driven** (`harness/linestyle/break.sh`, one row each in `BREAKS.tsv`): a pen that is accepted and ignored puts 40 checks red; the two resets removed put 4 and 1 red, the second visible ONLY to the pair of cases that draw without pressing Draw at all; a deleted menu puts 1 red; and Dashed and Dashed-dotted wired to each other's Praat command puts exactly TWO red -- both of them the ordering statements, with every distinctness check still green. That last one is this file's argument in a single number. `bash harness/linestyle/run.sh` first | `harness/linestyle/out/` |
| `v97_line_tree.R` | **The line chart stopped asking about the user's file and started asking what the columns mean.** The page used to open with `Data format: Wide / Long`, which is a question about storage the plugin can answer for itself -- it can see which columns are numeric and which name things. What it cannot see, and what no file records, is whether several numeric columns are the SAME measurement on different subjects or DIFFERENT measurements on one subject: four singers' F0 and one singer's F0 beside a contact quotient are the same table shape and completely different figures. The storage question is gone; a page of its own asks the meaning, and everything else follows from the answer. The column page is BUILT FROM THE TABLE -- one tickbox per numeric column, all ticked, no ceiling, where five hardcoded `Series N` menus used to be; the interval is offered only where `@emlLineTreeRepeats` finds repeated observations and the offer NAMES how many it found, so a control is never present and inert; the right-hand axis page is reached only from "different measurements, exactly two", which makes D138 -- one column drawn on both sides -- unsayable rather than guarded against; three or more unlike measurements are refused toward stacked panels. **IT IS DRIVEN AS DIALOGS BECAUSE EVERY CLAIM IS ABOUT WHAT A USER CAN REACH.** `harness/linetree` runs one Praat per leg under Xvfb with a compositor, photographs each page BEFORE dismissing it, OCRs the photograph, and reads the form's own variables back out of the process that drew; the transcript is bound by sha256 to the three graph files that produced it, comments stripped. A source grep could not have caught what this found: Praat truncates a field's derived variable name at the first character that cannot be in an identifier, so `optionmenu: "Right-hand axis"` is readable only as `right`, and every two-measurement draw aborted with `Unknown variable: right_hand_axis` the instant Draw was pressed -- the page looked correct and could not be got past. **All four dispatch rows are driven live, each pinned by something only that row produces**: the melt's `eml_value`/`eml_series` pair, `@emlDrawTimeSeriesCI`'s own report of groups and observations per point, the right-hand pair with its `(right axis)` key entry, and the two-scale disclosure that says the lines are means and says why no interval is offered across two scales. A CI figure's filled bands are separated from every other leg's strokes by a pixel-count floor. **Both refusals are pinned on the words the user is shown, including the last line** -- one long `comment:` is wrapped on screen but sized as one row, so the button strip landed on the overflow and the tail of the three-measurement refusal, the half naming `press Draw again` and the way out, was drawn UNDER the OK button until this harness photographed it. The replication fork is pinned both ways and for both reasons: absent where the table has no repeats, and absent where it HAS them but the two series are on two scales. Seven columns give seven tickboxes, seven hues and seven fully stroked series, and the ten-band ink census says the seventh is on the page. **AND THE ANSWER IS INDEPENDENT OF THE STORAGE**, which is what section 16 is for: a table holding its two measurements STACKED -- one value column beside a column naming what was measured, the shape every EML stats tool in this plugin emits -- means what a table holding them side by side means, so it reaches the same right-hand axis page and draws the same figure. The series are then the LEVELS of the name column, counted by the same `@emlCountGroups` the draw layer counts its series with; two reach the axis page, three or more meet the refusal three columns meet, reworded for a page that has one column and no tickboxes to untick. `@emlGraphsPivotSeries` spreads the levels into a column each before the draw call -- the mirror image of the melt, recorded as a CONVERT step so a replay rebuilds it, and invisible to the user. **THE CLAIM PINNED ABOVE ALL THE OTHERS IS A FILE COMPARISON**: `long_titled` and `wide_titled` hold the same twenty-four points and the same two expressions in the two storages, walk the same five dialogs with the same keystrokes, and produce the same PNG -- byte for byte, one md5, one byte count. Their untitled twins `long_meas2` and `meas2` differ in 11463 pixels of 2160000 and every one of them is in rows 54 to 97 of 1200, the title line, because each title correctly names its own table; `harness/linetree/pngdiff.py` measures both pairs and the transcript carries the rows. `bash harness/linetree/run.sh` first -- a subset drives that subset and carries the rest over, refusing if the code has moved, because fifteen legs and three replays is over twenty minutes; `break.sh` drives thirteen deliberate defects, each watched red -- the key only on a grouped figure, the same column on both axes, the three-measurement refusal that never fires, an unconditional interval offer, a deaf second-axis gate, a five-column ceiling at the list the melt is fed, the series-name menu built over every column again (which is how the TIME column came to be seeded as the series namer), a melt that is not recorded as a step, a role that is not emitted, a column list that is not lifted into the block, the pivot dropped so the draw layer is handed the long table unspread, the level refusal removed so three levels get the column page's unfollowable advice, and the long branch falling back to one series, which is where this page stood the morning of 19 August | `harness/linetree/out/` |

**This table had a fourth column, headed *Checks*, and it was removed on 15
August 2026 for the reason the headline total was removed.** Thirty-nine
hand-maintained figures, each true on the day it was written and each free to
drift the moment its script gained an assertion, in a document whose subject is
count accuracy. It is also the column that made the table look like an
inventory when it is not one. **Treat this table as the narrative index — what
each script is *for*, what population it covers, and what that population
contains that the ones before it could not.** The inventory, complete and
current by construction, is what `validate/tools/gen_counts.R` prints from a
run: every script that reported, with its passes, its checks and its
attestations, stamped with the commit they were measured at.

**The narrative stopped at `v38` until 16 August 2026, and every validator
from `v39` to `v78` — forty of them — had no row here at all.** The counts
lint was clean throughout,
because it asks whether a total is stated and not whether an enumeration is
complete — which is this audit's recurring lesson arriving in the document that
records it: a defect survives when the unit of enumeration is wrong. The
missing rows were written by reading each script, not its filename, and each
says what population it covers and what that population holds that the earlier
ones exclude by construction. Where a row names a number, the number was read
off the script or its evidence; where a row names a predecessor that could not
have caught something, that predecessor's own reason is the one recorded here.

### Notes on individual scripts

**`v02`** is the load-bearing one for the adjustment logic. On that data the
Soprano–Mezzo contrast is `0.0527` under Bonferroni and `0.0228` under Holm —
opposite sides of .05. A control that relabelled the adjustment without
applying it could not produce that difference. It also checks Holm's
monotonicity constraint, which is the specific behaviour a naive step-down
implementation gets wrong.

**`v03`** cross-checks the RM-ANOVA twice: against the closed-form sums of
squares in `helpers.R`, and against base R's `aov()` with an `Error()`
stratum, which is an independent implementation. Both must agree with the
plugin.

**`v04`** covers the tied case: all three post-hoc raw p-values are
identical, so Holm must give all three the same adjusted value rather than
three different step values.

**`v05`** validates the Info window and the exported CSV separately. They
disagree in precision — the CSV carries `0.0000003` where the Info window
prints `< .001` — and checking only one surface would miss that.

---

## Expected failures

One check fails on purpose. Two more used to, and the record of what they
were is kept rather than deleted — a suite that quietly drops its failures
as they are fixed leaves no way to tell a fix from a deletion.

### 1. ~~`v06` pins a defect~~ — RESOLVED 5 August 2026

Finding **D15** was: under the heading *Paired t-test*, the plugin printed
`Matched-pairs r  0.971`. That value is the matched-pairs rank-biserial
correlation of the **Wilcoxon signed-rank** test. The correlation derived
from the paired *t* is **0.871**. Both are plausible and nothing on screen
distinguished them.

The plugin now reports Cohen's *d*z and *r*-from-*t* under the paired *t*,
and the rank-biserial *r* under Wilcoxon. `v06` was rewritten to assert the
corrected behaviour and **passes**; it retains a guard check that the two
effect sizes stay numerically distinct, so a future regression that routes
the rank statistic back under the parametric heading would fail the suite.

### 2. ~~`v07` fails on four open findings, and one undriven case~~
### — RESOLVED 6 August 2026, all seven cases driven

Six of the seven cases were driven on 5 August 2026, loaded into Praat
unchanged and taken through a wrapper. Four of them exposed defects — D96,
D97, D98, D99 — and all four were fixed on 6 August and the cases re-driven.
The seventh, R7, was driven headlessly on 6 August (see its row below), so
the red path is now complete. The table below records both states, because the fix is only
believable next to what it replaced.

| Case | Verdict | What happened |
|---|---|---|
| R1 | **Passes** (was partial) | States "complete cases n = 4" and "4 row(s) excluded for missing data" — the requirement, met twice. But every complete case was exactly linear, so the RM-ANOVA error term is identically zero, and the omnibus printed `F(2, 6) = 21110623253299200.0000` with a 48-place *p*, while its own post-hoc caught the same condition and refused. **D97 — FIXED 6 Aug**: the omnibus refuses too. The floor is relative (`ssErr <= 1e-10 * ssTot`); the residual sits at ~1e-16 of the total, so an equality test against zero would not have fired. |
| R2 | **Passes** (was failing) | **Two drives on two different tables — read the numbers with that in mind.** On 5 Aug, on the table `v07` constructs (70/72, 80/83, 90/94), the plugin computed *F*(2, 2) = 441.0000, *p* = 0.0023, GG epsilon = 0.5000 and three post-hoc *p*-values from two subjects with no comment of any kind. That is the defect, D98. The epsilon is itself the tell: 0.5 is exactly the 1/(*k*−1) floor forced by *n* = 2, and the plugin has that value in hand at print time. **FIXED 6 Aug**, and re-driven on a different two-subject table (10/14/21, 12/17/22) which gives *F*(2, 2) = 111.0000, *p* = 0.0089 — the numbers in `evidence/info/rp_r2_rmanova_info.txt`. A caution now prints directly under the GG line; the suite asserts its POSITION as well as its text, since at the foot of the report it would read as being about the post-hoc. The input changed between drives, so this row is a before/after on the *behaviour*, not on the *numbers*. |
| R3 | **Passes** | Refuses and names it: "All differences are identical (zero variance)". Fabricates no statistic. |
| R4 | **Passes** | Refuses and names the group and its *n*: "Group ""Soprano"": n=6, group ""Alto"": n=1". |
| R5 | **Passes** (was partial) | Refused before computing, which was the important half, but named only the first offending group — six singletons, six attempts — and leaked the internal procedure name. **D99 — FIXED 6 Aug**: "Group column ""singer_id"" has 6 groups for 6 rows - one per row. This is an identifier column, not a grouping column." |
| R6 | **Passes** (was partial) | First recorded as a silent row-drop. That was **wrong** — the plugin printed `N (valid) 4` and `N (undefined) 1`. What it could not do was tell an empty cell from an unparseable string from a European decimal comma. Worse than the report suggested: `Get value:` returns 1 for `"1,5"`, so the comma cell was not dropped at all, it entered the mean as a different number. **D96 — FIXED 6 Aug**: one classifier, `@eml_classifyCell`, used by every extraction path including the row-wise ones; the three conditions are reported separately with row and value named, and the comma cell is excluded rather than guessed at. |
| R7 | **Passes** (was not driven) | The small-range measure, D88 as a data case: contact quotient in 0.401–0.548, where a `roundTo` of 10 gives a 0–10 axis and the data occupies 1.5% of the panel. **Driven 6 Aug 2026**, headlessly — the draw procedures call no `beginPause:`, so the whole objection that this case "had to be judged from a figure" was never true; the axis is a number and can be read like any other. `@emlDrawSpaghettiPlot` on the reshaped table gives an axis of 0.380–0.580, so the data occupies **73.5%** of the panel. Capture: `evidence/info/v07_r7_axis_info.txt`. Figure: `evidence/figures/r7_small_range_axis.png`. |

The requirement each case was written against, stated before the drive so
the drive could fail:

| Case | Input | Behaviour the plugin must show |
|---|---|---|
| R1 | 8 subjects, 4 complete cases | Report the complete-case count it analysed, or refuse. Silently analysing 4 while the table shows 8 is a defect |
| R2 | n = 2 subjects, k = 3 | df error = 2. Compute or refuse, but say which; do not present a p-value from df 2 without comment |
| R3 | Zero variance throughout | Refuse, naming the zero variance |
| R4 | One group with n = 1 | Refuse, naming the group and its n |
| R5 | Grouping column unique per row | Refuse before running, naming group count against row count. 15 pairs would otherwise be attempted, none estimable |
| R6 | Non-numeric entry in a measure column | Reject the column **by type**, naming it. Not "incomplete data" — see D83. *This requirement was itself too strong: the 21 July complete-case convention says analyse and state the exclusion. What the case actually tests is whether the three causes of exclusion can be told apart* |
| R7 | Contact quotient, range 0.40–0.55 | After the D88 fix, an axis that fits the data. With `roundTo = 10` the axis is 0–10 and the data occupies 2% of the panel |

---

## Coverage, stated honestly

**Covered.** Twelve of the fourteen orchestrators in
`stats/eml-analysis.praat`, each driven through its real GUI and checked
against base R: two-group, *k*-group ANOVA with Tukey, Kruskal-Wallis with
Dunn, two-way, pairwise with Bonferroni and Holm, paired *t*, RM-ANOVA with
Greenhouse-Geisser, Friedman, correlation, regression, descriptive, and
normality including the parametric/nonparametric recommendation it issues.
Plus one CSV export row and all seven red-path cases.

### One dataset per test — say so plainly

Every orchestrator above is driven on **exactly one input table**, and this
is the single largest limit on what a green run means. The shapes:

| Script | Input | n | Design |
|---|---|---|---|
| v01 | `demo_3groups_input.csv` | 45 | 3 × 15, balanced |
| v02 | `demo_3groups_b_input.csv` | 45 | 3 × 15, balanced |
| v03, v04 | `demo_rm3_input.csv` | 20 | 3 within-subject levels, complete |
| v05, v06 | `demo_paired_input.csv` | 20 | complete pairs |
| v08 | `v08_twogroup_input.csv` | 40 | 2 × 20, balanced |
| v09 | `v09_anova_tukey_input.csv` | 45 | 3 × 15, balanced |
| v10 | `v10_kw_dunn_input.csv` | 45 | 3 × 15, balanced |
| v11 | `v11_twoway_input.csv` | 48 | 2 × 2 × 12, fully crossed and balanced |
| v12 | `v12_correlation_input.csv` | 30 | bivariate |
| v13 | `v13_regression_input.csv` | 25 | one predictor |
| v14 | `v14_descriptive_input.csv` | 45 | 3 × 15 |
| v15 | `v15_normality_input.csv` | 40 | three measured columns |
| v07, v16, v17 | constructed in-script | 2–8 | degenerate / export / broom parity |

What that table implies, stated as findings rather than left to be noticed:

- **Every between-groups design is balanced.** No unequal-*n* case reaches
  Tukey, Dunn, Games-Howell, or the two-way sums of squares. Type I vs
  Type III SS cannot diverge on a balanced crossed design, so v11 cannot
  distinguish them — the convention recorded above is asserted, not tested.
- **`v10_kw_dunn_input.csv` has no ties**: 45 values, 45 distinct. The
  plugin's Dunn/Kruskal-Wallis tie correction has therefore never executed
  on a driven dataset. It is covered only by the primitives suite under
  `plugin/dev/tests/`. This is the sharpest single gap in the folder.
- **No missing data outside the red path.** Every green-path input is
  complete; the complete-case convention is exercised only by R1 and R6.
- **No ill-conditioned or wide-magnitude data.** Every measured column sits
  in an ordinary acoustic range (F0 in Hz, SPL in dB, jitter in percent).
  Nothing here would separate a numerically stable implementation from an
  unstable one — the class of failure the NIST StRD sets are built to find.

None of this makes a passing check wrong. It bounds what the checks are
evidence *of*: correct arithmetic on well-conditioned, balanced, complete
data, which is the common case and not the hard one.

### The tiers that answer it

`v18_sweep_parity.R` (added 6 August 2026) takes the first two of those
findings off the list for one-way ANOVA, Tukey and Kruskal-Wallis. It is a
different kind of evidence and is labelled as such: the cases are generated
headlessly by `harness/sweep/tierB_grid.praat`, which calls the shipping
procedures directly under `praat --run`, so there is no click-through
provenance behind any of its checks. What it establishes is that the
procedure is right on 16 shapes the GUI drives never produced — including
6:1 unbalanced Tukey-Kramer and heavy ties in Kruskal-Wallis.

Two further tiers exist as harness rather than as committed evidence:

- `harness/sweep/tierA_properties.praat` — 22 invariance and refusal
  properties that hold whatever the data is (one-way *F* = *t*² at *k* = 2,
  location invariance, scale equivariance, group-label permutation
  invariance, rank invariance of *H* under a monotone transform, matrix
  symmetry, and eight malformed-input cases whose refusal must *name* what
  is wrong). No oracle and no certified value, so nothing is transcribed.

  A correction, recorded rather than quietly edited: an earlier version of
  this line called `lowerCase$` a portability defect Tier A had found. It is
  not one. The plugin floors at Praat **6.6.30** — enforced in `setup.praat`,
  which refuses to register any menu item below it — and `lowerCase$` exists
  there. What Tier A actually hit was the sandbox's Praat 6.4.06, which is
  below the supported floor. `eml_normalizeLabel` now uses an equivalent
  regex only so the harness runs wherever CI puts it.
- `validate/tools/nist_ingest.R` + `validate/lre.R` — the NIST StRD tier.
  The certified values are parsed out of NIST's published `.dat` files, never
  transcribed, and scored as **log relative error** (correct significant
  digits) rather than pass/fail, which is the convention StRD work is
  reported in. The `.dat` files are not redistributed here; `nist_ingest.R`
  documents the two `curl` lines that fetch them. **This is the only external
  authority anywhere in this folder** — every other check is ultimately "R
  agrees", and R is a peer, not a referee.

**Not covered, and why.** The LMM orchestrator (module tabled by author
ruling of 4 August) and the reliability orchestrator (a Phase 4 stub that
returns "not yet implemented").

**Not converted to the broom shape, but EXPORTABLE since 14 August 2026:
`@emlRunDescriptiveAnalysis`.**
Recorded here on 13 August 2026 because nothing said so and the omission reads
like an oversight. Two reasons, either sufficient. First, broom has no `tidy`
method for a summary of a vector, and eleven of the fifteen quantities the
procedure produces -- mean, median, sd, variance, sem, q1, q3, iqr, min, max,
range -- have no column in the vocabulary `eml-result-writer.praat` enforces;
declaring it would mean inventing eleven non-broom names and then defending
them in the parity check as broom-shaped. Second, and decisively:
`harness/broom_cases/contamination_probe.praat` uses this procedure as the
canonical UNCONVERTED path, to prove a converted analysis does not leak its
declaration into the next one. Converting it breaks that probe. The legacy
long format is the right container for a heterogeneous bag of named scalars.

**AUTHOR RULING, 14 August 2026, correcting the last clause of the above.**
That paragraph used to end "and its export is correctly gated off in the
wizard (`wizCanExport` stays 0)". Both reasons for not converting are still
right; the conclusion drawn from them was not. Describe and normality must be
able to save. `@emlRunDescriptiveAnalysis` now fills the LEGACY buffer through
`@emlCSVAddDescriptiveRow` -- sixteen statistics, no invented broom column
names -- and the wizard sets `wizCanExport = 1`. It still does not declare, so
`contamination_probe.praat` keeps its canonical unconverted subject and the
second reason above stands untouched.

A first attempt DID declare into tidy, and would have shipped a file carrying
`term` and `method` and nothing else: `@eml_orderedCols` walks `emlVocabTidy$`
as a WHITELIST and drops what is not in it, without comment. The first reason
recorded here on 13 August is what stopped that, which is the argument for
writing reasons down rather than conclusions.

**The last clause of that paragraph is out of date, and the correction belongs
beside it rather than in place of it.** Driven on 16 August 2026 with two new
declarations added and the vocabulary deliberately left un-extended, the run
does not ship a thin file: `@eml_vocabCheck` halts, naming the column, listing
the legal ones, and naming both files that have to change in the same commit.
The silent path closed when that guard was written. What is still silent is
`@eml_orderedCols`' other rule — a legal column that is EMPTY IN EVERY ROW is
omitted from the written file with no comment — which is why `v71` trusts the
vocabulary for neither half and reads the written CSV instead.

`validate/v49_every_path_exports.R` now enumerates every terminal branch of
the wizard and every analysis wrapper out of the source, and fails if any of
them cannot reach the export step. It found a fourth gap nobody had named:
`scripts/eml-check-normality.praat`, the standalone menu wrapper, ran an
orchestrator that had declared correctly for weeks and then offered the user
no way to keep the result.

### What this folder is NOT the only validation of

An earlier version of this section said a 2 August test inventory had found
coverage across the procedure library "uneven rather than complete". That
was superseded on 4 August and is withdrawn. It understated the plugin.

**The statistics themselves are already externally validated, and not by
this folder.** `plugin/dev/tests/` holds the library's own suites, and the
numeric literals in them were transcribed from R or scipy, never from the
plugin's own output — the rule that makes a suite a test rather than a
photograph. Provenance, including interpreter versions, is recorded in
`plugin/dev/tests/REFERENCE_PROVENANCE.md`.

- `stats/eml-inferential.praat` — **28 of 28 procedures under an external
  oracle**, 442 passing checks (409 from eight base-R scripts, 33 from a <!-- count-scope: plugin/dev/tests primitives suite, not validate/ -->
  scikit-posthocs Dunn verifier).

  <!-- This 442 is the PRIMITIVES suite under plugin/dev/tests/. It is not
  the validate/ figure and does not move when validate/ changes. On 6 Aug
  2026 a global search-and-replace aimed at V1's stale validate/ counts
  overwrote it with the validate/ headline and destroyed the 409 + 33
  breakdown, leaving an orphaned parenthesis — in a document whose subject
  is count accuracy. Restored, and flagged here so the next sweep does not
  do it again. --> Welch and Student *t*, Mann-Whitney,
  one-way and two-way ANOVA, Tukey, Kruskal-Wallis, Dunn, Scheffé,
  Pearson and Spearman, linear regression, Theil-Sen, Shapiro-Wilk,
  Bonferroni, Holm, Benjamini-Hochberg, Cohen's *d*, rank-biserial. Four
  of the 28 are private helpers reached only through oracled callers.
- `stats/eml-core-descriptive.praat` — asserted against closed-form
  analytic values. For mean, median, variance, SD, percentile and MAD the
  closed form is the stronger oracle; R would only re-derive the same
  arithmetic. Validated, by a different and adequate method.

The measurement above is from `audit/reports/CORRECTION_coverage_2026-08-04.md`,
which also withdraws an earlier "5% externally validated" figure produced by
grepping Praat procedure names inside the `.R` files. That grep counted
*mentions*, not oracles, and was wrong.

### The gap this folder exists to close

**`stats/eml-analysis.praat` — 14 orchestrators, none with a direct
external oracle.** These procedures do no arithmetic. They pull columns
out of a Table, decide which already-validated primitive to call, hand it
arguments, and assemble the printed report. Seven are touched by
`dev/tests/phase2/test-workflow-verification.praat`, but that suite checks
that report procedures emit the expected *markers* — headings, spacing,
Info persistence — not that the right value lands under the right one.

That layer is where finding **D15** lived: Cohen's *d*z and the
matched-pairs rank-biserial were both individually R-validated and
passing, and the plugin still printed the rank statistic under the heading
*Paired t-test*. Every primitive green, the assembly wrong. A
primitive-level suite cannot reach that by construction, which is the
argument for this folder existing at all.

Scripts `v01`–`v06` cover four of the fourteen: pairwise, RM-ANOVA,
Friedman, and paired *t*. Scripts `v08`–`v15` cover the remaining eight —
two-group, ANOVA with Tukey, Kruskal-Wallis with Dunn, two-way,
correlation, regression, descriptive, and normality. Two of the fourteen
are out of scope: the LMM orchestrator (module tabled by author ruling of
4 August) and the reliability orchestrator (a Phase 4 stub that returns
"not yet implemented").

**Only partly a validation of the graphing layer.** `v23` is the first
R-side check of anything drawn: it reads the Q-Q figure's own plotted point
pairs and binds them to the same numbers the reported *W* came from.

**This paragraph said the annotation layer — `@emlBridgeGroupComparison` and
everything it renders onto a figure — was "still uncovered", and that stopped
being true and is corrected here rather than quietly deleted.** `v32` renders
the comparison matrix the bridge populates and counts the dark pixels inside
its band; `v61` drives the bridge on the *k* ≥ 3 nonparametric arm, which is
where it used to abort; `v66` and `v69` read what the bridge draws — the
post-hoc sub-line and the bracket caption — off the rendered figure. What is
still true is narrower and worth keeping in the sharper form: **the bridge is a
SECOND orchestrator, and nothing in this folder recomputes its statistics.**
The 14 August 2026 audit did it by hand against scipy and found no
disagreement anywhere, which is a record and not a check. Closing that is the
Ruling 2 unification, which requires the harness to land with the change rather
than after it. Purely visual defects (D88, D89, D90) remain recorded in the
audit log and are not testable from R.

### Refused permanently, with the reason on record

**Durbin–Watson.** Not implemented, and not to be implemented. Two reasons,
the second being the binding one:

1. Its *p*-value needs eigenvalues, and `stats/eml-linalg.praat` has no eigen
   routine.
2. **The statistic is order-dependent and the plugin's Tables carry no
   ordering semantics.** On `practice_hrs_wk` it would report autocorrelation
   in *spreadsheet row order* — a number that looks meaningful, is not, and
   gives no signal to the user that it is not. Adding an eigen routine would
   fix reason 1 and leave reason 2 exactly where it is.

### What a clean clone structurally cannot show — finding P1

**Finding P1 (file modes) cannot be validated from this repository, and it is
recorded here so that its absence from every green run is not read as its
absence from the product.** Thirteen of the twenty-one files in
`plugin/scripts/` were mode `0600` in the built tree, which makes the plugin
unreadable to any account other than the one that installed it. **Those two
figures are the measurement of 4 August 2026 and are a fact about the built
tree of that day, not about this checkout**, which is why they are not
maintained: the folder has gained files since, so a reader who counts it today
will get a different denominator and should read the finding against
`audit/DRIVE_FINDINGS_2026-08-04.md`, which names the thirteen. Git records
only the executable bit. The permission bits therefore do not survive a commit,
do not appear in a `git diff`, and cannot be asserted by anything in this
folder: a checker written here would read the modes of a checkout, which are
whatever the cloning umask made them, and would pass on a clone of a defective
package.

The 14 August 2026 audit confirmed both halves of that. The clone it examined
was clean, and the audit's verdict was that the check belongs to the packaging
step rather than to the repository. A reader checking that today will find one
file under `plugin/` that is not `0644` — `dev/tools/vacuity-negative-controls.py`,
at `0755` since 12 August — and it is the exception that states the rule
rather than a contradiction of it: the executable bit is the one permission git
does record, `git ls-files -s` shows it as `100755`, and it is therefore the
only mode in this tree that a clone is guaranteed to reproduce. So the discipline
is `chmod 0644` across the tree before zipping, and verification **on the built
artefact**, which is the only place the question is answerable. A green suite
here says nothing about it either way, and is not supposed to.

### Documented gaps, ruled out of Phase One rather than overlooked

- **Histogram with normal overlay** beside the Q-Q. Ruled out on the
  convention argument: the Q-Q is the normality figure in R's standard
  diagnostic set, and `Shapiro–Wilk` + Q-Q is the pairing a reviewer expects.
  Revisitable on practitioner feedback.
- **`Describe Table column` has no visual output** and no completion dialog
  to hang one on. Adding one means a new dialog, a new include of the graph
  layer, and a column picker. Ruled a Phase One gap, not a Phase One task.

---

## For an independent reviewer

The useful question is not whether the numbers agree — they do, and you can
confirm that in one command. It is whether the *comparisons are the right
ones*:

- Are the tolerances defensible given what the plugin displays?
- Is the Greenhouse-Geisser implementation in `helpers.R` the standard one,
  and does the `aov()` cross-check in `v03` genuinely constitute an
  independent path?
- Is the rank-biserial convention in `helpers.R` the one that makes the D15
  claim correct? The whole finding turns on that.
- Are the red-path cases in `v07` the ones worth having, and what is missing?

Four conventions in particular are asserted rather than derived, and each
would change numbers across several scripts if you disagree:

- **Rank-biserial sign.** `rank_biserial_indep` uses (*U*₁ − *U*₂)/(*n*₁*n*₂),
  positive when group 1 dominates, so its sign agrees with Cohen's *d*. The
  convention 1 − 2*U*₁/(*n*₁*n*₂) gives the opposite sign throughout v08 and
  v10.
- **Skewness and kurtosis.** The sample-corrected G1 and G2, matching what
  `@emlSkewness` and `@emlKurtosis` compute. Not the population moments,
  which differ in the second decimal on v14's data — inside the printed
  precision.
- **Quartiles.** R's `quantile` type 7. Type 6 differs in the third decimal
  on the same column.
- **Dunn's test.** Dunn (1964) with the standard tie correction. The
  plugin's own suite checks its Dunn against scikit-posthocs; `helpers.R` is
  a third path in base R, and all three agree to four decimals on v10.

The four failing checks in `v07` are also worth arguing with. Each asserts
that a plugin behaviour is wrong. If you think R2's uncommented *F*(2, 2) or
R6's silent row-drop is acceptable, say so — the finding, not the check, is
what would be wrong.

Disagreement on any of these is more valuable than a passing run.
